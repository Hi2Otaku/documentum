---
phase: 02-document-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/models/document.py
  - src/app/models/__init__.py
  - src/app/core/config.py
  - src/app/core/minio_client.py
  - src/app/schemas/document.py
  - src/app/main.py
  - docker-compose.yml
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-07, DOC-08]

must_haves:
  truths:
    - "Document and DocumentVersion models exist and can be created in the database"
    - "MinIO client is initialized and documents bucket is created at startup"
    - "Settings include MinIO configuration"
    - "Pydantic schemas cover upload, response, version listing, and metadata update"
  artifacts:
    - path: "src/app/models/document.py"
      provides: "Document and DocumentVersion SQLAlchemy models"
      contains: "class Document"
    - path: "src/app/core/minio_client.py"
      provides: "MinIO client singleton with upload/download/delete helpers"
      contains: "minio_client"
    - path: "src/app/schemas/document.py"
      provides: "Pydantic request/response schemas for documents"
      contains: "class DocumentResponse"
    - path: "src/app/core/config.py"
      provides: "MinIO connection settings"
      contains: "minio_endpoint"
  key_links:
    - from: "src/app/models/document.py"
      to: "src/app/models/base.py"
      via: "inherits BaseModel"
      pattern: "class Document\\(BaseModel\\)"
    - from: "src/app/core/minio_client.py"
      to: "src/app/core/config.py"
      via: "reads settings.minio_*"
      pattern: "settings\\.minio_"
    - from: "src/app/main.py"
      to: "src/app/core/minio_client.py"
      via: "calls ensure_documents_bucket at startup"
      pattern: "ensure_documents_bucket"
---

<objective>
Create the data layer for document management: SQLAlchemy models (Document, DocumentVersion), MinIO client module, Pydantic schemas, and config settings.

Purpose: Establish all contracts (models, schemas, MinIO helpers) that Plan 02 (service + router) will implement against.
Output: Models, schemas, MinIO client, config additions, docker-compose env vars.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-document-management/02-RESEARCH.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From src/app/models/base.py:
```python
class Base(DeclarativeBase): pass

class BaseModel(Base):
    __abstract__ = True
    id: Mapped[uuid.UUID] = mapped_column(Uuid(), primary_key=True, default=uuid.uuid4)
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]
    created_by: Mapped[str | None]
    is_deleted: Mapped[bool]
```

From src/app/models/user.py:
```python
class User(BaseModel):
    __tablename__ = "users"
    id  # UUID PK
    username, email, hashed_password, is_active, is_superuser
```

From src/app/core/config.py:
```python
class Settings(BaseSettings):
    database_url: str
    redis_url: str = "redis://localhost:6379/0"
    secret_key: str
    # ... no MinIO settings yet
settings = Settings()
```

From src/app/schemas/common.py:
```python
class PaginationMeta(BaseModel): page, page_size, total_count, total_pages
class EnvelopeResponse(BaseModel, Generic[T]): data, meta, errors
```

From src/app/models/__init__.py:
```python
# Must register Document, DocumentVersion here for Base.metadata
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document models, MinIO client, and config settings</name>
  <files>src/app/models/document.py, src/app/core/minio_client.py, src/app/core/config.py, src/app/models/__init__.py, src/app/main.py, docker-compose.yml</files>
  <read_first>src/app/models/base.py, src/app/models/user.py, src/app/models/__init__.py, src/app/core/config.py, src/app/main.py, docker-compose.yml</read_first>
  <action>
1. **Create `src/app/models/document.py`** with two models inheriting from BaseModel:

   **Document model** (`__tablename__ = "documents"`):
   - `title`: String(500), not null
   - `author`: String(255), nullable
   - `filename`: String(500), not null (original upload filename)
   - `content_type`: String(255), default "application/octet-stream", not null
   - `custom_properties`: JSON column (use `sqlalchemy.JSON`, NOT JSONB -- per Phase 1 decision for dialect-agnostic models), default=dict, not null. Per user decision: schema-free, any JSON key-value pairs.
   - `locked_by`: Uuid(), ForeignKey("users.id"), nullable (per DOC-03)
   - `locked_at`: DateTime(timezone=True), nullable
   - `current_major_version`: Integer, default=0, not null
   - `current_minor_version`: Integer, default=1, not null
   - `versions`: relationship to DocumentVersion, back_populates="document", order_by="DocumentVersion.created_at"

   **DocumentVersion model** (`__tablename__ = "document_versions"`):
   - `document_id`: Uuid(), ForeignKey("documents.id"), not null
   - `major_version`: Integer, not null (per user decision: initial is 0, major on lifecycle promotion)
   - `minor_version`: Integer, not null (per user decision: initial is 1, increment on check-in)
   - `content_hash`: String(64), not null (SHA-256 hex digest for dedup per user decision)
   - `content_size`: Integer, not null (bytes)
   - `minio_object_key`: String(500), not null (format: "{doc_id}/{version_id}" per user decision)
   - `filename`: String(500), not null
   - `content_type`: String(255), default "application/octet-stream", not null
   - `comment`: Text, nullable
   - `document`: relationship back_populates="versions"
   - Add UniqueConstraint on (document_id, major_version, minor_version) via `__table_args__`

   Import UUID, DateTime, ForeignKey, Integer, JSON, String, Text, Uuid from sqlalchemy.
   Import UniqueConstraint from sqlalchemy.

2. **Update `src/app/models/__init__.py`**: Add imports for Document and DocumentVersion from app.models.document. Add them to `__all__`.

3. **Update `src/app/core/config.py`**: Add four MinIO settings to the Settings class:
   - `minio_endpoint: str = "localhost:9000"`
   - `minio_access_key: str = "minioadmin"`
   - `minio_secret_key: str = "minioadmin"`
   - `minio_secure: bool = False`

4. **Create `src/app/core/minio_client.py`**: Module-level MinIO client singleton with async helpers wrapped in `asyncio.to_thread()` (MinIO SDK is synchronous -- per research pitfall #1):
   - `minio_client = Minio(endpoint=settings.minio_endpoint, access_key=settings.minio_access_key, secret_key=settings.minio_secret_key, secure=settings.minio_secure)`
   - `DOCUMENTS_BUCKET = "documents"` (per user decision: single bucket)
   - `async def ensure_documents_bucket()`: Check if bucket exists, create if not. Wrap in asyncio.to_thread.
   - `async def upload_object(object_name: str, data: bytes, content_type: str) -> str`: put_object via BytesIO, return object_name. Wrap in asyncio.to_thread.
   - `async def download_object(object_name: str) -> bytes`: get_object, read, close+release_conn in finally. Wrap in asyncio.to_thread.
   - `async def delete_object(object_name: str) -> None`: remove_object. Wrap in asyncio.to_thread.
   Log bucket creation with `logging.getLogger(__name__)`.

5. **Update `src/app/main.py`**: In the `lifespan` function, after `seed_admin()`, add a try/except block calling `await ensure_documents_bucket()` from `app.core.minio_client`. Log warning if MinIO not available (same pattern as seed_admin).

6. **Update `docker-compose.yml`**: Add MinIO env vars to the `api` service environment:
   - `MINIO_ENDPOINT=minio:9000`
   - `MINIO_ACCESS_KEY=minioadmin`
   - `MINIO_SECRET_KEY=minioadmin`
   - `MINIO_SECURE=false`
   Also add `minio` to the `api` service `depends_on` with `condition: service_healthy`.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -c "from app.models.document import Document, DocumentVersion; print('Models OK')" && python -c "from app.core.minio_client import minio_client, upload_object, download_object, delete_object, ensure_documents_bucket, DOCUMENTS_BUCKET; print('MinIO client OK')" && python -c "from app.core.config import settings; assert hasattr(settings, 'minio_endpoint'); print('Config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class Document(BaseModel)" src/app/models/document.py` succeeds
    - `grep -q "class DocumentVersion(BaseModel)" src/app/models/document.py` succeeds
    - `grep -q "custom_properties.*JSON" src/app/models/document.py` succeeds (JSON not JSONB)
    - `grep -q "locked_by" src/app/models/document.py` succeeds
    - `grep -q "content_hash" src/app/models/document.py` succeeds
    - `grep -q "minio_object_key" src/app/models/document.py` succeeds
    - `grep -q "UniqueConstraint" src/app/models/document.py` succeeds
    - `grep -q "minio_endpoint" src/app/core/config.py` succeeds
    - `grep -q "asyncio.to_thread" src/app/core/minio_client.py` succeeds
    - `grep -q "DOCUMENTS_BUCKET" src/app/core/minio_client.py` succeeds
    - `grep -q "ensure_documents_bucket" src/app/main.py` succeeds
    - `grep -q "Document" src/app/models/__init__.py` succeeds
    - `grep -q "MINIO_ENDPOINT" docker-compose.yml` succeeds
  </acceptance_criteria>
  <done>Document and DocumentVersion models exist with all columns. MinIO client module provides async upload/download/delete/ensure_bucket. Config has MinIO settings. Models registered in __init__.py. App startup ensures bucket. Docker compose passes MinIO env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic schemas for documents</name>
  <files>src/app/schemas/document.py</files>
  <read_first>src/app/schemas/common.py, src/app/schemas/user.py, src/app/models/document.py</read_first>
  <action>
Create `src/app/schemas/document.py` with these Pydantic v2 models:

1. **DocumentUpload** (for POST /documents multipart metadata -- these come as form fields alongside the file):
   - `title: str` (min_length=1, max_length=500)
   - `author: str | None = None`
   - `custom_properties: dict[str, Any] | None = None`

2. **DocumentUpdate** (for PUT /documents/{id} metadata update):
   - `title: str | None = None` (max_length=500)
   - `author: str | None = None`
   - `custom_properties: dict[str, Any] | None = None`

3. **DocumentResponse** (API response):
   - `id: uuid.UUID`
   - `title: str`
   - `author: str | None`
   - `filename: str`
   - `content_type: str`
   - `custom_properties: dict[str, Any]`
   - `locked_by: uuid.UUID | None`
   - `locked_at: datetime | None`
   - `current_major_version: int`
   - `current_minor_version: int`
   - `current_version: str` (computed property returning "{major}.{minor}")
   - `created_at: datetime`
   - `updated_at: datetime`
   - `created_by: str | None`
   - `is_deleted: bool`
   - Use `model_config = ConfigDict(from_attributes=True)`
   - Add a `@computed_field` or `@model_validator(mode='after')` to produce `current_version` from major/minor.

4. **DocumentVersionResponse** (version history):
   - `id: uuid.UUID`
   - `document_id: uuid.UUID`
   - `major_version: int`
   - `minor_version: int`
   - `version_label: str` (computed: "{major}.{minor}")
   - `content_hash: str`
   - `content_size: int`
   - `filename: str`
   - `content_type: str`
   - `comment: str | None`
   - `created_at: datetime`
   - `created_by: str | None`
   - Use `model_config = ConfigDict(from_attributes=True)`

5. **CheckinRequest** (for POST /documents/{id}/checkin form fields):
   - `comment: str | None = None`

6. **DocumentListParams** (query params for GET /documents):
   - `page: int = 1` (ge=1)
   - `page_size: int = 20` (ge=1, le=100)
   - `title: str | None = None` (filter)
   - `author: str | None = None` (filter)

Import from pydantic: BaseModel, ConfigDict, computed_field, Field.
Import uuid, datetime, Any from typing.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -c "from app.schemas.document import DocumentUpload, DocumentUpdate, DocumentResponse, DocumentVersionResponse, CheckinRequest, DocumentListParams; print('Schemas OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class DocumentUpload" src/app/schemas/document.py` succeeds
    - `grep -q "class DocumentResponse" src/app/schemas/document.py` succeeds
    - `grep -q "class DocumentVersionResponse" src/app/schemas/document.py` succeeds
    - `grep -q "class CheckinRequest" src/app/schemas/document.py` succeeds
    - `grep -q "from_attributes" src/app/schemas/document.py` succeeds
    - `grep -q "current_version" src/app/schemas/document.py` succeeds
    - `grep -q "version_label" src/app/schemas/document.py` succeeds
  </acceptance_criteria>
  <done>All Pydantic schemas for document endpoints exist. DocumentResponse and DocumentVersionResponse have computed version strings. Schemas validate correctly.</done>
</task>

</tasks>

<verification>
- All models import successfully: `python -c "from app.models import Document, DocumentVersion"`
- All schemas import successfully: `python -c "from app.schemas.document import DocumentResponse"`
- MinIO client imports: `python -c "from app.core.minio_client import minio_client"`
- Config has MinIO settings: `python -c "from app.core.config import settings; print(settings.minio_endpoint)"`
</verification>

<success_criteria>
- Document and DocumentVersion models defined with all required columns
- MinIO client module with async-wrapped upload/download/delete/ensure_bucket
- Config extended with minio_endpoint, minio_access_key, minio_secret_key, minio_secure
- Pydantic schemas cover upload, update, response, version response, checkin, list params
- Models registered in __init__.py
- App lifespan ensures MinIO bucket on startup
- Docker compose passes MinIO env vars to api service
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-management/02-01-SUMMARY.md`
</output>
