---
phase: 19-event-driven-activities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/models/enums.py
  - src/app/models/workflow.py
  - src/app/schemas/template.py
  - src/app/services/engine_service.py
  - src/app/services/event_handlers.py
  - src/app/services/template_service.py
  - alembic/versions/phase19_001_event_activities.py
  - tests/test_event_activities.py
autonomous: true
requirements:
  - EVTACT-01
  - EVTACT-02
  - EVTACT-03

must_haves:
  truths:
    - "EVENT activity template can be created with event_type_filter and event_filter_config"
    - "Template validation rejects EVENT activity without event_type_filter"
    - "EVENT activity stays ACTIVE when engine reaches it (no work items created)"
    - "EVENT activity completes automatically when matching document.uploaded event fires"
    - "EVENT activity completes automatically when matching lifecycle.changed event fires"
    - "EVENT activity completes automatically when matching workflow.completed event fires"
    - "EVENT activity ignores non-matching events"
    - "EVENT activity with event_filter_config only matches events whose payload matches the filter"
  artifacts:
    - path: "src/app/models/enums.py"
      provides: "EVENT value in ActivityType enum"
      contains: "EVENT = \"event\""
    - path: "src/app/models/workflow.py"
      provides: "event_type_filter and event_filter_config columns on ActivityTemplate"
      contains: "event_type_filter"
    - path: "src/app/services/event_handlers.py"
      provides: "Three event handlers + shared _try_complete_event_activities function"
      contains: "_try_complete_event_activities"
    - path: "tests/test_event_activities.py"
      provides: "Test coverage for EVTACT-01, EVTACT-02, EVTACT-03"
      min_lines: 80
  key_links:
    - from: "src/app/services/event_handlers.py"
      to: "src/app/services/engine_service.py"
      via: "_advance_from_activity call inside _try_complete_event_activities"
      pattern: "_advance_from_activity"
    - from: "src/app/services/engine_service.py"
      to: "src/app/models/enums.py"
      via: "ActivityType.EVENT case in dispatch block"
      pattern: "ActivityType\\.EVENT"
    - from: "src/app/services/template_service.py"
      to: "src/app/models/enums.py"
      via: "MISSING_EVENT_TYPE validation rule"
      pattern: "MISSING_EVENT_TYPE"
---

<objective>
Add the EVENT activity type to the backend: enum, model columns, migration, Pydantic schemas, engine dispatch, event bus handlers for the three supported event types, template validation, and integration tests.

Purpose: This is the core engine logic that enables workflows to pause at EVENT activities and automatically resume when a matching domain event fires.
Output: Fully working backend EVENT activity support with passing tests.
</objective>

<execution_context>
@.claude/workflows/execute-plan.md
@.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-event-driven-activities/19-RESEARCH.md

@src/app/models/enums.py
@src/app/models/workflow.py
@src/app/schemas/template.py
@src/app/services/engine_service.py
@src/app/services/event_handlers.py
@src/app/services/event_bus.py
@src/app/services/template_service.py
@alembic/versions/phase18_001_sub_workflows.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/models/enums.py:
```python
class ActivityType(str, enum.Enum):
    START = "start"
    END = "end"
    MANUAL = "manual"
    AUTO = "auto"
    SUB_WORKFLOW = "sub_workflow"
    # Add: EVENT = "event"
```

From src/app/models/workflow.py (ActivityTemplate, lines 84-112):
```python
class ActivityTemplate(BaseModel):
    __tablename__ = "activity_templates"
    # ... existing columns ...
    sub_template_id: Mapped[uuid.UUID | None] = mapped_column(Uuid(), ForeignKey("process_templates.id"), nullable=True)
    variable_mapping: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    # Add: event_type_filter, event_filter_config
```

From src/app/services/event_handlers.py:
```python
@event_bus.on("workflow.completed")
async def _resume_parent_on_child_complete(db: AsyncSession, event: DomainEvent) -> None:
    # Pattern to follow for EVENT activity handlers
```

From src/app/services/template_service.py (line 795):
```python
# 7b. MISSING_SUB_TEMPLATE validation pattern to replicate for MISSING_EVENT_TYPE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model, migration, schemas, and enum for EVENT activity type</name>
  <files>
    src/app/models/enums.py,
    src/app/models/workflow.py,
    src/app/schemas/template.py,
    alembic/versions/phase19_001_event_activities.py,
    tests/test_event_activities.py
  </files>
  <read_first>
    src/app/models/enums.py,
    src/app/models/workflow.py,
    src/app/schemas/template.py,
    alembic/versions/phase18_001_sub_workflows.py
  </read_first>
  <behavior>
    - Test: Creating an ActivityTemplate with activity_type=EVENT, event_type_filter="document.uploaded" succeeds
    - Test: ActivityTemplateResponse includes event_type_filter and event_filter_config fields
    - Test: Template validation rejects EVENT activity without event_type_filter (MISSING_EVENT_TYPE error)
  </behavior>
  <action>
    1. Add `EVENT = "event"` to ActivityType enum in `src/app/models/enums.py` (after SUB_WORKFLOW).

    2. Add two nullable columns to ActivityTemplate in `src/app/models/workflow.py`:
       - `event_type_filter: Mapped[str | None] = mapped_column(String(255), nullable=True)` -- which event type this activity listens for (e.g., "document.uploaded")
       - `event_filter_config: Mapped[dict | None] = mapped_column(JSON, nullable=True)` -- optional key-value filter on event payload

    3. Add `event_type_filter: str | None = None` and `event_filter_config: dict | None = None` to:
       - `ActivityTemplateCreate` schema
       - `ActivityTemplateUpdate` schema
       - `ActivityTemplateResponse` schema

    4. Create Alembic migration `alembic/versions/phase19_001_event_activities.py`:
       - Add 'event' to activitytype enum: `op.execute("ALTER TYPE activitytype ADD VALUE IF NOT EXISTS 'event'")`
       - Add `event_type_filter` column (String(255), nullable) to activity_templates
       - Add `event_filter_config` column (JSON, nullable) to activity_templates
       - Follow the pattern from `phase18_001_sub_workflows.py`

    5. Add MISSING_EVENT_TYPE validation in `src/app/services/template_service.py`:
       - After the MISSING_SUB_TEMPLATE block (~line 805), add a new block:
       - For every activity with `activity_type == ActivityType.EVENT`, check `event_type_filter` is set
       - Error code: "MISSING_EVENT_TYPE", message: "Event activity '{name}' requires an event_type_filter"

    6. Write test scaffolds in `tests/test_event_activities.py` for the behaviors listed above, plus placeholders for Task 2 tests.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -m pytest tests/test_event_activities.py::test_validate_event_missing_filter -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "EVENT" src/app/models/enums.py
    - grep -q "event_type_filter" src/app/models/workflow.py
    - grep -q "event_type_filter" src/app/schemas/template.py
    - grep -q "event_filter_config" src/app/schemas/template.py
    - grep -q "MISSING_EVENT_TYPE" src/app/services/template_service.py
    - test -f alembic/versions/phase19_001_event_activities.py
    - test -f tests/test_event_activities.py
  </acceptance_criteria>
  <done>EVENT enum value exists, ActivityTemplate has event_type_filter + event_filter_config columns, schemas expose both fields, migration file created, MISSING_EVENT_TYPE validation rejects EVENT activities without event_type_filter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine dispatch and event bus handlers for EVENT activities</name>
  <files>
    src/app/services/engine_service.py,
    src/app/services/event_handlers.py,
    tests/test_event_activities.py
  </files>
  <read_first>
    src/app/services/engine_service.py,
    src/app/services/event_handlers.py,
    src/app/services/event_bus.py,
    tests/test_event_activities.py
  </read_first>
  <behavior>
    - Test: ENGINE -- when workflow reaches EVENT activity, activity state becomes ACTIVE (no work items created)
    - Test: EVENT activity completes when matching document.uploaded event fires
    - Test: EVENT activity completes when matching lifecycle.changed event fires
    - Test: EVENT activity completes when matching workflow.completed event fires (coexists with sub-workflow handler)
    - Test: EVENT activity ignores non-matching event type
    - Test: EVENT activity with event_filter_config only completes when payload matches filter
    - Test: EVENT activity does not block parallel branches (parallel branch can complete independently)
  </behavior>
  <action>
    1. In `src/app/services/engine_service.py`, add the EVENT case in `_advance_from_activity` after the SUB_WORKFLOW block:
       ```python
       elif target_at.activity_type == ActivityType.EVENT:
           # Leave as ACTIVE -- event bus handler will complete when matching event fires.
           # No work items created, no Celery task dispatched.
           pass
       ```
       Import ActivityType.EVENT if not already covered by the existing import.

    2. In `src/app/services/event_handlers.py`, add these at the bottom of the file:

       a. A shared helper `_try_complete_event_activities(db, event, event_type)`:
          - Query all ActivityInstances with state=ACTIVE where the joined ActivityTemplate has activity_type=EVENT and event_type_filter matches the event_type parameter, and the parent WorkflowInstance has state=RUNNING
          - For each match, check event_filter_config against event.payload using `_matches_filter()`
          - For matches, load the workflow, template (with selectinload for activity_templates, flow_templates, process_variables), build template_to_instance mapping, load instance variables, and call `_advance_from_activity`
          - Flush after all matches processed
          - Log each completion

       b. A simple `_matches_filter(filter_config: dict, payload: dict) -> bool`:
          - For each key/value in filter_config, check that `str(payload.get(key))` equals `str(expected)`
          - Return False if any key doesn't match

       c. Three event handlers using @event_bus.on():
          - `@event_bus.on("document.uploaded")` -> `_complete_event_activities_on_document_uploaded` calling `_try_complete_event_activities(db, event, "document.uploaded")`
          - `@event_bus.on("lifecycle.changed")` -> `_complete_event_activities_on_lifecycle_changed` calling `_try_complete_event_activities(db, event, "lifecycle.changed")`
          - `@event_bus.on("workflow.completed")` -> `_complete_event_activities_on_workflow_completed` calling `_try_complete_event_activities(db, event, "workflow.completed")`

       NOTE: The workflow.completed handler coexists with _resume_parent_on_child_complete -- both register for the same event and run independently. The sub-workflow handler checks parent_workflow_id and returns early if not a sub-workflow. The EVENT handler checks for active EVENT activities. No conflict.

       Add required imports: ActivityType from enums, ActivityTemplate join for the query.

    3. Write/update integration tests in `tests/test_event_activities.py` covering all behaviors listed above. Use the existing test patterns (async db session fixture, create template + install + start workflow, then emit events and verify activity state changes).
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -m pytest tests/test_event_activities.py -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ActivityType.EVENT" src/app/services/engine_service.py
    - grep -q "_try_complete_event_activities" src/app/services/event_handlers.py
    - grep -q "_matches_filter" src/app/services/event_handlers.py
    - grep -q "document.uploaded" src/app/services/event_handlers.py
    - grep -q "lifecycle.changed" src/app/services/event_handlers.py
    - grep -c "event_bus.on" src/app/services/event_handlers.py returns at least 6 (3 existing + 3 new)
  </acceptance_criteria>
  <done>Engine leaves EVENT activities as ACTIVE on activation, three event bus handlers complete matching EVENT activities automatically, filter matching works, all tests pass</done>
</task>

</tasks>

<verification>
- All tests pass: `pytest tests/test_event_activities.py -x`
- Full test suite unbroken: `pytest tests/ -x`
- EVENT enum in ActivityType
- event_type_filter and event_filter_config in ActivityTemplate model, all 3 schemas, and migration
- MISSING_EVENT_TYPE validation rule rejects EVENT without filter
- 3 event handlers registered for document.uploaded, lifecycle.changed, workflow.completed
</verification>

<success_criteria>
- EVENT activities can be created via API with event_type_filter and event_filter_config
- Template validation rejects EVENT without event_type_filter
- Engine activates EVENT activities without creating work items
- Matching domain events automatically complete EVENT activities and advance the workflow
- Non-matching events are ignored
- Filter config narrows matching to specific payload attributes
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/19-event-driven-activities/19-01-SUMMARY.md`
</output>
