---
phase: 33-saved-searches-smart-folders
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - frontend/src/api/savedSearches.ts
  - frontend/src/components/search/SaveSearchDialog.tsx
  - frontend/src/components/search/SavedSearchesList.tsx
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/components/folders/FolderTree.tsx
  - frontend/src/pages/BrowsePage.tsx
autonomous: true
requirements:
  - SRCH-04
  - SRCH-05

must_haves:
  truths:
    - "User can click Save Search on SearchPage, enter a name, and persist the current query + filters"
    - "User can see previously saved searches on the SearchPage and click to reload them"
    - "User can delete a saved search from the list"
    - "Smart folder nodes appear below real folders in the BrowsePage tree with a distinct icon"
    - "Clicking a smart folder node shows search results in the content grid"
  artifacts:
    - path: "frontend/src/api/savedSearches.ts"
      provides: "API client for saved searches CRUD"
      exports: ["fetchSavedSearches", "createSavedSearch", "updateSavedSearch", "deleteSavedSearch", "fetchSmartFolders"]
    - path: "frontend/src/components/search/SaveSearchDialog.tsx"
      provides: "Dialog to name and save current search"
      contains: "SaveSearchDialog"
    - path: "frontend/src/components/search/SavedSearchesList.tsx"
      provides: "List of saved searches with load/delete"
      contains: "SavedSearchesList"
    - path: "frontend/src/pages/SearchPage.tsx"
      provides: "Updated search page with save button and saved searches section"
      contains: "SaveSearchDialog"
    - path: "frontend/src/components/folders/FolderTree.tsx"
      provides: "Smart folder nodes rendered below real folders"
      contains: "smart"
    - path: "frontend/src/pages/BrowsePage.tsx"
      provides: "Smart folder click handler showing search results in content grid"
      contains: "smartFolder"
  key_links:
    - from: "frontend/src/pages/SearchPage.tsx"
      to: "frontend/src/api/savedSearches.ts"
      via: "createSavedSearch mutation"
      pattern: "createSavedSearch"
    - from: "frontend/src/pages/BrowsePage.tsx"
      to: "frontend/src/api/savedSearches.ts"
      via: "fetchSmartFolders query"
      pattern: "fetchSmartFolders"
    - from: "frontend/src/pages/BrowsePage.tsx"
      to: "frontend/src/api/search.ts"
      via: "searchDocuments for smart folder results"
      pattern: "searchDocuments"
---

<objective>
Add frontend UI for saving searches and displaying smart folders in the browse tree.

Purpose: Completes the user-facing experience so saved searches persist across sessions and smart folders appear as virtual folder nodes.
Output: Save Search dialog on SearchPage, saved searches list, smart folder nodes in BrowsePage tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-saved-searches-smart-folders/33-CONTEXT.md
@.planning/phases/33-saved-searches-smart-folders/33-01-SUMMARY.md

@frontend/src/pages/SearchPage.tsx
@frontend/src/pages/BrowsePage.tsx
@frontend/src/components/folders/FolderTree.tsx
@frontend/src/api/search.ts
@frontend/src/api/folders.ts
@frontend/src/components/search/SearchFilters.tsx

<interfaces>
<!-- From Plan 01 output — SavedSearch API contract -->

API endpoints (per D-04):
- GET /api/v1/saved-searches — list user's saved searches
- GET /api/v1/saved-searches?smart_folders_only=true — list smart folders only
- POST /api/v1/saved-searches — create saved search
- PUT /api/v1/saved-searches/:id — update saved search
- DELETE /api/v1/saved-searches/:id — delete saved search

SavedSearch response shape:
```typescript
interface SavedSearch {
  id: string;
  name: string;
  query: string;
  filters: {
    folder_id?: string;
    document_type_id?: string;
    lifecycle_state?: string;
  };
  is_smart_folder: boolean;
  display_order: number;
  created_at: string;
  updated_at: string;
}
```

From frontend/src/api/folders.ts:
```typescript
export interface FolderTreeNode {
  id: string;
  name: string;
  is_cabinet: boolean;
  document_count: number;
  children: FolderTreeNode[];
}
```

From frontend/src/components/search/SearchFilters.tsx:
```typescript
export interface SearchFilterValues {
  folder_id?: string;
  document_type_id?: string;
  lifecycle_state?: string;
}
```

From frontend/src/api/search.ts:
```typescript
export async function searchDocuments(params: SearchParams): Promise<SearchResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client and SearchPage save/load UI</name>
  <files>
    frontend/src/api/savedSearches.ts,
    frontend/src/components/search/SaveSearchDialog.tsx,
    frontend/src/components/search/SavedSearchesList.tsx,
    frontend/src/pages/SearchPage.tsx
  </files>
  <read_first>
    frontend/src/api/search.ts,
    frontend/src/api/folders.ts,
    frontend/src/pages/SearchPage.tsx,
    frontend/src/components/search/SearchFilters.tsx
  </read_first>
  <action>
1. Create `frontend/src/api/savedSearches.ts` following the search.ts pattern (authHeaders, handle401):
   - Export interface SavedSearch { id, name, query, filters: SearchFilterValues, is_smart_folder, display_order, created_at, updated_at }
   - Export interface SavedSearchCreate { name, query, filters, is_smart_folder?, display_order? }
   - fetchSavedSearches(): GET /api/v1/saved-searches -> returns data array from envelope
   - fetchSmartFolders(): GET /api/v1/saved-searches?smart_folders_only=true -> returns data array
   - createSavedSearch(data: SavedSearchCreate): POST /api/v1/saved-searches -> returns single SavedSearch
   - updateSavedSearch(id, data): PUT /api/v1/saved-searches/:id -> returns single SavedSearch
   - deleteSavedSearch(id): DELETE /api/v1/saved-searches/:id -> void
   - Export savedSearchKeys query key factory: { all: ["saved-searches"], list: () => [...all, "list"], smartFolders: () => [...all, "smart-folders"] }

2. Create `frontend/src/components/search/SaveSearchDialog.tsx` per D-07:
   - Props: open (boolean), onOpenChange, query (string), filters (SearchFilterValues), onSaved (callback)
   - Renders a Dialog (from shadcn/ui) with: name input (required), is_smart_folder checkbox labeled "Show as Smart Folder in Browse tree", save button
   - On save: calls createSavedSearch with { name, query, filters, is_smart_folder }, invalidates savedSearchKeys.all via queryClient, calls onSaved, closes dialog
   - Uses useMutation from @tanstack/react-query

3. Create `frontend/src/components/search/SavedSearchesList.tsx` per D-08:
   - Props: onLoadSearch(query: string, filters: SearchFilterValues) callback
   - Fetches saved searches with useQuery(savedSearchKeys.list(), fetchSavedSearches)
   - Renders a card/section titled "Saved Searches" with each saved search as a row showing: name, query preview (truncated), smart folder badge if is_smart_folder, load button, delete button (with deleteSavedSearch mutation + invalidation)
   - If no saved searches, show "No saved searches yet" message
   - On load click: calls onLoadSearch(search.query, search.filters)

4. Update `frontend/src/pages/SearchPage.tsx` per D-07, D-08:
   - Add state: saveDialogOpen (boolean)
   - Import SaveSearchDialog and SavedSearchesList
   - Add "Save Search" button (BookmarkPlus icon from lucide-react) next to the search input, disabled when query is empty. onClick opens the save dialog
   - Render SaveSearchDialog with current query and filters
   - Render SavedSearchesList below the search filters in the aside, or below the search input area
   - onLoadSearch handler: sets query state and filters state to the saved values
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchSavedSearches" frontend/src/api/savedSearches.ts
    - grep -q "fetchSmartFolders" frontend/src/api/savedSearches.ts
    - grep -q "createSavedSearch" frontend/src/api/savedSearches.ts
    - grep -q "SaveSearchDialog" frontend/src/components/search/SaveSearchDialog.tsx
    - grep -q "SavedSearchesList" frontend/src/components/search/SavedSearchesList.tsx
    - grep -q "SaveSearchDialog" frontend/src/pages/SearchPage.tsx
    - grep -q "SavedSearchesList" frontend/src/pages/SearchPage.tsx
  </acceptance_criteria>
  <done>SearchPage has Save Search button that opens dialog, saved searches list shows below filters with load/delete actions, API client handles all CRUD</done>
</task>

<task type="auto">
  <name>Task 2: Smart folder nodes in BrowsePage tree</name>
  <files>
    frontend/src/components/folders/FolderTree.tsx,
    frontend/src/pages/BrowsePage.tsx
  </files>
  <read_first>
    frontend/src/components/folders/FolderTree.tsx,
    frontend/src/pages/BrowsePage.tsx,
    frontend/src/api/savedSearches.ts,
    frontend/src/api/search.ts
  </read_first>
  <action>
1. Update `frontend/src/components/folders/FolderTree.tsx` per D-09:
   - Add new prop: smartFolders?: Array of { id: string; name: string } (simplified smart folder node)
   - Add new prop: selectedSmartFolderId?: string | null
   - Add new prop: onSmartFolderSelect?: (id: string) => void
   - After rendering the real folder tree nodes, if smartFolders array is non-empty, render a separator (thin hr or label "Smart Folders") then render each smart folder as a tree item with:
     - Search icon (import Search from lucide-react) in place of folder icon, colored purple/violet to distinguish
     - Name text
     - No chevron (smart folders have no children)
     - Highlight if selectedSmartFolderId matches
     - onClick calls onSmartFolderSelect

2. Update `frontend/src/pages/BrowsePage.tsx` per D-06, D-10:
   - Import fetchSmartFolders, savedSearchKeys from savedSearches.ts API client
   - Import searchDocuments from search.ts API client
   - Add state: selectedSmartFolderId (string | null, default null), smartFolderResults (search result array)
   - Add useQuery for smart folders: queryKey savedSearchKeys.smartFolders(), queryFn fetchSmartFolders
   - Pass smartFolders prop to FolderTree component (map to { id, name } from the smart folder response)
   - Pass selectedSmartFolderId and onSmartFolderSelect to FolderTree
   - onSmartFolderSelect handler: set selectedSmartFolderId, clear selectedFolderId (mutual exclusion — either a real folder or smart folder is selected, not both), clear selectedDocumentId
   - When selectedSmartFolderId is set, find the corresponding smart folder object, use its query + filters to call searchDocuments via a useQuery. Display results in the content grid using the same Table layout but with the search result fields (title, content_type, document_type_name, lifecycle_state). Show a header like "Smart Folder: {name}" instead of the breadcrumb
   - When a real folder is selected via handleFolderSelect, clear selectedSmartFolderId
   - Modify the "no folder selected" empty state to also check for smart folder selection — if neither folder nor smart folder is selected, show the empty state
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "smartFolder" frontend/src/components/folders/FolderTree.tsx
    - grep -q "onSmartFolderSelect" frontend/src/components/folders/FolderTree.tsx
    - grep -q "Search" frontend/src/components/folders/FolderTree.tsx
    - grep -q "smartFolder" frontend/src/pages/BrowsePage.tsx
    - grep -q "searchDocuments" frontend/src/pages/BrowsePage.tsx
    - grep -q "fetchSmartFolders" frontend/src/pages/BrowsePage.tsx
  </acceptance_criteria>
  <done>Smart folder nodes appear in BrowsePage folder tree below real folders with a Search icon. Clicking a smart folder executes its saved query and shows results in the content grid</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: cd frontend && npx tsc --noEmit
- SaveSearchDialog renders in SearchPage
- SavedSearchesList renders in SearchPage
- FolderTree accepts and renders smart folder nodes
- BrowsePage fetches smart folders and handles selection
</verification>

<success_criteria>
- Save Search button on SearchPage opens dialog to name and optionally mark as smart folder
- Saved searches list on SearchPage shows all user's saved searches with load/delete
- Smart folder nodes render in BrowsePage tree below real folders with Search icon
- Clicking a smart folder shows search results in the content grid per D-06/D-10
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/33-saved-searches-smart-folders/33-02-SUMMARY.md`
</output>
