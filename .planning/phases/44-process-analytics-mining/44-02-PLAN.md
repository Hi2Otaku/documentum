---
phase: 44-process-analytics-mining
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - frontend/src/api/analytics.ts
  - frontend/src/pages/AnalyticsPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/SidebarNav.tsx
autonomous: true
requirements: [ANLYT-01, ANLYT-02, ANLYT-03]

must_haves:
  truths:
    - "Admin can navigate to /admin/analytics from the sidebar"
    - "Dashboard shows execution path frequency table/visualization"
    - "Dashboard shows cycle time bar charts per activity and per template"
    - "Dashboard shows bottleneck activities ranked by duration"
    - "Admin can filter analytics by template and trigger manual refresh"
  artifacts:
    - path: "frontend/src/api/analytics.ts"
      provides: "API client for analytics endpoints"
      exports: ["fetchPaths", "fetchCycleTimes", "fetchBottlenecks", "fetchSummary", "triggerRefresh", "analyticsKeys"]
    - path: "frontend/src/pages/AnalyticsPage.tsx"
      provides: "Process analytics dashboard page"
      exports: ["AnalyticsPage"]
    - path: "frontend/src/App.tsx"
      provides: "Route registration"
      contains: "/admin/analytics"
    - path: "frontend/src/components/layout/SidebarNav.tsx"
      provides: "Nav link"
      contains: "Analytics"
  key_links:
    - from: "frontend/src/pages/AnalyticsPage.tsx"
      to: "frontend/src/api/analytics.ts"
      via: "useQuery hooks"
      pattern: "useQuery.*analyticsKeys"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/AnalyticsPage.tsx"
      via: "Route element"
      pattern: "AnalyticsPage"
    - from: "frontend/src/components/layout/SidebarNav.tsx"
      to: "/admin/analytics"
      via: "nav item"
      pattern: "analytics"
---

<objective>
Build the process analytics frontend: API client, Recharts-based dashboard page with execution paths, cycle time charts, bottleneck table, template filter, and manual refresh. Wire into routing and sidebar navigation.

Purpose: Delivers the admin-facing process mining dashboard (ANLYT-01, ANLYT-02, ANLYT-03).
Output: Analytics API client, AnalyticsPage with Recharts visualizations, route + nav integration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-process-analytics-mining/44-CONTEXT.md
@.planning/phases/44-process-analytics-mining/44-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/app/schemas/analytics.py (created by Plan 01):
```python
class ExecutionPath(BaseModel):
    path: list[str]         # ordered activity names
    frequency: int
    avg_total_duration_seconds: float | None

class CycleTimeStats(BaseModel):
    activity_name: str | None
    template_name: str | None
    template_id: str | None
    avg_seconds: float
    median_seconds: float
    min_seconds: float
    max_seconds: float
    sample_count: int

class BottleneckActivity(BaseModel):
    activity_name: str
    template_name: str
    avg_duration_seconds: float
    total_executions: int
    pct_of_total_time: float

class AnalyticsSummary(BaseModel):
    total_instances: int
    completed_instances: int
    avg_completion_seconds: float | None
    paths_discovered: int
    last_refreshed: str | None
```

API endpoints (created by Plan 01):
- GET /api/v1/analytics/summary
- GET /api/v1/analytics/paths?template_id=&limit=20
- GET /api/v1/analytics/cycle-times?template_id=&group_by=activity|template
- GET /api/v1/analytics/bottlenecks?template_id=&limit=10
- POST /api/v1/analytics/refresh

All return EnvelopeResponse: `{ data: T, meta: null, errors: [] }`

From frontend/src/api/monitoring.ts (pattern reference):
- apiFetch/apiPost helper pattern with authHeaders, handle401
- Query key factory pattern: `analyticsKeys`

From frontend/src/components/layout/SidebarNav.tsx:
- Nav item format: `{ icon: IconComponent, label: "Label", route: "/path", adminOnly: true }`
- Last item currently: `{ icon: Activity, label: "System Health", route: "/admin/monitoring", adminOnly: true }`

From frontend/src/App.tsx:
- Admin route pattern: `<Route path="/admin/analytics" element={<AnalyticsPage />} />`

Recharts usage (from existing dashboard components):
- BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer from 'recharts'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics API client and dashboard page</name>
  <read_first>
    - frontend/src/api/monitoring.ts (API client pattern: apiFetch, authHeaders, handle401, query keys)
    - frontend/src/pages/MonitoringPage.tsx (admin dashboard page pattern with useQuery)
    - frontend/src/components/dashboard/BottleneckChart.tsx (Recharts BarChart usage pattern)
    - frontend/src/components/dashboard/WorkloadChart.tsx (Recharts pattern)
  </read_first>
  <files>
    frontend/src/api/analytics.ts,
    frontend/src/pages/AnalyticsPage.tsx
  </files>
  <action>
    **frontend/src/api/analytics.ts** -- API client following monitoring.ts pattern:
    - TypeScript interfaces mirroring Python schemas: `ExecutionPath`, `CycleTimeStats`, `BottleneckActivity`, `AnalyticsSummary`
    - Query key factory: `analyticsKeys = { summary, paths, cycleTimes, bottlenecks }` (each as readonly tuple)
    - `fetchSummary(): Promise<AnalyticsSummary>` -- GET /api/v1/analytics/summary, unwrap .data
    - `fetchPaths(templateId?: string, limit?: number): Promise<ExecutionPath[]>` -- GET with query params, unwrap .data
    - `fetchCycleTimes(templateId?: string, groupBy?: "activity" | "template"): Promise<CycleTimeStats[]>` -- GET with query params, unwrap .data
    - `fetchBottlenecks(templateId?: string, limit?: number): Promise<BottleneckActivity[]>` -- GET with query params, unwrap .data
    - `triggerRefresh(): Promise<void>` -- POST /api/v1/analytics/refresh
    - Use same apiFetch/apiPost helper pattern as monitoring.ts (inline helpers, authHeaders from localStorage, handle401)

    **frontend/src/pages/AnalyticsPage.tsx** -- Process analytics dashboard:
    - Named export `AnalyticsPage`
    - Use `useQuery` from `@tanstack/react-query` for all four data fetches (summary, paths, cycleTimes, bottlenecks)
    - State: `selectedTemplateId` (string | undefined) for filter dropdown
    - Use `useQuery` to fetch template list from existing `/api/v1/templates` endpoint for the filter dropdown (reuse templates API or inline fetch)
    - Layout sections (use Tailwind, shadcn Card if available or plain divs with card styling matching MonitoringPage):

    **Section 1 - Summary cards row** (top):
    - 4 stat cards: Total Instances, Completed, Avg Completion Time (formatted as Xh Ym), Paths Discovered
    - Last refreshed timestamp. "Refresh Now" button calling triggerRefresh() then invalidating all queries.

    **Section 2 - Template filter**:
    - Select dropdown with "All Templates" default + list of installed templates. Changing selection refetches all analytics queries with template_id param.

    **Section 3 - Execution Paths** (ANLYT-01):
    - Table showing discovered paths: columns = Path (activity names joined with arrow unicode char), Frequency, Avg Duration.
    - Sort by frequency descending. Show top 20.
    - Use a simple HTML table with Tailwind styling (not a full @tanstack/react-table).

    **Section 4 - Cycle Time Analysis** (ANLYT-02):
    - Two tabs or toggle: "By Activity" / "By Template"
    - Recharts horizontal BarChart: Y-axis = activity/template name, X-axis = seconds (show as formatted time).
    - Use `<ResponsiveContainer width="100%" height={Math.max(300, items.length * 40)}>` for dynamic height.
    - Bar color: blue for activity view, green for template view.
    - Tooltip showing avg, min, max, median, sample count.

    **Section 5 - Bottleneck Analysis** (ANLYT-03):
    - Recharts BarChart: horizontal bars showing avg_duration_seconds per activity, colored by pct_of_total_time (gradient from green to red).
    - Below chart: table with columns Activity, Template, Avg Duration, Executions, % of Total Time. Sorted by avg duration desc.

    - Loading states: show skeleton/spinner per section using existing patterns.
    - Error states: show error message per section.
    - Auto-refresh: `refetchInterval: 60_000` on summary query only (lightweight).
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchPaths" frontend/src/api/analytics.ts
    - grep -q "fetchCycleTimes" frontend/src/api/analytics.ts
    - grep -q "fetchBottlenecks" frontend/src/api/analytics.ts
    - grep -q "fetchSummary" frontend/src/api/analytics.ts
    - grep -q "triggerRefresh" frontend/src/api/analytics.ts
    - grep -q "analyticsKeys" frontend/src/api/analytics.ts
    - grep -q "AnalyticsPage" frontend/src/pages/AnalyticsPage.tsx
    - grep -q "BarChart" frontend/src/pages/AnalyticsPage.tsx
    - grep -q "useQuery" frontend/src/pages/AnalyticsPage.tsx
    - grep -q "Bottleneck" frontend/src/pages/AnalyticsPage.tsx
    - grep -q "Cycle Time" frontend/src/pages/AnalyticsPage.tsx
    - grep -q "Execution Path" frontend/src/pages/AnalyticsPage.tsx
  </acceptance_criteria>
  <done>API client exports all fetch functions with correct types. AnalyticsPage renders summary cards, execution path table, cycle time bar charts (activity + template toggle), and bottleneck visualization. Template filter controls all queries. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Route registration and sidebar navigation</name>
  <read_first>
    - frontend/src/App.tsx (route registration pattern)
    - frontend/src/components/layout/SidebarNav.tsx (nav item array)
  </read_first>
  <files>
    frontend/src/App.tsx,
    frontend/src/components/layout/SidebarNav.tsx
  </files>
  <action>
    **frontend/src/App.tsx**:
    - Add import: `import { AnalyticsPage } from "./pages/AnalyticsPage";`
    - Add route inside the `<AdminRoute>` block, after the monitoring route:
      `<Route path="/admin/analytics" element={<AnalyticsPage />} />`

    **frontend/src/components/layout/SidebarNav.tsx**:
    - Add `BarChart3` (or `TrendingUp` or `LineChart`) icon import from `lucide-react` (pick one that is available -- check existing imports first).
    - Add nav item after the "System Health" entry:
      `{ icon: BarChart3, label: "Process Analytics", route: "/admin/analytics", adminOnly: true }`
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AnalyticsPage" frontend/src/App.tsx
    - grep -q "/admin/analytics" frontend/src/App.tsx
    - grep -q "Process Analytics" frontend/src/components/layout/SidebarNav.tsx
    - grep -q "/admin/analytics" frontend/src/components/layout/SidebarNav.tsx
  </acceptance_criteria>
  <done>AnalyticsPage accessible at /admin/analytics. Sidebar shows "Process Analytics" link in admin section. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- Route exists: `grep "/admin/analytics" frontend/src/App.tsx`
- Nav link exists: `grep "Process Analytics" frontend/src/components/layout/SidebarNav.tsx`
- API client complete: `grep "export async function" frontend/src/api/analytics.ts` shows 5 functions
</verification>

<success_criteria>
Admin can navigate to /admin/analytics from sidebar. Dashboard renders summary stats, execution path table, cycle time bar charts with activity/template toggle, and bottleneck analysis visualization. Template filter dropdown controls all queries. Refresh button triggers immediate analytics cache refresh. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/44-process-analytics-mining/44-02-SUMMARY.md`
</output>
