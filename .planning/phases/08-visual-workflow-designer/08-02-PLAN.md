---
phase: 08-visual-workflow-designer
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/main.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/ProtectedRoute.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/TemplateListPage.tsx
autonomous: true
requirements:
  - DESIGN-07
must_haves:
  truths:
    - "User can log in with username/password and see the template list"
    - "Unauthenticated users are redirected to /login"
    - "Template list shows name, state badge, version, and last modified"
    - "User can click 'New Template' to create a template and navigate to editor"
    - "User can click a template card to navigate to /templates/:id/edit"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Login form calling /api/v1/auth/login"
      min_lines: 30
    - path: "frontend/src/pages/TemplateListPage.tsx"
      provides: "Template list with cards"
      min_lines: 40
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Nav bar with app name, Templates link, logout"
      min_lines: 20
    - path: "frontend/src/components/layout/ProtectedRoute.tsx"
      provides: "Auth guard redirecting to /login"
      min_lines: 10
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "frontend/src/api/auth.ts"
      via: "loginApi function call"
      pattern: "loginApi"
    - from: "frontend/src/pages/TemplateListPage.tsx"
      to: "frontend/src/api/templates.ts"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*templates"
    - from: "frontend/src/App.tsx"
      to: "react-router"
      via: "Route definitions"
      pattern: "Route.*path"
---

<objective>
Build the app shell (nav bar, routing, auth guard) and the two non-designer pages: login page and template list page. After this plan, users can authenticate, see their templates, create new templates, and navigate to the designer (which Plan 04 builds).

Purpose: Deliver a working authenticated SPA that integrates with the existing backend auth and template list APIs.
Output: Login page, template list page, app shell with nav, protected routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-visual-workflow-designer/08-CONTEXT.md
@.planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
@.planning/phases/08-visual-workflow-designer/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From frontend/src/api/auth.ts:
```typescript
export async function loginApi(username: string, password: string): Promise<string>;
```

From frontend/src/api/templates.ts:
```typescript
export async function listTemplates(page?: number, pageSize?: number): Promise<ProcessTemplateResponse[]>;
export async function createTemplate(data: ProcessTemplateCreate): Promise<ProcessTemplateResponse>;
export async function deleteTemplate(id: string): Promise<void>;
```

From frontend/src/stores/authStore.ts:
```typescript
export const useAuthStore: UseBoundStore<StoreApi<{
  token: string | null;
  isAuthenticated: boolean;
  setToken: (token: string) => void;
  logout: () => void;
}>>;
```

From frontend/src/stores/uiStore.ts:
```typescript
export const useUiStore: UseBoundStore<StoreApi<{
  leftSidebarCollapsed: boolean;
  rightPanelOpen: boolean;
  errorPanelExpanded: boolean;
  toggleLeftSidebar: () => void;
  setRightPanelOpen: (open: boolean) => void;
  setErrorPanelExpanded: (expanded: boolean) => void;
}>>;
```

From frontend/src/types/api.ts:
```typescript
export interface ProcessTemplateResponse {
  id: string;
  name: string;
  description: string | null;
  version: number;
  state: 'draft' | 'active';
  is_installed: boolean;
  installed_at: string | null;
  created_at: string;
  updated_at: string;
  created_by: string | null;
  is_deleted: boolean;
}

export interface ProcessTemplateCreate {
  name: string;
  description?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell, routing, protected routes, and login page</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/main.tsx,
    frontend/src/components/layout/AppShell.tsx,
    frontend/src/components/layout/ProtectedRoute.tsx,
    frontend/src/pages/LoginPage.tsx
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/main.tsx
    frontend/src/stores/authStore.ts
    frontend/src/api/auth.ts
    frontend/src/types/api.ts
    .planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
  </read_first>
  <action>
    1. Update `frontend/src/main.tsx`:
       - Import `@fontsource/inter` at top
       - Import `./index.css`
       - Wrap App in `<QueryClientProvider>` (from @tanstack/react-query) and `<BrowserRouter>` (from react-router)
       - Add `<Toaster />` from sonner at the root level
       - Use React 19 `createRoot(document.getElementById('root')!).render(...)`

    2. Update `frontend/src/App.tsx` (per D-19):
       - Define 3 routes using react-router `<Routes>` + `<Route>`:
         - `/login` -> `<LoginPage />`
         - `/` -> `<Navigate to="/templates" replace />`
         - Protected routes (wrapped in `<ProtectedRoute>`):
           - `/templates` -> `<AppShell><TemplateListPage /></AppShell>`
           - `/templates/:id/edit` -> `<AppShell><div>Designer placeholder</div></AppShell>` (Plan 04 replaces)

    3. Create `frontend/src/components/layout/ProtectedRoute.tsx` (per D-20):
       - Reads `isAuthenticated` from `useAuthStore`
       - If not authenticated, return `<Navigate to="/login" replace />`
       - If authenticated, render `<Outlet />`

    4. Create `frontend/src/components/layout/AppShell.tsx` (per D-18):
       - Top nav bar (48px height, border-b, bg-background):
         - Left: App name "Workflow Designer" in `text-lg font-semibold`
         - Center: "Templates" nav link using react-router `<Link to="/templates">`
         - Right: "Logout" button (variant="ghost") that calls `useAuthStore.getState().logout()` and navigates to `/login`
       - Below nav: `<Outlet />` for page content or `{children}` pattern

    5. Create `frontend/src/pages/LoginPage.tsx` (per D-20, UI-SPEC Section 6):
       - Centered card (max-w-[400px], mx-auto, mt-24)
       - App name/heading "Workflow Designer" at top of card
       - Form with: username `<Input>`, password `<Input type="password">`, "Sign In" `<Button>` (primary variant)
       - On submit: call `loginApi(username, password)` from `frontend/src/api/auth.ts`
       - On success: call `useAuthStore.getState().setToken(token)`, navigate to `/templates` using react-router `useNavigate()`
       - On error: show error message "Invalid username or password." below form in red text
       - Loading state: button shows spinner text "Signing in..." and is disabled during API call
       - No "register" link — admin creates accounts (per UI-SPEC)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/App.tsx contains "Route" and "path"
    - frontend/src/App.tsx contains "/login"
    - frontend/src/App.tsx contains "/templates"
    - frontend/src/App.tsx contains "/templates/:id/edit"
    - frontend/src/components/layout/ProtectedRoute.tsx contains "isAuthenticated"
    - frontend/src/components/layout/ProtectedRoute.tsx contains "Navigate"
    - frontend/src/components/layout/ProtectedRoute.tsx contains "/login"
    - frontend/src/components/layout/AppShell.tsx contains "Workflow Designer"
    - frontend/src/components/layout/AppShell.tsx contains "Logout"
    - frontend/src/components/layout/AppShell.tsx contains "Templates"
    - frontend/src/pages/LoginPage.tsx contains "loginApi"
    - frontend/src/pages/LoginPage.tsx contains "setToken"
    - frontend/src/pages/LoginPage.tsx contains "Sign In"
    - frontend/src/pages/LoginPage.tsx contains "Invalid username or password"
    - frontend/src/main.tsx contains "QueryClientProvider"
    - frontend/src/main.tsx contains "BrowserRouter"
    - frontend/src/main.tsx contains "Toaster"
    - `cd frontend && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    Login page authenticates against existing backend API using form-data. Protected routes redirect to login when unauthenticated. App shell has nav bar with Templates link and Logout button. Three routes defined: /login, /templates, /templates/:id/edit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Template list page with cards, create, and delete</name>
  <files>
    frontend/src/pages/TemplateListPage.tsx
  </files>
  <read_first>
    frontend/src/api/templates.ts
    frontend/src/types/api.ts
    .planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
  </read_first>
  <action>
    Create `frontend/src/pages/TemplateListPage.tsx` (per D-15, UI-SPEC Section 6):

    1. Page header:
       - Title "Workflow Templates" in `text-xl font-semibold`
       - "New Template" primary `<Button>` in header row
       - On "New Template" click: call `createTemplate({ name: 'Untitled Template' })` via TanStack Query `useMutation`, then navigate to `/templates/{newId}/edit`

    2. Template list:
       - Use TanStack Query `useQuery({ queryKey: ['templates'], queryFn: () => listTemplates() })`
       - Loading state: 3 `<Skeleton>` cards (80px height each, animated shimmer) as described in UI-SPEC Section 8
       - Empty state: heading "No workflow templates", body "Create your first workflow template to get started.", "New Template" button
       - Grid of `<Card>` components, each showing:
         - Template name in `font-semibold`
         - State `<Badge>` — "Draft" (variant="secondary") or "Active" (variant="default" with green styling)
         - Version number: `v{version}`
         - Last modified: formatted `updated_at` date (use `new Date(t.updated_at).toLocaleDateString()`)
         - Click entire card: navigate to `/templates/{id}/edit`
       - Each card has overflow menu (three-dot icon `<DropdownMenu>`) with "Delete" option (red text)

    3. Delete flow:
       - "Delete" in dropdown triggers confirmation `<Dialog>`: "Delete template '{name}'? This cannot be undone. Running workflow instances will not be affected."
       - "Cancel" and "Delete" buttons in dialog
       - On confirm: call `deleteTemplate(id)` via `useMutation`, invalidate `['templates']` query
       - Show success toast via `sonner`: "Template deleted"

    4. Error state:
       - If query fails, show error message with retry button
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/TemplateListPage.tsx contains "useQuery"
    - frontend/src/pages/TemplateListPage.tsx contains "listTemplates"
    - frontend/src/pages/TemplateListPage.tsx contains "Workflow Templates"
    - frontend/src/pages/TemplateListPage.tsx contains "New Template"
    - frontend/src/pages/TemplateListPage.tsx contains "No workflow templates"
    - frontend/src/pages/TemplateListPage.tsx contains "useMutation"
    - frontend/src/pages/TemplateListPage.tsx contains "deleteTemplate"
    - frontend/src/pages/TemplateListPage.tsx contains "Dialog"
    - frontend/src/pages/TemplateListPage.tsx contains "Badge"
    - frontend/src/pages/TemplateListPage.tsx contains "/templates/"
    - `cd frontend && npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    Template list page displays all templates with name, state badge, version, and last modified date. New Template button creates a template and navigates to the designer. Cards are clickable to navigate to editor. Delete with confirmation dialog works. Loading skeleton and empty states implemented.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` completes without errors
- Login page exists with form-data auth flow
- Template list page fetches and displays templates
- Protected routes redirect unauthenticated users to /login
- All 3 routes defined in App.tsx
</verification>

<success_criteria>
- User can log in at /login and is redirected to /templates on success
- Unauthenticated access to /templates redirects to /login
- Template list shows cards with name, state badge, version, last modified
- "New Template" creates a template and navigates to designer
- Delete template shows confirmation dialog and removes from list
- App shell has nav bar with "Templates" link and "Logout" button
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-workflow-designer/08-02-SUMMARY.md`
</output>
