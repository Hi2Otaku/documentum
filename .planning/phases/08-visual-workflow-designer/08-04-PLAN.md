---
phase: 08-visual-workflow-designer
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - frontend/src/pages/DesignerPage.tsx
  - frontend/src/components/designer/Canvas.tsx
  - frontend/src/components/designer/Toolbar.tsx
  - frontend/src/components/designer/NodePalette.tsx
  - frontend/src/components/designer/PropertiesPanel.tsx
  - frontend/src/components/designer/ErrorPanel.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-04
  - DESIGN-05
must_haves:
  truths:
    - "User can drag nodes from the left palette onto the canvas"
    - "User can select a node and see its properties in the right panel"
    - "User can edit activity name, performer assignment, and trigger type in the properties panel"
    - "User can define process variables in the template-level Variables tab"
    - "When no node is selected, properties panel shows template info and variables tabs"
    - "Canvas renders React Flow with minimap, snap-to-grid, and background dots"
    - "Toolbar shows template name, unsaved indicator, and action buttons"
  artifacts:
    - path: "frontend/src/pages/DesignerPage.tsx"
      provides: "Three-panel layout page with ReactFlowProvider"
      min_lines: 40
    - path: "frontend/src/components/designer/Canvas.tsx"
      provides: "React Flow canvas with node/edge type registration"
      exports: ["Canvas"]
    - path: "frontend/src/components/designer/NodePalette.tsx"
      provides: "Left sidebar with draggable node items"
      exports: ["NodePalette"]
    - path: "frontend/src/components/designer/PropertiesPanel.tsx"
      provides: "Right sidebar with context-sensitive forms"
      exports: ["PropertiesPanel"]
    - path: "frontend/src/components/designer/Toolbar.tsx"
      provides: "Top toolbar with save, validate, undo/redo buttons"
      exports: ["Toolbar"]
    - path: "frontend/src/components/designer/ErrorPanel.tsx"
      provides: "Bottom collapsible validation error list"
      exports: ["ErrorPanel"]
  key_links:
    - from: "frontend/src/components/designer/Canvas.tsx"
      to: "frontend/src/stores/designerStore.ts"
      via: "useDesignerStore for nodes/edges"
      pattern: "useDesignerStore"
    - from: "frontend/src/components/designer/NodePalette.tsx"
      to: "frontend/src/components/designer/Canvas.tsx"
      via: "HTML5 DnD with application/reactflow data"
      pattern: "application/reactflow"
    - from: "frontend/src/components/designer/PropertiesPanel.tsx"
      to: "frontend/src/stores/designerStore.ts"
      via: "updateNodeData/updateEdgeData"
      pattern: "updateNodeData"
    - from: "frontend/src/pages/DesignerPage.tsx"
      to: "frontend/src/api/templates.ts"
      via: "TanStack Query for template loading"
      pattern: "getTemplateDetail"
---

<objective>
Build the main designer page with the three-panel layout: left node palette, center React Flow canvas, and right properties panel, plus the top toolbar and bottom error panel. This is the core visual workflow designer.

Purpose: Deliver the drag-and-drop canvas where users design workflow templates visually.
Output: Complete designer page with all panels, drag-and-drop, node/edge selection, property editing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-visual-workflow-designer/08-CONTEXT.md
@.planning/phases/08-visual-workflow-designer/08-RESEARCH.md
@.planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
@.planning/phases/08-visual-workflow-designer/08-01-SUMMARY.md
@.planning/phases/08-visual-workflow-designer/08-02-SUMMARY.md
@.planning/phases/08-visual-workflow-designer/08-03-SUMMARY.md

<interfaces>
From frontend/src/stores/designerStore.ts (Plan 03):
```typescript
export const useDesignerStore: {
  nodes: Node[];
  edges: Edge[];
  selectedNodeId: string | null;
  selectedEdgeId: string | null;
  isDirty: boolean;
  templateId: string | null;
  onNodesChange: (changes: NodeChange[]) => void;
  onEdgesChange: (changes: EdgeChange[]) => void;
  addNode: (node: Node) => void;
  addEdge: (edge: Edge) => void;
  deleteElements: (nodeIds: string[], edgeIds: string[]) => void;
  updateNodeData: (nodeId: string, data: Partial<ActivityNodeData>) => void;
  updateEdgeData: (edgeId: string, data: Partial<FlowEdgeData>) => void;
  setSelectedNode: (id: string | null) => void;
  setSelectedEdge: (id: string | null) => void;
  clearSelection: () => void;
  undo: () => void;
  redo: () => void;
  loadTemplate: (templateId: string, nodes: Node[], edges: Edge[]) => void;
  reset: () => void;
  setClean: () => void;
};
```

From frontend/src/stores/uiStore.ts (Plan 01):
```typescript
export const useUiStore: {
  leftSidebarCollapsed: boolean;
  rightPanelOpen: boolean;
  errorPanelExpanded: boolean;
  toggleLeftSidebar: () => void;
  setRightPanelOpen: (open: boolean) => void;
  setErrorPanelExpanded: (expanded: boolean) => void;
};
```

Node types (Plan 03): StartNode, ManualNode, AutoNode, EndNode
Edge types (Plan 03): NormalEdge, RejectEdge, ConditionalEdge

From frontend/src/lib/serialization.ts (Plan 01):
```typescript
export function activitiesToNodes(activities: ActivityTemplateResponse[]): Node[];
export function flowsToEdges(flows: FlowTemplateResponse[]): Edge[];
```

From frontend/src/api/templates.ts (Plan 01):
```typescript
export function getTemplateDetail(id: string): Promise<ProcessTemplateDetailResponse>;
export function listUsers(): // in users.ts
export function listGroups(): // in users.ts
```

From frontend/src/hooks/useAutoLayout.ts (Plan 03):
```typescript
export function getLayoutedElements(nodes: Node[], edges: Edge[], direction?: 'LR' | 'TB'): { nodes: Node[]; edges: Edge[] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Designer page layout, canvas component, and node palette</name>
  <files>
    frontend/src/pages/DesignerPage.tsx,
    frontend/src/components/designer/Canvas.tsx,
    frontend/src/components/designer/NodePalette.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/stores/designerStore.ts
    frontend/src/stores/uiStore.ts
    frontend/src/lib/serialization.ts
    frontend/src/hooks/useAutoLayout.ts
    frontend/src/components/nodes/StartNode.tsx
    frontend/src/components/nodes/ManualNode.tsx
    frontend/src/types/designer.ts
    .planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
  </read_first>
  <action>
    1. Create `frontend/src/pages/DesignerPage.tsx`:
       - Extract `id` from URL params using `useParams<{ id: string }>()`
       - Wrap entire page in `<ReactFlowProvider>` (CRITICAL: must wrap parent, not ReactFlow itself)
       - Load template on mount: use TanStack Query `useQuery({ queryKey: ['templates', id, 'detail'], queryFn: () => getTemplateDetail(id!) })`
       - On data load: convert with `activitiesToNodes(data.activities)` and `flowsToEdges(data.flows)`, call `useDesignerStore.getState().loadTemplate(id, nodes, edges)`
       - If template has no position data (all position_x are null/0): run `getLayoutedElements` and load the layouted result instead
       - Layout (per UI-SPEC Section 6 designer wireframe):
         ```
         h-screen flex flex-col
           Toolbar (48px)
           flex flex-1 overflow-hidden
             NodePalette (left, 220px or 48px collapsed)
             Canvas (flex-1)
             PropertiesPanel (right, 320px)
           ErrorPanel (bottom, collapsible)
         ```
       - Loading state: show centered spinner with "Loading template..." text
       - Error state: "Could not load template" with "Back to Templates" link
       - On unmount: call `useDesignerStore.getState().reset()`
       - Add `beforeunload` handler when `isDirty` is true (per UI-SPEC Section 5 destructive actions)

    2. Create `frontend/src/components/designer/Canvas.tsx`:
       - Define `nodeTypes` and `edgeTypes` objects OUTSIDE the component (CRITICAL to avoid re-render loops):
         ```typescript
         const nodeTypes = { startNode: StartNode, manualNode: ManualNode, autoNode: AutoNode, endNode: EndNode };
         const edgeTypes = { normalEdge: NormalEdge, rejectEdge: RejectEdge, conditionalEdge: ConditionalEdge };
         ```
       - Read `nodes, edges, onNodesChange, onEdgesChange, addEdge, setSelectedNode, setSelectedEdge, clearSelection` from `useDesignerStore`
       - Render `<ReactFlow>` with:
         - `nodes={nodes}` `edges={edges}`
         - `onNodesChange={onNodesChange}` `onEdgesChange={onEdgesChange}`
         - `nodeTypes={nodeTypes}` `edgeTypes={edgeTypes}`
         - `onConnect` handler: create new edge with `type: 'normalEdge'`, `data: { flowType: 'normal' }`, use `crypto.randomUUID()` for id
         - `onNodeClick`: call `setSelectedNode(node.id)`
         - `onEdgeClick`: call `setSelectedEdge(edge.id)`
         - `onPaneClick`: call `clearSelection()`
         - `snapToGrid={true}` `snapGrid={[20, 20]}` (per UI-SPEC Section 6 canvas)
         - `fitView` on initial render
         - `connectionLineStyle={{ strokeDasharray: '5 5' }}` (animated dashed connection line per UI-SPEC)
         - `defaultEdgeOptions={{ type: 'normalEdge' }}`
       - Canvas container: `w-full h-full` (CRITICAL: parent must have height or canvas collapses to 0)
       - Include `<MiniMap />` at bottom-right (per D-04), `<Controls />` at bottom-left, `<Background variant={BackgroundVariant.Dots} />` (per UI-SPEC)
       - Handle drop from palette (per D-01, DESIGN-02):
         - `onDragOver`: `event.preventDefault(); event.dataTransfer.dropEffect = 'move'`
         - `onDrop`: read `event.dataTransfer.getData('application/reactflow')`, if type exists:
           - Get position via `const { screenToFlowPosition } = useReactFlow(); const position = screenToFlowPosition({ x: event.clientX, y: event.clientY })`
           - Create node: `{ id: crypto.randomUUID(), type: nodeType, position, data: { name: 'New Activity', activityType: typeMap[nodeType] } }`
           - For startNode data: `{ name: 'Start', activityType: 'start' }`
           - For endNode data: `{ name: 'End', activityType: 'end' }`
           - For manualNode data: `{ name: 'New Manual Activity', activityType: 'manual' }`
           - For autoNode data: `{ name: 'New Auto Activity', activityType: 'auto' }`
           - Call `addNode(newNode)`, then `setSelectedNode(newNode.id)`

    3. Create `frontend/src/components/designer/NodePalette.tsx` (per D-01):
       - Read `leftSidebarCollapsed, toggleLeftSidebar` from `useUiStore`
       - Width: 220px expanded, 48px collapsed
       - Toggle button at top (ChevronLeft/ChevronRight icon)
       - Section header: "Activities" in `text-sm font-semibold text-muted-foreground uppercase`
       - 4 draggable items, each 44px height:
         - `Play` icon + "Start" — left accent border green-500
         - `User` icon + "Manual" — left accent border blue-500
         - `Zap` icon + "Auto" — left accent border orange-500
         - `Square` icon + "End" — left accent border red-500
       - Each item: `role="button"`, `draggable={true}`, `aria-label="Drag to add {type} activity"` (per accessibility)
       - `onDragStart`: `event.dataTransfer.setData('application/reactflow', nodeType); event.dataTransfer.effectAllowed = 'move';`
       - Collapsed mode: show only icons with `<Tooltip>` showing label on hover
       - Cursor on drag: `cursor-grab`, `active:cursor-grabbing`

    4. Update `frontend/src/App.tsx`:
       - Replace the designer placeholder `<div>Designer placeholder</div>` with `<DesignerPage />`
       - Import DesignerPage from `@/pages/DesignerPage`
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/DesignerPage.tsx contains "ReactFlowProvider"
    - frontend/src/pages/DesignerPage.tsx contains "useParams"
    - frontend/src/pages/DesignerPage.tsx contains "getTemplateDetail"
    - frontend/src/pages/DesignerPage.tsx contains "activitiesToNodes"
    - frontend/src/pages/DesignerPage.tsx contains "flowsToEdges"
    - frontend/src/pages/DesignerPage.tsx contains "loadTemplate"
    - frontend/src/pages/DesignerPage.tsx contains "beforeunload"
    - frontend/src/components/designer/Canvas.tsx contains "nodeTypes"
    - frontend/src/components/designer/Canvas.tsx contains "edgeTypes"
    - frontend/src/components/designer/Canvas.tsx contains "ReactFlow"
    - frontend/src/components/designer/Canvas.tsx contains "screenToFlowPosition"
    - frontend/src/components/designer/Canvas.tsx contains "application/reactflow"
    - frontend/src/components/designer/Canvas.tsx contains "onDrop"
    - frontend/src/components/designer/Canvas.tsx contains "MiniMap"
    - frontend/src/components/designer/Canvas.tsx contains "snapToGrid"
    - frontend/src/components/designer/Canvas.tsx contains "snapGrid"
    - frontend/src/components/designer/NodePalette.tsx contains "draggable"
    - frontend/src/components/designer/NodePalette.tsx contains "application/reactflow"
    - frontend/src/components/designer/NodePalette.tsx contains "onDragStart"
    - frontend/src/components/designer/NodePalette.tsx contains "Activities"
    - frontend/src/App.tsx contains "DesignerPage"
    - `cd frontend && npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    Designer page loads templates from API and renders three-panel layout. Canvas displays React Flow with custom nodes/edges, minimap, snap-to-grid, dot background. Node palette allows dragging Start/Manual/Auto/End nodes onto canvas. Dropped nodes are created at drop position with correct type and default name. Node/edge selection updates store. Template data loads and deserializes to canvas on mount.
  </done>
</task>

<task type="auto">
  <name>Task 2: Properties panel, error panel, and toolbar</name>
  <files>
    frontend/src/components/designer/PropertiesPanel.tsx,
    frontend/src/components/designer/ErrorPanel.tsx,
    frontend/src/components/designer/Toolbar.tsx
  </files>
  <read_first>
    frontend/src/stores/designerStore.ts
    frontend/src/stores/uiStore.ts
    frontend/src/api/templates.ts
    frontend/src/api/users.ts
    frontend/src/types/api.ts
    frontend/src/types/designer.ts
    .planning/phases/08-visual-workflow-designer/08-UI-SPEC.md
  </read_first>
  <action>
    1. Create `frontend/src/components/designer/PropertiesPanel.tsx` (per D-09, D-10, D-11, D-12, DESIGN-04, DESIGN-05):
       - Width: 320px, right sidebar, border-l, bg-background, overflow-y auto
       - Read `selectedNodeId, selectedEdgeId, nodes, edges, updateNodeData, updateEdgeData` from `useDesignerStore`
       - Find selected node/edge: `nodes.find(n => n.id === selectedNodeId)` / `edges.find(e => e.id === selectedEdgeId)`

       **When a NODE is selected** (per D-09):
       - Header: "Activity Properties" in `text-lg font-semibold`
       - Activity type read-only `<Badge>`: show node.data.activityType with appropriate color
       - Name `<Input>` field with `<Label>` — onChange calls `updateNodeData(nodeId, { name: value })`
       - Description `<Textarea>` — onChange calls `updateNodeData(nodeId, { description: value })`
       - **Performer section** (only for manual nodes, per D-10):
         - "Performer" section header
         - Type `<Select>` with options: Supervisor, User, Group, Sequential, Runtime Selection
         - Dynamic second field based on selected type:
           - "user": user picker — use TanStack Query `useQuery({ queryKey: ['users'], queryFn: listUsers })` to fetch users, render as `<Select>` with user options. Selected value stored as `performerId`.
           - "group": group picker — use `useQuery({ queryKey: ['groups'], queryFn: listGroups })`, render as `<Select>`. Selected value stored as `performerId`.
           - "sequential": ordered list of user IDs in `performerList`. Show list with add/remove buttons. Each entry is a user `<Select>`.
           - "supervisor" / "runtime": no second field
         - On type change: call `updateNodeData(nodeId, { performerType: newType, performerId: null, performerList: null })`
         - On user/group select: call `updateNodeData(nodeId, { performerId: selectedId })`
       - **Trigger type** (shown when node has 2+ incoming edges):
         - `<Select>` with AND-join, OR-join options
         - onChange: `updateNodeData(nodeId, { triggerType: value })`
       - **Routing type** (shown when node has 2+ outgoing edges):
         - `<Select>` with options: None, Conditional, Performer Chosen, Broadcast
         - onChange: `updateNodeData(nodeId, { routingType: value })`
       - **Method name** (only for auto nodes):
         - `<Input>` for method name
         - onChange: `updateNodeData(nodeId, { methodName: value })`

       **When an EDGE is selected** (per D-09):
       - Header: "Flow Properties" in `text-lg font-semibold`
       - Flow type `<Select>`: Normal, Reject
       - Label `<Input>` for display_label
       - Condition expression `<Textarea>` (shown when source node has routing_type === 'conditional')
       - All changes call `updateEdgeData(edgeId, { ... })`

       **When NOTHING is selected** (per D-11, D-12):
       - Show `<Tabs>` with two tabs: "Template" and "Variables"
       - **Template tab**: Name `<Input>`, Description `<Textarea>` (read from/update template metadata — store a local templateName/templateDescription in DesignerPage and pass via props or context)
       - **Variables tab** (per DESIGN-05):
         - List existing variables with name, type `<Badge>`, delete `<Button>` (Trash icon)
         - "Add Variable" `<Button>` at bottom
         - Variable form (inline): name `<Input>`, type `<Select>` (string/int/boolean/date), default value `<Input>`
         - Variables managed locally in component state. On save (Plan 05), they sync to API.
         - Show each variable as a row: name, type badge, delete icon button

    2. Create `frontend/src/components/designer/ErrorPanel.tsx` (per D-14, UI-SPEC Section 6):
       - Read `errorPanelExpanded, setErrorPanelExpanded` from `useUiStore`
       - Props: `errors: ValidationErrorDetail[]`, `onErrorClick: (entityId: string) => void`
       - Collapsed: header only — "Errors ({count})" with chevron toggle button
       - Expanded: scrollable list (max-h-[200px]) of error items
       - Each error item:
         - Red `CircleX` icon (from lucide-react) + error message text
         - `onClick`: calls `onErrorClick(error.entity_id)` to pan to problem node
         - `role="listitem"` for accessibility
       - When `errors.length === 0` and panel was expanded by validation: show green `CheckCircle` icon + "Validation passed"
       - Header background: `bg-muted`, sticky top

    3. Create `frontend/src/components/designer/Toolbar.tsx` (per UI-SPEC Section 6 toolbar):
       - Props: `templateName: string`, `onSave: () => void`, `onValidateInstall: () => void`, `saving: boolean`, `validating: boolean`
       - Height: 48px, border-b, flex items-center, gap-2, px-4
       - Left section:
         - Back button (`ArrowLeft` icon) — navigates to `/templates` using `useNavigate()`. If `isDirty`, show confirmation dialog first.
         - Template name display in `text-lg font-semibold`
         - Unsaved changes indicator: amber dot (`w-2 h-2 rounded-full bg-amber-500`) next to name when `isDirty` is true
       - Right section (flex gap-2):
         - "Save Template" `<Button variant="secondary">` with `Save` icon — onClick calls `onSave`, shows spinner when `saving`
         - "Validate & Install" `<Button variant="default">` with `ShieldCheck` icon — onClick calls `onValidateInstall`, shows spinner when `validating`
         - Undo `<Button variant="ghost" size="icon">` with `Undo2` icon — onClick calls `useDesignerStore.getState().undo()`, disabled when `undoStack.length === 0`
         - Redo `<Button variant="ghost" size="icon">` with `Redo2` icon — onClick calls `useDesignerStore.getState().redo()`, disabled when `redoStack.length === 0`
         - Auto-layout `<Button variant="ghost" size="icon">` with `LayoutGrid` icon — onClick runs `getLayoutedElements(nodes, edges)` and sets result via `setNodes`/`setEdges`, `<Tooltip>` "Auto-arrange nodes"
       - All toolbar buttons have `aria-label` attributes (per accessibility)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Activity Properties"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Flow Properties"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "updateNodeData"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "updateEdgeData"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "performerType"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Supervisor"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "triggerType"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "routingType"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "methodName"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Variables"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Add Variable"
    - frontend/src/components/designer/PropertiesPanel.tsx contains "Tabs"
    - frontend/src/components/designer/ErrorPanel.tsx contains "ValidationErrorDetail"
    - frontend/src/components/designer/ErrorPanel.tsx contains "Errors"
    - frontend/src/components/designer/ErrorPanel.tsx contains "Validation passed"
    - frontend/src/components/designer/ErrorPanel.tsx contains "onErrorClick"
    - frontend/src/components/designer/Toolbar.tsx contains "Save Template"
    - frontend/src/components/designer/Toolbar.tsx contains "Validate"
    - frontend/src/components/designer/Toolbar.tsx contains "Undo"
    - frontend/src/components/designer/Toolbar.tsx contains "Redo"
    - frontend/src/components/designer/Toolbar.tsx contains "isDirty"
    - frontend/src/components/designer/Toolbar.tsx contains "aria-label"
    - `cd frontend && npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    Properties panel shows context-sensitive forms: activity properties when node selected (name, performer, trigger, routing, method), flow properties when edge selected (type, label, condition), template tabs when nothing selected (info + variables). Error panel shows validation errors with click-to-navigate. Toolbar has save, validate/install, undo/redo, and auto-layout buttons with unsaved changes indicator.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` completes without errors
- Designer page renders three-panel layout
- Canvas accepts drops from palette and creates nodes
- Properties panel updates node/edge data through store
- Toolbar has all required buttons with correct icons
</verification>

<success_criteria>
- User can drag nodes from palette to canvas (DESIGN-01, DESIGN-02)
- User can configure activity properties in right panel (DESIGN-04)
- User can define process variables in Variables tab (DESIGN-05)
- Canvas shows minimap, snap-to-grid, dot background
- Toolbar shows template name, unsaved indicator, all action buttons
- Error panel shows validation errors with click-to-pan
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-workflow-designer/08-04-SUMMARY.md`
</output>
