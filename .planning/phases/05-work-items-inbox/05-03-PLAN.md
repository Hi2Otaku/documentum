---
phase: 05-work-items-inbox
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/test_inbox.py
autonomous: true
requirements:
  - INBOX-01
  - INBOX-02
  - INBOX-03
  - INBOX-04
  - INBOX-05
  - INBOX-06
  - INBOX-07
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Work item appears in performer's inbox when workflow starts with performer_type=user"
    - "Work item appears in supervisor's inbox when performer_type=supervisor"
    - "Multiple work items created for group members when performer_type=group"
    - "Inbox list is filterable by state, priority, template name"
    - "Inbox list is sortable by priority, due_date, created_at"
    - "Work item detail includes activity info, workflow summary, and documents"
    - "Acquire transitions AVAILABLE->ACQUIRED, release transitions ACQUIRED->AVAILABLE"
    - "Complete from inbox advances workflow to next activity"
    - "Reject marks work item as REJECTED"
    - "Comments can be added and listed on work items"
    - "Priority and due_date are visible in inbox response"
  artifacts:
    - path: "tests/test_inbox.py"
      provides: "Integration tests for INBOX-01 through INBOX-07 and PERF-01 through PERF-03"
      min_lines: 200
  key_links:
    - from: "tests/test_inbox.py"
      to: "src/app/routers/inbox.py"
      via: "HTTP requests to /api/v1/inbox endpoints"
      pattern: "/api/v1/inbox"
    - from: "tests/test_inbox.py"
      to: "tests/conftest.py"
      via: "installed_template and other fixtures"
      pattern: "installed_template"
---

<objective>
Write integration tests covering all 10 phase requirements (INBOX-01 through INBOX-07 and PERF-01 through PERF-03).

Purpose: Proves the inbox API works end-to-end: work items appear for the right performers, inbox shows correct nested data, acquire/complete/reject lifecycle works, comments work, filtering/sorting works.
Output: tests/test_inbox.py with comprehensive integration test coverage.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-work-items-inbox/05-CONTEXT.md
@.planning/phases/05-work-items-inbox/05-RESEARCH.md
@.planning/phases/05-work-items-inbox/05-01-SUMMARY.md
@.planning/phases/05-work-items-inbox/05-02-SUMMARY.md

<interfaces>
<!-- Inbox API endpoints (from Plan 02) -->
Endpoints under /api/v1/inbox:
- GET  /inbox                          — list inbox items (filters: state, priority, template_name; sort: priority, due_date, created_at)
- GET  /inbox/{work_item_id}           — get detail with comments
- POST /inbox/{work_item_id}/acquire   — claim work item
- POST /inbox/{work_item_id}/release   — unclaim work item
- POST /inbox/{work_item_id}/complete  — complete (body: {"output_variables": {}})
- POST /inbox/{work_item_id}/reject    — reject (body: {"reason": "..."})
- GET  /inbox/{work_item_id}/comments  — list comments
- POST /inbox/{work_item_id}/comments  — add comment (body: {"content": "..."})

All require Authorization: Bearer {token}. All return EnvelopeResponse with "data" key.

<!-- Existing test patterns (from conftest.py) -->
Fixtures available:
- admin_user, admin_token — superuser with JWT
- regular_user, regular_token — non-admin user with JWT
- async_client — httpx AsyncClient wired to test DB
- installed_template — start -> manual(performer=admin_user) -> end, validated+installed
- parallel_template — start -> reviewA + reviewB (AND-join) -> merge -> end

<!-- Workflow API for test setup -->
POST /api/v1/workflows — start workflow: {"template_id": "...", "document_ids": [], "performer_overrides": {}}
GET /api/v1/workflows/{id} — get workflow detail with work_items list

<!-- User/Group API for test setup -->
POST /api/v1/groups/ — create group: {"name": "...", "description": "..."}
POST /api/v1/groups/{id}/users/{user_id} — add user to group

<!-- Template API for custom templates -->
POST /api/v1/templates/ — create template
POST /api/v1/templates/{id}/activities — add activity
POST /api/v1/templates/{id}/flows — add flow
POST /api/v1/templates/{id}/validate — validate
POST /api/v1/templates/{id}/install — install
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inbox integration tests for all 10 requirements</name>
  <files>tests/test_inbox.py</files>
  <read_first>tests/conftest.py, src/app/routers/inbox.py, src/app/services/inbox_service.py, src/app/schemas/inbox.py, src/app/models/enums.py, src/app/routers/workflows.py</read_first>
  <action>
Create tests/test_inbox.py with pytest-asyncio integration tests. Use the async_client fixture for all HTTP calls. Follow existing test patterns from conftest.py.

**Helper fixture needed at top of file:**

```python
@pytest.fixture
async def started_workflow(async_client, admin_token, installed_template):
    """Start a workflow from installed_template so a work item exists for admin_user."""
    headers = {"Authorization": f"Bearer {admin_token}"}
    resp = await async_client.post(
        "/api/v1/workflows",
        json={"template_id": installed_template["template_id"]},
        headers=headers,
    )
    assert resp.status_code == 201
    wf = resp.json()["data"]
    return wf
```

**Tests to implement:**

1. **test_work_item_appears_in_inbox** (INBOX-01, PERF-02)
   - Use started_workflow fixture (uses installed_template which has performer_type=user, performer_id=admin_user.id)
   - GET /api/v1/inbox with admin_token
   - Assert response status 200
   - Assert data is a list with at least 1 item
   - Assert first item has state "available"
   - Assert first item has "activity" dict with "name" key
   - Assert first item has "workflow" dict with "template_name" key

2. **test_inbox_item_detail** (INBOX-03)
   - Start workflow, GET inbox to get work_item_id
   - GET /api/v1/inbox/{work_item_id} with admin_token
   - Assert 200, data has "activity" with "name", "activity_type"
   - Assert data has "workflow" with "id", "template_name", "state"
   - Assert data has "documents" key (list, may be empty)
   - Assert data has "comments" key (list)

3. **test_inbox_shows_priority_due_date** (INBOX-07)
   - Start workflow, GET inbox
   - Assert each item has "priority" field (int)
   - Assert each item has "due_date" field (null is OK, but field must exist)

4. **test_inbox_filters_and_sorting** (INBOX-02)
   - Start workflow, GET inbox with ?state=available
   - Assert all returned items have state "available"
   - GET inbox with ?sort_by=priority&sort_order=asc
   - Assert 200 response
   - GET inbox with ?template_name=Simple (matches "Simple Workflow" from installed_template)
   - Assert at least 1 result returned

5. **test_acquire_and_release** (D-08, D-09)
   - Start workflow, get work_item_id from inbox
   - POST /inbox/{id}/acquire -> assert 200, state "acquired", performer_id matches admin_user
   - POST /inbox/{id}/release -> assert 200, state "available", performer_id is null
   - POST /inbox/{id}/acquire again -> assert 200 (can re-acquire)

6. **test_complete_from_inbox** (INBOX-04)
   - Start workflow, get work_item_id from inbox
   - POST /inbox/{id}/acquire first (required per D-08)
   - POST /inbox/{id}/complete with body {"output_variables": {}}
   - Assert 200, state "complete"
   - GET /api/v1/workflows/{wf_id} -> workflow should have advanced (check state or activity states)

7. **test_complete_requires_acquire** (D-08)
   - Start workflow, get work_item_id from inbox (state is AVAILABLE)
   - POST /inbox/{id}/complete WITHOUT acquiring first
   - Assert 400 error (must be ACQUIRED)

8. **test_reject_work_item** (INBOX-05, D-04)
   - Start workflow, get work_item_id
   - POST /inbox/{id}/acquire
   - POST /inbox/{id}/reject with body {"reason": "Needs revision"}
   - Assert 200, state "rejected"

9. **test_work_item_comments** (INBOX-06)
   - Start workflow, get work_item_id
   - POST /inbox/{id}/comments with body {"content": "This looks good"}
   - Assert 201, response has "content" == "This looks good"
   - POST /inbox/{id}/comments with body {"content": "Second comment"}
   - GET /inbox/{id}/comments -> assert 2 comments returned
   - GET /inbox/{id} -> assert comment_count == 2

10. **test_supervisor_performer** (PERF-01)
    - Create a new template with a manual activity that has performer_type="supervisor" (no performer_id)
    - Validate and install
    - Start workflow as admin_user (admin becomes supervisor)
    - GET /inbox with admin_token
    - Assert work item appears (because admin is the supervisor/initiator)

11. **test_group_performer** (PERF-03)
    - Create a group via POST /api/v1/groups/ {"name": "Reviewers"}
    - Add admin_user and regular_user to the group
    - Create a new template with manual activity performer_type="group", performer_id=group_id
    - Validate and install
    - Start workflow
    - GET /inbox with admin_token -> assert at least 1 item
    - GET /inbox with regular_token -> assert at least 1 item
    - Per D-06: one work item per group member (parallel)

Each test function should be async (use @pytest.mark.asyncio or rely on pytest-asyncio auto mode). Use descriptive assertion messages.

Import pytest at the top. No need for additional fixtures beyond what conftest.py provides plus the started_workflow fixture defined in this file.
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone && pytest tests/test_inbox.py -x --timeout=60 -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "test_work_item_appears_in_inbox" tests/test_inbox.py returns a match
    - grep "test_inbox_item_detail" tests/test_inbox.py returns a match
    - grep "test_inbox_shows_priority_due_date" tests/test_inbox.py returns a match
    - grep "test_inbox_filters_and_sorting" tests/test_inbox.py returns a match
    - grep "test_acquire_and_release" tests/test_inbox.py returns a match
    - grep "test_complete_from_inbox" tests/test_inbox.py returns a match
    - grep "test_complete_requires_acquire" tests/test_inbox.py returns a match
    - grep "test_reject_work_item" tests/test_inbox.py returns a match
    - grep "test_work_item_comments" tests/test_inbox.py returns a match
    - grep "test_supervisor_performer" tests/test_inbox.py returns a match
    - grep "test_group_performer" tests/test_inbox.py returns a match
    - pytest tests/test_inbox.py -x passes all tests
    - pytest tests/ -x passes (full suite regression)
  </acceptance_criteria>
  <done>11 integration tests covering all 10 requirements pass. INBOX-01 through INBOX-07 verified via HTTP endpoints. PERF-01/02/03 verified with supervisor, user, and group performer templates. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- pytest tests/test_inbox.py -x --timeout=60 passes all 11 tests
- pytest tests/ -x passes entire test suite (no regressions)
- Each requirement has at least one test covering it
</verification>

<success_criteria>
- 11 test functions covering INBOX-01 through INBOX-07 and PERF-01 through PERF-03
- All tests pass against the live API via async_client
- Tests exercise: inbox listing, detail view, filtering, sorting, acquire/release, complete, reject, comments, supervisor performer, group performer
- Full test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-work-items-inbox/05-03-SUMMARY.md`
</output>
