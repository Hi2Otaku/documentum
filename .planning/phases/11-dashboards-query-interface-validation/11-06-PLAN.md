---
phase: 11-dashboards-query-interface-validation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/main.py
  - src/app/routers/dashboard.py
autonomous: true
gap_closure: true
requirements:
  - BAM-01
  - BAM-02
  - BAM-03
  - BAM-04
  - EXAMPLE-03

must_haves:
  truths:
    - "All dashboard endpoints use get_current_active_admin dependency"
    - "Audit router is registered in main.py and GET /api/v1/audit returns 200"
  artifacts:
    - path: "src/app/main.py"
      provides: "Audit router registration restored"
      contains: "audit.router"
    - path: "src/app/routers/dashboard.py"
      provides: "Admin-only dashboard access"
      contains: "get_current_active_admin"
  key_links:
    - from: "src/app/main.py"
      to: "src/app/routers/audit.py"
      via: "include_router"
      pattern: "application\\.include_router\\(audit\\.router"
---

<objective>
Fix two regressions from Phase 11 execution: (1) restore the audit router registration that was dropped from main.py by commit 9fc9e56, and (2) change all dashboard endpoints from get_current_user to get_current_active_admin for proper admin-only access.

Purpose: Unblocks EXAMPLE-03 (E2E audit trail query) and secures BAM dashboard endpoints to admin-only access.
Output: Fixed main.py and dashboard.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboards-query-interface-validation/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore audit router registration in main.py</name>
  <files>src/app/main.py</files>
  <read_first>
    - src/app/main.py
    - src/app/routers/audit.py (first 5 lines to confirm router variable name)
  </read_first>
  <action>
    Fix the regression from Phase 11-01 commit 9fc9e56 that dropped the audit router.

    1. In the import line (line 9), add `audit` to the imported routers. The current import is:
       `from app.routers import aliases, auth, dashboard, documents, groups, health, inbox, lifecycle, query, roles, templates, users, workflows`
       Change to:
       `from app.routers import aliases, audit, auth, dashboard, documents, groups, health, inbox, lifecycle, query, roles, templates, users, workflows`

    2. Add audit router registration in create_app(). Insert after the lifecycle router line (line 86) and before the dashboard router line (line 87):
       `application.include_router(audit.router, prefix=settings.api_v1_prefix)`

    Also check if `queues` router is missing (it exists as src/app/routers/queues.py). If missing, add it too for completeness. Add import and include_router call.

    Do NOT change anything else in main.py.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -c "from app.main import app; routes = [r.path for r in app.routes]; assert '/api/v1/audit' in str(routes), f'audit not found in {routes}'"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.routers import ..., audit, ...` present in main.py
    - `application.include_router(audit.router, prefix=settings.api_v1_prefix)` present in main.py
    - GET /api/v1/audit route is registered in the FastAPI app
  </acceptance_criteria>
  <done>Audit router registered in main.py. GET /api/v1/audit no longer returns 404.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dashboard auth to require admin</name>
  <files>src/app/routers/dashboard.py</files>
  <read_first>
    - src/app/routers/dashboard.py
    - src/app/core/dependencies.py (to confirm get_current_active_admin signature)
  </read_first>
  <action>
    Change all four dashboard endpoints from `get_current_user` to `get_current_active_admin`.

    1. Update the import line: change `from app.core.dependencies import get_current_user` to `from app.core.dependencies import get_current_active_admin`

    2. On each of the four endpoint functions (workflow_summary, bottleneck_activities, user_workload, template_metrics), change:
       `current_user: User = Depends(get_current_user)`
       to:
       `current_user: User = Depends(get_current_active_admin)`

    This is lines 26, 40, 53, 66 in the current file. There are exactly 4 occurrences to change.

    Do NOT modify any other logic, response models, or service calls.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -c "
import inspect
from app.routers.dashboard import workflow_summary, bottleneck_activities, user_workload, template_metrics
for fn in [workflow_summary, bottleneck_activities, user_workload, template_metrics]:
    src = inspect.getsource(fn)
    assert 'get_current_active_admin' in src, f'{fn.__name__} missing admin auth'
    assert 'get_current_user' not in src, f'{fn.__name__} still uses get_current_user'
print('All dashboard endpoints use get_current_active_admin')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `get_current_user` in src/app/routers/dashboard.py
    - Four occurrences of `get_current_active_admin` in src/app/routers/dashboard.py (one per endpoint)
    - Import line references `get_current_active_admin` not `get_current_user`
  </acceptance_criteria>
  <done>All four dashboard endpoints require admin authentication via get_current_active_admin. Non-admin users get 403.</done>
</task>

</tasks>

<verification>
- `python -c "from app.main import app; routes=[r.path for r in app.routes]; print([r for r in routes if 'audit' in str(r) or 'dashboard' in str(r)])"` shows both audit and dashboard routes
- `python -m pytest tests/test_contract_approval.py::test_contract_approval_e2e_execution -x` should no longer fail with 404 on /api/v1/audit (may still fail for other reasons until Plan 07 completes)
- `grep -c "get_current_active_admin" src/app/routers/dashboard.py` returns 4
- `grep -c "get_current_user" src/app/routers/dashboard.py` returns 0
</verification>

<success_criteria>
1. Audit router is registered in main.py - GET /api/v1/audit no longer returns 404
2. All four dashboard endpoints use get_current_active_admin - non-admin users receive 403
3. No other functionality is broken by these changes
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboards-query-interface-validation/11-06-SUMMARY.md`
</output>
