---
phase: 27-document-type-system
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - frontend/src/components/documents/TypeSelector.tsx
  - frontend/src/components/documents/TypeMetadataForm.tsx
  - frontend/src/components/documents/TypeBadge.tsx
  - frontend/src/components/documents/DocumentTable.tsx
  - frontend/src/components/documents/DocumentDetailPanel.tsx
  - frontend/src/components/documents/DocumentDropZone.tsx
  - frontend/src/api/documents.ts
autonomous: false
requirements:
  - TYPE-02
  - TYPE-05

must_haves:
  truths:
    - "User can select a document type when uploading a document"
    - "Type-specific metadata fields render dynamically from the JSON Schema"
    - "User can see the assigned type in the document table and detail panel"
    - "Untyped documents show em-dash in the type column"
    - "Inherited fields show (inherited) label"
  artifacts:
    - path: "frontend/src/components/documents/TypeSelector.tsx"
      provides: "Type dropdown for document forms"
      contains: "TypeSelector"
    - path: "frontend/src/components/documents/TypeMetadataForm.tsx"
      provides: "Dynamic form fields from JSON Schema"
      contains: "TypeMetadataForm"
    - path: "frontend/src/components/documents/TypeBadge.tsx"
      provides: "Type name display for table cells"
      contains: "TypeBadge"
  key_links:
    - from: "frontend/src/components/documents/DocumentDropZone.tsx"
      to: "frontend/src/api/documents.ts"
      via: "uploadDocument with documentTypeId"
      pattern: "documentTypeId"
    - from: "frontend/src/components/documents/TypeMetadataForm.tsx"
      to: "frontend/src/api/documentTypes.ts"
      via: "uses DocumentTypeResponse for schema"
      pattern: "metadata_schema"
    - from: "frontend/src/components/documents/DocumentTable.tsx"
      to: "frontend/src/components/documents/TypeBadge.tsx"
      via: "TypeBadge in Type column"
      pattern: "TypeBadge"
---

<objective>
Integrate document type selection and dynamic metadata forms into the document upload/edit experience, plus type display in tables and detail panel.

Purpose: Completes the frontend user experience so users can assign types at upload, see type-specific metadata fields, and view types in the document list. Satisfies TYPE-02 and TYPE-05.
Output: TypeSelector, TypeMetadataForm, TypeBadge components; modified DocumentDropZone, DocumentTable, DocumentDetailPanel, documents API client.
</objective>

<execution_context>
@D:\Python\documentum_clone\.claude\get-shit-done\workflows\execute-plan.md
@D:\Python\documentum_clone\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-document-type-system/27-CONTEXT.md
@.planning/phases/27-document-type-system/27-UI-SPEC.md
@.planning/phases/27-document-type-system/27-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 outputs -->

From frontend/src/api/documentTypes.ts:
```typescript
export interface DocumentTypeResponse {
  id: string; name: string; description: string | null;
  metadata_schema: Record<string, unknown>;
  parent_type_id: string | null; parent_type_name: string | null;
  field_count: number; document_count: number;
  created_at: string; updated_at: string;
}
export async function fetchDocumentTypes(): Promise<DocumentTypeResponse[]>
```

From frontend/src/api/documents.ts (current uploadDocument):
```typescript
export async function uploadDocument(file: File, title: string, author: string): Promise<DocumentResponse>
```

From frontend/src/api/documents.ts (DocumentResponse — modified in Plan 01 backend, needs frontend update):
```typescript
export interface DocumentResponse {
  // ... existing fields
  document_type_id: string | null;  // NEW from backend
  document_type_name: string | null; // NEW from backend
}
```

From frontend/src/components/documents/DocumentDropZone.tsx:
```typescript
interface DocumentDropZoneProps { onUploadComplete: () => void; }
// Currently calls: uploadDocument(file, title, username)
```

From frontend/src/components/documents/DocumentTable.tsx:
```typescript
// columns: thumbnail, title, author, lifecycle_state, current_version, locked_by, updated_at
// Need to add "Type" column after lifecycle_state, before current_version
```

From frontend/src/components/documents/DocumentDetailPanel.tsx:
```typescript
// Metadata card has: Filename, Type (content_type), Author, Created, Updated, Current Version
// Need to rename "Type" to "MIME Type" and add "Doc Type" row
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeSelector, TypeMetadataForm, TypeBadge and update documents API</name>
  <files>
    frontend/src/components/documents/TypeSelector.tsx
    frontend/src/components/documents/TypeMetadataForm.tsx
    frontend/src/components/documents/TypeBadge.tsx
    frontend/src/api/documents.ts
  </files>
  <read_first>
    frontend/src/api/documents.ts
    frontend/src/api/documentTypes.ts
    frontend/src/components/documents/DocumentTable.tsx
    frontend/src/components/documents/DocumentDetailPanel.tsx
    frontend/src/components/documents/DocumentDropZone.tsx
  </read_first>
  <action>
1. **frontend/src/api/documents.ts**:
   - Add `document_type_id: string | null;` and `document_type_name: string | null;` to `DocumentResponse` interface.
   - Modify `uploadDocument` signature to accept optional `documentTypeId?: string` and `customProperties?: Record<string, unknown>` parameters. In the FormData construction, if `documentTypeId` is provided, `formData.append("document_type_id", documentTypeId)`. If `customProperties` is provided, `formData.append("custom_properties", JSON.stringify(customProperties))`.
   - Add `updateDocument(id: string, data: { title?: string; author?: string; document_type_id?: string | null; custom_properties?: Record<string, unknown> }): Promise<DocumentResponse>` function using `apiMutate` with PUT to `/api/v1/documents/${id}`.

2. **frontend/src/components/documents/TypeBadge.tsx**:
   - Props: `typeName: string | null`.
   - Renders: If `typeName` is non-null, render `<span className="text-sm">{typeName}</span>`. If null, render `<span className="text-sm text-muted-foreground">{"\u2014"}</span>` (em-dash entity).

3. **frontend/src/components/documents/TypeSelector.tsx**:
   - Props: `value: string | null`, `onChange: (value: string | null) => void`, `disabled?: boolean`.
   - Uses `useQuery` with key `["documentTypes"]` calling `fetchDocumentTypes()`.
   - Renders: shadcn `Select` component. Placeholder: "Select a type...". First option: `<SelectItem value="__none__">No type</SelectItem>`. Then all types sorted alphabetically. Child types (those with `parent_type_id`) indented with 2-space prefix: `"  {name}"`. On value change, if `"__none__"` map to `null`, otherwise pass the type ID.
   - While loading: disabled with placeholder "Loading types...".
   - Expose `selectedType: DocumentTypeResponse | null` by finding the matching type from the query data -- useful for parent component to get the schema.

4. **frontend/src/components/documents/TypeMetadataForm.tsx**:
   - Props: `schema: Record<string, unknown>` (the effective JSON Schema -- already merged if child type), `values: Record<string, unknown>`, `onChange: (values: Record<string, unknown>) => void`, `errors?: Record<string, string>`, `inheritedFields?: string[]` (field keys that come from parent type).
   - Section header: `<div className="text-sm font-semibold text-muted-foreground uppercase tracking-wider">Type-Specific Metadata</div>`.
   - Field rendering from `schema.properties` (iterate object keys):
     - `"type": "string"` with no enum, no format -> `<Input type="text" />`
     - `"type": "string"` with `"format": "date"` -> `<Input type="date" />`
     - `"type": "string"` with `"enum": [...]` -> `<Select>` with enum values as options
     - `"type": "number"` -> `<Input type="number" />`
     - `"type": "boolean"` -> `<Checkbox />`
   - Each field wrapped with `<Label>` using the property `title` if available, else the property key. Required fields (key in `schema.required` array) show asterisk `<span className="text-destructive">*</span>` and `aria-required="true"` on the input.
   - Inherited fields: if key is in `inheritedFields` array, append `<span className="text-xs text-muted-foreground">(inherited)</span>` after the label text. Render inherited fields first.
   - Field gap: `space-y-4`.
   - Error display: if `errors[key]` exists, show `<span className="text-sm text-destructive">{errors[key]}</span>` below the field.
   - Field order: inherited fields first (sorted by key), then own fields (in `properties` key order).
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "document_type_id" frontend/src/api/documents.ts` finds the field in DocumentResponse and uploadDocument
    - `grep "document_type_name" frontend/src/api/documents.ts` finds the field in DocumentResponse
    - `grep "updateDocument" frontend/src/api/documents.ts` finds the new function
    - `grep "TypeBadge" frontend/src/components/documents/TypeBadge.tsx` finds the component export
    - `grep "TypeSelector" frontend/src/components/documents/TypeSelector.tsx` finds the component export
    - `grep "Select a type" frontend/src/components/documents/TypeSelector.tsx` finds the placeholder
    - `grep "No type" frontend/src/components/documents/TypeSelector.tsx` finds the null option
    - `grep "TypeMetadataForm" frontend/src/components/documents/TypeMetadataForm.tsx` finds the component export
    - `grep "inherited" frontend/src/components/documents/TypeMetadataForm.tsx` finds the inherited label
    - `grep "Type-Specific Metadata" frontend/src/components/documents/TypeMetadataForm.tsx` finds the section header
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>TypeSelector renders type dropdown with "No type" option and indented child types. TypeMetadataForm renders dynamic fields from JSON Schema with inherited labels. TypeBadge shows type name or em-dash. Documents API supports document_type_id in upload and update.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate type components into DocumentTable, DetailPanel, and DropZone</name>
  <files>
    frontend/src/components/documents/DocumentTable.tsx
    frontend/src/components/documents/DocumentDetailPanel.tsx
    frontend/src/components/documents/DocumentDropZone.tsx
  </files>
  <read_first>
    frontend/src/components/documents/DocumentTable.tsx
    frontend/src/components/documents/DocumentDetailPanel.tsx
    frontend/src/components/documents/DocumentDropZone.tsx
    frontend/src/api/documents.ts
    frontend/src/api/documentTypes.ts
    frontend/src/components/documents/TypeSelector.tsx
    frontend/src/components/documents/TypeMetadataForm.tsx
    frontend/src/components/documents/TypeBadge.tsx
  </read_first>
  <action>
1. **frontend/src/components/documents/DocumentTable.tsx**:
   - Import `TypeBadge` from `./TypeBadge`.
   - Add a new column after `lifecycle_state` (State) and before `current_version` (Version):
     ```typescript
     columnHelper.accessor("document_type_name", {
       header: "Type",
       cell: (info) => <TypeBadge typeName={info.getValue()} />,
       size: 120,
     }),
     ```
   - This goes between the `lifecycle_state` column and `current_version` column in the columns array.

2. **frontend/src/components/documents/DocumentDetailPanel.tsx**:
   - In the metadata card grid, rename the existing "Type" label to "MIME Type" (this shows `document.content_type`).
   - Add a new row after "MIME Type": Label "Doc Type", Value shows `document.document_type_name ?? "\u2014"` (em-dash for untyped).
   - Import `TypeBadge` and use it for the Doc Type value, or just use inline span with em-dash fallback.

3. **frontend/src/components/documents/DocumentDropZone.tsx**:
   - This is the most significant change. The current DropZone immediately uploads on file drop/select. We need to add a pre-upload step where the user can optionally select a type and fill in metadata.
   - Add state: `pendingFiles: File[] | null`, `selectedTypeId: string | null`, `metadata: Record<string, unknown>`, `showUploadForm: boolean`.
   - Flow change:
     a. When files are dropped/selected, instead of uploading immediately, set `pendingFiles` and show upload form (if single file).
     b. Upload form shows: file name, TypeSelector dropdown, TypeMetadataForm (if type selected), "Upload" and "Cancel" buttons.
     c. For multiple files: skip the form and upload without type (preserving current behavior for batch uploads).
     d. On "Upload" click: call `uploadDocument(file, title, username, selectedTypeId || undefined, metadata && Object.keys(metadata).length > 0 ? metadata : undefined)`.
     e. On "Cancel": clear pendingFiles and showUploadForm.
   - Import `TypeSelector` and `TypeMetadataForm` from sibling components.
   - Import `fetchDocumentType` from `../../api/documentTypes` to get the effective schema when a type is selected (or use the schema from the TypeSelector's fetched types data).
   - When `selectedTypeId` changes: find the matching type from TypeSelector data, get its `metadata_schema`. If the type has a `parent_type_id`, fetch the parent type and merge schemas. Or simpler: store the fetched types list and compute merged schema client-side using the parent's metadata_schema + child's metadata_schema.
   - Handle API 422 errors on upload: parse the error response to extract field-level validation errors and display them in the TypeMetadataForm `errors` prop. Also show a toast: "Metadata validation failed: {count} field(s) have errors".
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TypeBadge" frontend/src/components/documents/DocumentTable.tsx` finds the import and usage
    - `grep "document_type_name" frontend/src/components/documents/DocumentTable.tsx` finds the column accessor
    - `grep "MIME Type" frontend/src/components/documents/DocumentDetailPanel.tsx` finds the renamed label
    - `grep "Doc Type" frontend/src/components/documents/DocumentDetailPanel.tsx` finds the new label
    - `grep "TypeSelector" frontend/src/components/documents/DocumentDropZone.tsx` finds the import
    - `grep "TypeMetadataForm" frontend/src/components/documents/DocumentDropZone.tsx` finds the import
    - `grep "documentTypeId\|document_type_id" frontend/src/components/documents/DocumentDropZone.tsx` finds the type ID being passed to upload
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>DocumentTable shows Type column with TypeBadge. DocumentDetailPanel shows Doc Type row with renamed MIME Type. DocumentDropZone has pre-upload form with TypeSelector and TypeMetadataForm for single-file uploads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete document type system end-to-end</name>
  <files>n/a</files>
  <action>Human verification checkpoint. No code changes -- user tests the full document type system flow in the browser.</action>
  <what-built>Complete Document Type System: admin type management page, document type assignment on upload, metadata validation, dynamic form rendering, type display in tables/detail.</what-built>
  <how-to-verify>
    1. Start the application (docker compose up, or manual backend + frontend dev servers).
    2. Log in as admin user.
    3. Navigate to /admin/types via the "Types" sidebar link -- verify the page loads with an empty state message.
    4. Click "Create Type" -- fill in name "Invoice", add JSON schema: `{"type":"object","properties":{"invoice_number":{"type":"string","title":"Invoice Number"},"amount":{"type":"number","title":"Amount"},"due_date":{"type":"string","format":"date","title":"Due Date"}},"required":["invoice_number"]}`. Save -- verify toast "Type created" and type appears in table.
    5. Create another type "Financial Report" with no parent and schema with property "fiscal_year" (type: number, required).
    6. Edit "Invoice" and set Parent Type to "Financial Report" -- save.
    7. Go to Documents page. Drop/select a single file. Verify the upload form appears with Type dropdown.
    8. Select "Invoice" type. Verify metadata fields appear: "Fiscal Year" (inherited), "Invoice Number", "Amount", "Due Date".
    9. Try to upload without filling "Invoice Number" (required) -- verify 422 error with field-level message.
    10. Fill all required fields and upload -- verify success.
    11. In the documents table, verify the "Type" column shows "Invoice" for the uploaded document.
    12. Click the document row -- verify the detail panel shows "Doc Type: Invoice" and "MIME Type" label (not just "Type").
  </how-to-verify>
  <verify>
    <automated>cd D:/Python/documentum_clone && pytest tests/test_document_types.py -x --timeout=60 && cd D:/Python/documentum_clone/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>User has verified: type CRUD works, type assignment on upload works, metadata validation rejects missing required fields, dynamic form renders correct controls, type column and detail panel display correctly.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` -- no TypeScript errors
- `cd frontend && npx vite build` -- production build succeeds
- `pytest tests/test_document_types.py -x` -- all backend tests still pass
- Manual verification of the full flow (checkpoint task)
</verification>

<success_criteria>
- User can select a type when uploading a document and fill in type-specific metadata
- Dynamic form renders correct controls for string, number, boolean, date, enum fields
- Required field validation shows errors from backend
- Type column visible in document table with TypeBadge
- Detail panel shows Doc Type and renamed MIME Type
- Inherited fields show "(inherited)" label
- Untyped documents show em-dash
</success_criteria>

<output>
After completion, create `.planning/phases/27-document-type-system/27-04-SUMMARY.md`
</output>
