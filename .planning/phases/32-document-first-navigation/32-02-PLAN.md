---
phase: 32-document-first-navigation
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/SidebarNav.tsx
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Navigating to /browse renders the BrowsePage component"
    - "Navigating to / redirects to /browse"
    - "Browse appears as the first item in the sidebar navigation"
    - "Existing pages (Documents, Folders, Search) remain accessible"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "/browse route and / redirect"
      contains: "BrowsePage"
    - path: "frontend/src/components/layout/SidebarNav.tsx"
      provides: "Browse nav item as first entry"
      contains: "Browse"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/BrowsePage.tsx"
      via: "Route element import"
      pattern: "BrowsePage"
    - from: "frontend/src/components/layout/SidebarNav.tsx"
      to: "/browse"
      via: "NAV_ITEMS array entry"
      pattern: "/browse"
---

<objective>
Wire BrowsePage into the application routing and sidebar navigation, making `/browse` the default entry point.

Purpose: Users land on Browse by default, and can always navigate back to it from the sidebar.
Output: Updated App.tsx routes and SidebarNav.tsx navigation items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-document-first-navigation/32-CONTEXT.md
@.planning/phases/32-document-first-navigation/32-01-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/components/layout/SidebarNav.tsx

<interfaces>
From frontend/src/pages/BrowsePage.tsx (created in plan 01):
```typescript
export function BrowsePage(): JSX.Element;
```

From frontend/src/App.tsx (current routing structure):
```typescript
// Current: <Route path="/" element={<Navigate to="/inbox" replace />} />
// Current protected routes under <AppShell>: /templates, /inbox, /documents, /search, /workflows
// Admin routes: /dashboard, /query, /admin/types, /admin/folders
```

From frontend/src/components/layout/SidebarNav.tsx:
```typescript
interface NavItem { icon: LucideIcon; label: string; route: string; adminOnly: boolean; }
const NAV_ITEMS: NavItem[] = [
  { icon: LayoutTemplate, label: "Templates", route: "/templates", adminOnly: false },
  { icon: Inbox, label: "Inbox", route: "/inbox", adminOnly: false },
  { icon: FileText, label: "Documents", route: "/documents", adminOnly: false },
  { icon: Search, label: "Search", route: "/search", adminOnly: false },
  { icon: GitBranch, label: "Workflows", route: "/workflows", adminOnly: false },
  // ... admin items
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /browse route to App.tsx and redirect / to /browse</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current routing)
    - frontend/src/pages/BrowsePage.tsx (verify export exists from plan 01)
  </read_first>
  <action>
    Modify `frontend/src/App.tsx` (per D-03):

    1. Add import: `import { BrowsePage } from "./pages/BrowsePage";`

    2. Change the root redirect from `/inbox` to `/browse`:
       - Before: `<Route path="/" element={<Navigate to="/inbox" replace />} />`
       - After: `<Route path="/" element={<Navigate to="/browse" replace />} />`

    3. Add `/browse` route inside the protected `<AppShell>` routes, as the FIRST route:
       `<Route path="/browse" element={<BrowsePage />} />`
       Place it before `/templates`.

    4. Change the catch-all redirect from `/inbox` to `/browse`:
       - Before: `<Route path="*" element={<Navigate to="/inbox" replace />} />`
       - After: `<Route path="*" element={<Navigate to="/browse" replace />} />`

    Keep ALL existing routes intact — /inbox, /documents, /search, /workflows, /admin/folders all remain.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "BrowsePage" frontend/src/App.tsx
    - grep -q '"/browse"' frontend/src/App.tsx
    - grep -q 'Navigate to="/browse"' frontend/src/App.tsx
    - grep -q '"/inbox"' frontend/src/App.tsx
  </acceptance_criteria>
  <done>
    /browse route renders BrowsePage. / and catch-all redirect to /browse. All existing routes preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Browse as first sidebar navigation item</name>
  <files>frontend/src/components/layout/SidebarNav.tsx</files>
  <read_first>
    - frontend/src/components/layout/SidebarNav.tsx (current NAV_ITEMS array)
  </read_first>
  <action>
    Modify `frontend/src/components/layout/SidebarNav.tsx` (per D-11, D-12):

    1. Add import of `FolderTree` icon from lucide-react (use `FolderTree` icon, not `FolderOpen` which is already used for Folders admin page). If `FolderTree` is not available in the lucide-react version, use `LibraryBig` or `FolderSearch` instead. Check the available icons by looking at existing imports.

    2. Add Browse as the FIRST item in the NAV_ITEMS array:
       ```typescript
       { icon: FolderTree, label: "Browse", route: "/browse", adminOnly: false },
       ```
       This goes BEFORE the existing Templates entry.

    Keep all other nav items unchanged. The existing "Folders" admin item remains at `/admin/folders`.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Browse" frontend/src/components/layout/SidebarNav.tsx
    - grep -q "/browse" frontend/src/components/layout/SidebarNav.tsx
  </acceptance_criteria>
  <done>
    Browse appears as the first item in sidebar navigation with a folder tree icon. All other nav items remain unchanged.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- Navigating to `/` redirects to `/browse`
- `/browse` route exists and renders BrowsePage
- Sidebar shows "Browse" as first nav item
- All existing routes (/inbox, /documents, /search, etc.) still work
</verification>

<success_criteria>
- /browse route renders BrowsePage component
- / redirects to /browse (per D-03)
- Browse is the first sidebar nav item (per D-11)
- All existing pages remain accessible (per D-12)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-document-first-navigation/32-02-SUMMARY.md`
</output>
