---
phase: 13-inbox-work-items
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/pages/InboxPage.tsx
  - frontend/src/components/inbox/InboxTable.tsx
  - frontend/src/components/inbox/InboxDetailPanel.tsx
  - frontend/src/components/inbox/CommentList.tsx
  - frontend/src/components/inbox/CommentCompose.tsx
autonomous: true
requirements: [INB-01, INB-02]

must_haves:
  truths:
    - "User can view pending work items in a paginated table with task name, workflow, priority, state badge, and created date columns"
    - "User can filter work items by state (All, Available, Acquired, Delegated)"
    - "User can click a work item row to see its full details in the right panel"
    - "Detail panel shows activity name, state badge, priority, workflow context, instructions, and comment history"
    - "User can add a comment to a work item from the detail panel"
  artifacts:
    - path: "frontend/src/pages/InboxPage.tsx"
      provides: "Split-pane inbox page with tabs"
      min_lines: 50
    - path: "frontend/src/components/inbox/InboxTable.tsx"
      provides: "TanStack Table with columns, pagination, row selection"
      min_lines: 80
    - path: "frontend/src/components/inbox/InboxDetailPanel.tsx"
      provides: "Right panel showing work item details"
      min_lines: 60
    - path: "frontend/src/components/inbox/CommentList.tsx"
      provides: "Chronological comment display"
      exports: ["CommentList"]
    - path: "frontend/src/components/inbox/CommentCompose.tsx"
      provides: "Comment input with submit"
      exports: ["CommentCompose"]
  key_links:
    - from: "frontend/src/pages/InboxPage.tsx"
      to: "frontend/src/api/inbox.ts"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*inbox"
    - from: "frontend/src/components/inbox/InboxTable.tsx"
      to: "frontend/src/api/inbox.ts"
      via: "InboxItemResponse type"
      pattern: "InboxItemResponse"
    - from: "frontend/src/components/inbox/InboxDetailPanel.tsx"
      to: "frontend/src/api/inbox.ts"
      via: "fetchInboxItem query"
      pattern: "fetchInboxItem"
    - from: "frontend/src/components/inbox/CommentCompose.tsx"
      to: "frontend/src/api/inbox.ts"
      via: "addComment mutation"
      pattern: "addComment"
---

<objective>
Build the My Inbox tab with split-pane layout: work item table on the left with filtering and pagination, detail panel on the right with work item information and comment history.

Purpose: This is the primary daily-use surface -- users see and interact with their assigned workflow tasks here. The table + detail split-pane pattern (per D-01) enables rapid scanning and in-context viewing.
Output: Fully functional InboxPage (My Inbox tab), InboxTable, InboxDetailPanel, CommentList, CommentCompose. Queues tab shows placeholder (Plan 03 fills it in).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-inbox-work-items/13-CONTEXT.md
@.planning/phases/13-inbox-work-items/13-UI-SPEC.md
@.planning/phases/13-inbox-work-items/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From frontend/src/api/inbox.ts:
```typescript
export interface InboxItemResponse {
  id: string; state: string; priority: number; due_date: string | null;
  instructions: string | null; performer_id: string | null; created_at: string;
  completed_at: string | null; activity: ActivitySummary; workflow: WorkflowSummary;
  documents: DocumentSummary[]; comment_count: number;
}
export interface InboxItemDetailResponse extends InboxItemResponse {
  comments: CommentResponse[];
}
export interface CommentResponse {
  id: string; user_id: string; content: string; created_at: string;
}
export interface PaginationMeta {
  page: number; page_size: number; total_count: number; total_pages: number;
}
export interface PaginatedInboxResponse {
  data: InboxItemResponse[]; meta: PaginationMeta;
}
export function fetchInboxItems(params: InboxListParams): Promise<PaginatedInboxResponse>;
export function fetchInboxItem(id: string): Promise<InboxItemDetailResponse>;
export function fetchComments(workItemId: string): Promise<CommentResponse[]>;
export function addComment(workItemId: string, content: string): Promise<CommentResponse>;
```

From frontend/src/components/inbox/:
```typescript
export function WorkItemStateBadge({ state }: { state: string }): JSX.Element;
export function PriorityIcon({ priority }: { priority: number }): JSX.Element | null;
export function InboxEmptyState({ heading, body, action? }: { heading: string; body: string; action?: React.ReactNode }): JSX.Element;
```

From frontend/src/stores/authStore.ts:
```typescript
export const useAuthStore: { userId: string | null; token: string | null; ... }
```

Existing patterns from frontend/src/components/query/QueryResultTable.tsx:
- Uses @tanstack/react-table with useReactTable, getCoreRowModel, getSortedRowModel
- Uses shadcn Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- Pagination with "Page X of Y", Previous/Next buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build InboxTable with TanStack Table, filtering, and pagination</name>
  <files>frontend/src/components/inbox/InboxTable.tsx</files>
  <read_first>
    - frontend/src/components/query/QueryResultTable.tsx (TanStack Table pattern to adapt)
    - frontend/src/components/ui/table.tsx (shadcn Table components)
    - frontend/src/components/ui/select.tsx (for state filter dropdown)
    - frontend/src/components/ui/skeleton.tsx (for loading state)
    - frontend/src/components/ui/button.tsx (for pagination buttons)
    - .planning/phases/13-inbox-work-items/13-UI-SPEC.md (layout spec, column definitions, pagination spec)
    - frontend/src/api/inbox.ts (InboxItemResponse type — from Plan 01)
  </read_first>
  <action>
Create `frontend/src/components/inbox/InboxTable.tsx`:

**Props interface:**
```typescript
interface InboxTableProps {
  items: InboxItemResponse[];
  isLoading: boolean;
  selectedId: string | null;
  onSelectItem: (id: string) => void;
  // Pagination
  currentPage: number;
  totalPages: number;
  onPageChange: (page: number) => void;
  // Filter
  stateFilter: string;
  onStateFilterChange: (state: string) => void;
}
```

**Filter bar** (above table, 48px height, per D-03):
- State filter: shadcn Select with options "All" (value ""), "Available" (value "available"), "Acquired" (value "acquired"), "Delegated" (value "delegated")
- Label "State" to the left of the select (text-xs text-muted-foreground)
- Wrap in a div with `h-12 flex items-center gap-2 px-4` and background `bg-secondary/50`

**Table columns** (per D-03, using TanStack Table `createColumnHelper<InboxItemResponse>()`):
1. **Task** — `item.activity.name` — text-sm font-semibold, flex-1
2. **Workflow** — `item.workflow.template_name` — text-sm, truncate with Tooltip on overflow
3. **Priority** — `<PriorityIcon priority={item.priority} />` — w-[60px] centered
4. **State** — `<WorkItemStateBadge state={item.state} />` — w-[100px]
5. **Created** — `new Date(item.created_at).toLocaleDateString()` — text-xs text-muted-foreground, w-[100px]

**Table rendering:**
- Use `useReactTable` with `getCoreRowModel()` (no client-side sorting — server handles it)
- Table header row: h-10, bg-secondary
- Table body rows: min-h-[48px], cursor-pointer, hover:bg-accent/50
- Selected row: `bg-accent` + 3px left border using `border-l-[3px] border-primary` (per UI-SPEC)
- Row onClick calls `onSelectItem(row.original.id)`

**Loading state:**
- When `isLoading` is true, render 5 Skeleton rows (each 48px, spanning full width) using the Skeleton component
- Each skeleton row: `<Skeleton className="h-[48px] w-full" />`

**Empty state:**
- When `items.length === 0` and not loading, render `<InboxEmptyState heading="No work items" body="You have no pending tasks. New items will appear here when workflows assign work to you." />`

**Pagination footer** (per UI-SPEC, 48px height):
- Centered row: "Page {currentPage} of {totalPages}" text
- Previous button (disabled when page === 1), Next button (disabled when page === totalPages)
- Use shadcn Button with variant="outline" size="sm"
- Gap of 8px between elements
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/inbox/InboxTable.tsx` exists
    - Contains `useReactTable` import from @tanstack/react-table
    - Contains 5 column definitions: activity.name, workflow.template_name, priority, state, created_at
    - Contains Select component for state filter with options: All, Available, Acquired, Delegated
    - Contains pagination with Previous/Next buttons and "Page" text
    - Contains Skeleton loading state (5 rows)
    - Contains InboxEmptyState for empty list
    - Selected row uses `border-l-[3px] border-primary`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>InboxTable renders a paginated, filterable work item table with row selection highlighting, loading skeletons, and empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Build InboxDetailPanel, CommentList, CommentCompose, and wire InboxPage</name>
  <files>frontend/src/components/inbox/InboxDetailPanel.tsx, frontend/src/components/inbox/CommentList.tsx, frontend/src/components/inbox/CommentCompose.tsx, frontend/src/pages/InboxPage.tsx</files>
  <read_first>
    - frontend/src/pages/InboxPage.tsx (placeholder to replace)
    - frontend/src/components/ui/card.tsx (for workflow context section)
    - frontend/src/components/ui/separator.tsx (for section divider)
    - frontend/src/components/ui/tabs.tsx (for My Inbox / Queues tabs)
    - frontend/src/components/ui/avatar.tsx (for comment author avatars)
    - frontend/src/components/ui/skeleton.tsx (for detail panel loading)
    - frontend/src/components/ui/textarea.tsx (for comment compose — from Plan 01)
    - frontend/src/components/ui/button.tsx (for Add Comment button)
    - frontend/src/api/inbox.ts (fetchInboxItem, fetchComments, addComment — from Plan 01)
    - frontend/src/stores/authStore.ts (userId for determining action visibility)
    - .planning/phases/13-inbox-work-items/13-UI-SPEC.md (detail panel layout, copywriting, interaction contracts)
    - frontend/src/components/inbox/InboxTable.tsx (just created in Task 1)
  </read_first>
  <action>
**Create `frontend/src/components/inbox/CommentList.tsx`:**

Props: `{ comments: CommentResponse[] }`

- If no comments: render `<p className="text-sm text-muted-foreground">No comments yet.</p>`
- For each comment, render:
  - Row: Avatar (initials from user_id first 2 chars, since we don't have username in comment response) + content + timestamp
  - Avatar: `<Avatar className="h-7 w-7"><AvatarFallback className="text-xs">{comment.user_id.slice(0, 2).toUpperCase()}</AvatarFallback></Avatar>`
  - Content: `text-sm` wrapped in a div
  - Timestamp: `text-xs text-muted-foreground` — format with `new Date(comment.created_at).toLocaleString()`
  - Gap between comments: `space-y-3`

**Create `frontend/src/components/inbox/CommentCompose.tsx`:**

Props: `{ workItemId: string }`

- Textarea with placeholder "Write a comment..." (from UI-SPEC copywriting)
- "Add Comment" Button below the textarea (variant="outline", size="sm")
- Use TanStack Query `useMutation` calling `addComment(workItemId, content)`
- On success: clear textarea, invalidate `["inbox", workItemId, "comments"]` query
- On error: show error in console (toast not yet set up — just log for now)
- Button disabled when textarea is empty or mutation is pending

**Create `frontend/src/components/inbox/InboxDetailPanel.tsx`:**

Props: `{ workItemId: string | null }`

**When workItemId is null** (no selection):
- Centered content: heading "Select a work item" (text-lg font-semibold) + body "Click a row in the table to view its details." (text-sm text-muted-foreground)

**When workItemId is set:**
- Use TanStack Query: `useQuery({ queryKey: ["inbox", workItemId], queryFn: () => fetchInboxItem(workItemId), enabled: !!workItemId })`
- Use TanStack Query for comments: `useQuery({ queryKey: ["inbox", workItemId, "comments"], queryFn: () => fetchComments(workItemId), enabled: !!workItemId })`

**Loading state:**
- Skeleton blocks: 1 heading line (h-6 w-48), card with 3 lines (h-4 w-full each), button row (3 h-9 w-20 blocks)

**Loaded layout** (per UI-SPEC detail panel internal layout, top to bottom):

1. **Header** (p-6): activity name (text-base font-semibold) + `<WorkItemStateBadge state={item.state} />` + `<PriorityIcon priority={item.priority} />` in a flex row with gap-2

2. **Workflow Context** (px-6 pb-4): Card containing:
   - Row: Label "Template" (text-xs text-muted-foreground) + value `item.workflow.template_name` (text-sm)
   - Row: Label "Workflow State" + `<WorkItemStateBadge state={item.workflow.state} />`
   - Row: Label "Assigned To" + performer_id or "Unassigned" (text-sm text-muted-foreground)
   - Row: Label "Due Date" + formatted due_date or "No due date" (text-sm text-muted-foreground)
   - Row: Label "Created" + formatted created_at
   - Each row: flex justify-between, py-1

3. **Instructions** (px-6 pb-4): If `item.instructions` or `item.activity.instructions` exists, render as text-sm block. Otherwise omit section.

4. **Actions placeholder** (px-6 pb-4): Render a div with id `inbox-actions-slot` and a comment `{/* Action buttons added by Plan 03 */}`. For now, just show the raw action buttons inline:
   - Show "Acquire" button (variant="default") if `item.state === "available"`
   - Show "Complete" button (variant="default"), "Reject" button (variant="outline" with destructive text), "Delegate" button (variant="outline") if `item.state === "acquired"` and `item.performer_id === userId` (get userId from `useAuthStore`)
   - Wire Acquire button to `useMutation` calling `acquireWorkItem(id)` — on success invalidate `["inbox"]` and `["inbox", id]` queries
   - Complete and Reject buttons are wired in Plan 03 with dialogs. For now, make them non-functional (just buttons that exist).
   - Delegate button also non-functional for now.
   - Button row: flex gap-2

5. **Separator**: `<Separator />`

6. **Comments** (p-6): Section heading "Comments" (text-base font-semibold mb-3) + `<CommentList comments={comments} />` + `<CommentCompose workItemId={workItemId} />` with mt-4 between them.

**Replace `frontend/src/pages/InboxPage.tsx`:**

Replace the placeholder with the full inbox page:

- Use shadcn Tabs with two TabsTrigger: "My Inbox" (value="inbox") and "Queues" (value="queues") — per D-02
- State management (all component-local, per UI-SPEC):
  - `selectedWorkItemId: string | null` — initially null
  - `activeTab: "inbox" | "queues"` — initially "inbox"
  - `stateFilter: string` — initially "" (All)
  - `currentPage: number` — initially 1
- TanStack Query for inbox list: `useQuery({ queryKey: ["inbox", { state: stateFilter, skip: (currentPage - 1) * 20, limit: 20 }], queryFn: () => fetchInboxItems({ state: stateFilter || undefined, skip: (currentPage - 1) * 20, limit: 20 }) })`
- When stateFilter changes: reset currentPage to 1, clear selectedWorkItemId
- When currentPage changes: clear selectedWorkItemId

**My Inbox tab content** — split-pane layout (per UI-SPEC):
- Container: `flex` with `h-[calc(100vh-theme(spacing.16))]` (subtract app shell header height)
- Left pane: `flex-1 min-w-[400px] flex flex-col overflow-hidden` — contains InboxTable
- Right pane: `w-[420px] border-l overflow-y-auto` — contains InboxDetailPanel
- No gap between panes (border serves as divider, per UI-SPEC)

**Queues tab content:**
- For now, render `<InboxEmptyState heading="No queues" body="Queue browsing will be available soon." />`
- Plan 03 replaces this placeholder.

**Responsive behavior** (per UI-SPEC):
- At < 1024px (lg breakpoint): hide detail panel, table takes full width. Clicking a row could navigate to detail but for simplicity in this plan, just hide the panel with `hidden lg:block` on the right pane.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/pages/InboxPage.tsx` exists and no longer contains "Coming Soon"
    - InboxPage contains Tabs with triggers "My Inbox" and "Queues"
    - InboxPage contains `useQuery` with queryKey including "inbox"
    - InboxPage contains split-pane layout with `min-w-[400px]` and `w-[420px]`
    - File `frontend/src/components/inbox/InboxDetailPanel.tsx` exists
    - InboxDetailPanel contains "Select a work item" text for empty state
    - InboxDetailPanel contains `fetchInboxItem` call
    - InboxDetailPanel contains `acquireWorkItem` mutation
    - InboxDetailPanel renders WorkItemStateBadge and PriorityIcon
    - InboxDetailPanel renders Separator between actions and comments
    - File `frontend/src/components/inbox/CommentList.tsx` exists with "No comments yet." text
    - File `frontend/src/components/inbox/CommentCompose.tsx` exists with "Write a comment..." placeholder and "Add Comment" button
    - CommentCompose uses useMutation with addComment
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>InboxPage renders split-pane layout with tabbed navigation, InboxTable with filter/pagination on the left, InboxDetailPanel with work item info, acquire button, and comments on the right. Queues tab shows placeholder.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- InboxPage no longer shows "Coming Soon"
- Split-pane layout visible: table left (flex-1), detail right (420px)
- Tabs render: "My Inbox" and "Queues"
- State filter Select renders with 4 options
- Table renders 5 columns
- Pagination renders at table footer
- Detail panel shows "Select a work item" when nothing selected
- Acquire button visible for available items
- Comments section renders with compose input
</verification>

<success_criteria>
- User can view pending work items in a paginated, filterable table (INB-01)
- User can click a row to see full details with comments (INB-02)
- User can add comments to work items
- Acquire action works (single click, no dialog)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-inbox-work-items/13-02-SUMMARY.md`
</output>
