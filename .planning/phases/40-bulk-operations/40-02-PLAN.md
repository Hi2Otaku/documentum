---
phase: 40-bulk-operations
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/api/bulk.ts
  - frontend/src/pages/BrowsePage.tsx
  - frontend/src/components/documents/BulkActionToolbar.tsx
  - frontend/src/components/documents/BulkJobDialog.tsx
  - frontend/src/pages/BulkJobsPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/AppShell.tsx
autonomous: true
requirements:
  - BULK-01
  - BULK-02
  - BULK-03
  - BULK-04

must_haves:
  truths:
    - "User can select multiple documents via checkboxes in the BrowsePage document table"
    - "Selected documents show a toolbar with Update, Delete, and Lifecycle actions"
    - "Clicking a bulk action opens a dialog, submits the API call, and shows a progress/status view"
    - "User can navigate to a Bulk Jobs page to see past job history with success/failure counts"
  artifacts:
    - path: "frontend/src/api/bulk.ts"
      provides: "API client functions for bulk endpoints"
      exports: ["bulkUpdate", "bulkDelete", "bulkLifecycle", "fetchBulkJobs", "fetchBulkJob"]
    - path: "frontend/src/components/documents/BulkActionToolbar.tsx"
      provides: "Toolbar shown when documents are selected"
      contains: "BulkActionToolbar"
    - path: "frontend/src/components/documents/BulkJobDialog.tsx"
      provides: "Dialog for configuring and tracking bulk operations"
      contains: "BulkJobDialog"
    - path: "frontend/src/pages/BulkJobsPage.tsx"
      provides: "Job history page listing past bulk operations"
      contains: "BulkJobsPage"
    - path: "frontend/src/pages/BrowsePage.tsx"
      provides: "Updated with checkbox selection and bulk toolbar"
      contains: "selectedDocIds"
  key_links:
    - from: "frontend/src/components/documents/BulkActionToolbar.tsx"
      to: "frontend/src/api/bulk.ts"
      via: "API call on action click"
      pattern: "bulk(Update|Delete|Lifecycle)"
    - from: "frontend/src/pages/BrowsePage.tsx"
      to: "frontend/src/components/documents/BulkActionToolbar.tsx"
      via: "rendered when selection > 0"
      pattern: "BulkActionToolbar"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/BulkJobsPage.tsx"
      via: "route definition"
      pattern: "/bulk-jobs"
---

<objective>
Add multi-select checkboxes to the BrowsePage document table, a bulk action toolbar, job progress dialog, and a job history page.

Purpose: Give users a visual way to select documents and apply bulk operations with live feedback (BULK-01 through BULK-04).
Output: Working frontend bulk operations flow from selection to job history.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-bulk-operations/40-CONTEXT.md
@.planning/phases/40-bulk-operations/40-01-SUMMARY.md

@frontend/src/pages/BrowsePage.tsx
@frontend/src/App.tsx
@frontend/src/api/documents.ts
@frontend/src/components/layout/AppShell.tsx

<interfaces>
<!-- Backend API contracts from Plan 40-01 -->

POST /api/v1/bulk/update
  Body: { document_ids: string[], metadata: Record<string, any> }
  Response: { data: BulkJobResponse }

POST /api/v1/bulk/delete
  Body: { document_ids: string[] }
  Response: { data: BulkJobResponse }

POST /api/v1/bulk/lifecycle
  Body: { document_ids: string[], target_state: "draft" | "review" | "approved" | "archived" }
  Response: { data: BulkJobResponse }

GET /api/v1/bulk/jobs?page=1&page_size=20
  Response: { data: BulkJobResponse[], meta: PaginationMeta }

GET /api/v1/bulk/jobs/{job_id}
  Response: { data: BulkJobResponse }

BulkJobResponse: {
  id: string, job_type: string, status: string,
  total_count: number, success_count: number, failure_count: number,
  results: Array<{document_id: string, status: string, error?: string}> | null,
  created_by: string, created_at: string, completed_at: string | null
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client and BulkActionToolbar with selection in BrowsePage</name>
  <read_first>
    - frontend/src/pages/BrowsePage.tsx
    - frontend/src/api/documents.ts
    - frontend/src/api/folders.ts
    - frontend/src/components/ui/checkbox.tsx (if exists, else note shadcn pattern)
    - frontend/src/components/ui/button.tsx
    - frontend/src/stores/authStore.ts
  </read_first>
  <files>
    frontend/src/api/bulk.ts,
    frontend/src/components/documents/BulkActionToolbar.tsx,
    frontend/src/pages/BrowsePage.tsx
  </files>
  <action>
    1. Create `frontend/src/api/bulk.ts`:
       - `const API = "/api/v1/bulk"` base URL
       - `bulkUpdate(documentIds: string[], metadata: Record<string, any>)` — POST to /update
       - `bulkDelete(documentIds: string[])` — POST to /delete
       - `bulkLifecycle(documentIds: string[], targetState: string)` — POST to /lifecycle
       - `fetchBulkJobs(page = 1, pageSize = 20)` — GET /jobs with query params
       - `fetchBulkJob(jobId: string)` — GET /jobs/{jobId}
       - All use fetch with Authorization header from localStorage token (follow same pattern as other api/*.ts files)
       - Export `bulkKeys` query key factory: `{ all: ["bulk-jobs"], list: (p) => [..., p], detail: (id) => [..., id] }`

    2. Create `frontend/src/components/documents/BulkActionToolbar.tsx`:
       - Props: `{ selectedCount: number, selectedIds: string[], onClear: () => void, onJobStarted: (jobId: string) => void }`
       - Horizontal bar with: "{N} selected" label, "Update Metadata" button, "Change State" button (dropdown with Draft/Review/Approved/Archived), "Delete" button (destructive variant), "Clear" button
       - "Update Metadata" opens a simple JSON textarea dialog (Dialog from shadcn) where user pastes metadata JSON, confirms, calls `bulkUpdate` API
       - "Change State" is a DropdownMenu with 4 lifecycle states; clicking one calls `bulkLifecycle` directly
       - "Delete" shows AlertDialog with "Delete {N} documents?" confirmation, calls `bulkDelete`
       - On API success, calls `onJobStarted(response.data.id)` and `onClear()`
       - Use toast (from sonner if available, else window.alert) for error feedback
       - Import icons from lucide-react: Trash2, Edit, RefreshCw, X

    3. Update `frontend/src/pages/BrowsePage.tsx`:
       - Add state: `const [selectedDocIds, setSelectedDocIds] = useState<Set<string>>(new Set())`
       - Add a Checkbox import (from shadcn ui/checkbox or use native `<input type="checkbox">`)
       - In the document table header row, add a "select all" checkbox that toggles all visible document IDs
       - In each document table row, add a checkbox bound to selectedDocIds set
       - When `selectedDocIds.size > 0`, render `<BulkActionToolbar>` above the table (between breadcrumb and table)
       - Clear selection when folder changes (add `setSelectedDocIds(new Set())` to handleFolderSelect and handleSmartFolderSelect)
       - `onJobStarted` callback: store the job ID in state for the BulkJobDialog (Task 2)
       - Add state: `const [activeJobId, setActiveJobId] = useState<string | null>(null)`
  </action>
  <acceptance_criteria>
    - grep -r "bulkUpdate" frontend/src/api/bulk.ts returns match
    - grep -r "bulkDelete" frontend/src/api/bulk.ts returns match
    - grep -r "bulkLifecycle" frontend/src/api/bulk.ts returns match
    - grep -r "fetchBulkJobs" frontend/src/api/bulk.ts returns match
    - grep -r "BulkActionToolbar" frontend/src/components/documents/BulkActionToolbar.tsx returns match
    - grep -r "selectedDocIds" frontend/src/pages/BrowsePage.tsx returns match
    - grep -r "BulkActionToolbar" frontend/src/pages/BrowsePage.tsx returns match
    - grep -r "checkbox" frontend/src/pages/BrowsePage.tsx -i returns match
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BrowsePage has checkbox selection on document rows, select-all in header, and a BulkActionToolbar appears when documents are selected with working API calls</done>
</task>

<task type="auto">
  <name>Task 2: BulkJobDialog, BulkJobsPage, and routing</name>
  <read_first>
    - frontend/src/api/bulk.ts
    - frontend/src/pages/BrowsePage.tsx
    - frontend/src/App.tsx
    - frontend/src/components/layout/AppShell.tsx
    - frontend/src/components/ui/dialog.tsx (if exists)
    - frontend/src/components/ui/badge.tsx
  </read_first>
  <files>
    frontend/src/components/documents/BulkJobDialog.tsx,
    frontend/src/pages/BulkJobsPage.tsx,
    frontend/src/pages/BrowsePage.tsx,
    frontend/src/App.tsx,
    frontend/src/components/layout/AppShell.tsx
  </files>
  <action>
    1. Create `frontend/src/components/documents/BulkJobDialog.tsx`:
       - Props: `{ jobId: string | null, open: boolean, onClose: () => void }`
       - Uses `useQuery` with `fetchBulkJob(jobId)` with refetchInterval=2000 when status is "pending" or "running" (stop polling when "completed" or "failed")
       - Shows Dialog with:
         - Title: "Bulk {job_type} Job"
         - Progress: "{success_count + failure_count} / {total_count}" with a visual progress bar (div with width percentage and bg-primary)
         - Status badge: pending=yellow, running=blue, completed=green, failed=red (use Badge component)
         - When completed: show "Succeeded: {success_count}, Failed: {failure_count}"
         - If failure_count > 0 and results available: expandable list of failed items showing document_id and error message
         - Close button

    2. Create `frontend/src/pages/BulkJobsPage.tsx`:
       - Uses `useQuery` with `fetchBulkJobs(page)` for paginated list
       - Table with columns: Job Type (badge), Status (colored badge), Total, Succeeded, Failed, Created (relative time), Completed (relative time or "-")
       - Clicking a row opens BulkJobDialog for that job
       - Pagination controls at bottom (same pattern as other pages — Previous/Next buttons with page number)
       - Page title: "Bulk Operations" with Layers icon from lucide-react

    3. Update `frontend/src/pages/BrowsePage.tsx`:
       - Import and render `<BulkJobDialog jobId={activeJobId} open={!!activeJobId} onClose={() => setActiveJobId(null)} />`
       - This shows the progress dialog immediately after starting a bulk operation

    4. Update `frontend/src/App.tsx`:
       - Import BulkJobsPage
       - Add route `<Route path="/bulk-jobs" element={<BulkJobsPage />} />` inside the protected routes (not admin-only — any authenticated user)

    5. Update `frontend/src/components/layout/AppShell.tsx`:
       - Add a "Bulk Jobs" nav item with Layers icon linking to "/bulk-jobs"
       - Place it after the existing nav items (near Documents/Browse)
  </action>
  <acceptance_criteria>
    - grep -r "BulkJobDialog" frontend/src/components/documents/BulkJobDialog.tsx returns match
    - grep -r "BulkJobsPage" frontend/src/pages/BulkJobsPage.tsx returns match
    - grep -r "refetchInterval" frontend/src/components/documents/BulkJobDialog.tsx returns match
    - grep -r "bulk-jobs" frontend/src/App.tsx returns match
    - grep -r "BulkJobDialog" frontend/src/pages/BrowsePage.tsx returns match
    - grep -r "bulk-jobs" frontend/src/components/layout/AppShell.tsx -i returns match
    - grep -r "fetchBulkJobs" frontend/src/pages/BulkJobsPage.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BulkJobDialog polls for progress and shows completion status; BulkJobsPage shows job history table; route and nav wired; BrowsePage opens dialog after starting a job</done>
</task>

</tasks>

<verification>
- BrowsePage document table has checkboxes for multi-select
- Selecting documents shows BulkActionToolbar with Update/Delete/Lifecycle actions
- Starting a bulk operation opens BulkJobDialog with live progress polling
- /bulk-jobs route shows paginated job history
- AppShell nav includes "Bulk Jobs" link
- TypeScript compiles without errors
</verification>

<success_criteria>
- User can check multiple documents in browse view and see bulk toolbar (BULK-01, BULK-02)
- Bulk toolbar offers metadata update, lifecycle change, and delete with confirmation (BULK-01, BULK-02)
- After submitting, a dialog polls job status showing progress percentage (BULK-03)
- Job history page at /bulk-jobs shows past operations with counts (BULK-04)
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/40-bulk-operations/40-02-SUMMARY.md`
</output>
