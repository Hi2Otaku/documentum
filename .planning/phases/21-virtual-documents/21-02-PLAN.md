---
phase: 21-virtual-documents
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - frontend/src/api/virtualDocuments.ts
  - frontend/src/components/virtual-documents/VirtualDocumentChildrenList.tsx
  - frontend/src/components/virtual-documents/AddChildDialog.tsx
  - frontend/src/components/virtual-documents/CreateVirtualDocumentDialog.tsx
  - frontend/src/components/virtual-documents/VirtualDocumentDetailPanel.tsx
  - frontend/src/pages/DocumentsPage.tsx
  - frontend/src/components/documents/DocumentTable.tsx
autonomous: true
requirements: [VDOC-01, VDOC-02, VDOC-04]

must_haves:
  truths:
    - "User can create a virtual document from the documents page"
    - "User can view a virtual document and see its ordered children list"
    - "User can add existing documents as children to a virtual document"
    - "User can reorder children using move-up/move-down buttons"
    - "User can remove children from a virtual document"
    - "User can trigger PDF merge and download the combined file"
  artifacts:
    - path: "frontend/src/api/virtualDocuments.ts"
      provides: "API client for virtual document endpoints"
      exports: ["createVirtualDocument", "fetchVirtualDocument", "addChild", "removeChild", "reorderChildren", "mergePdfUrl"]
    - path: "frontend/src/components/virtual-documents/VirtualDocumentChildrenList.tsx"
      provides: "Children list with reorder and remove controls"
      contains: "VirtualDocumentChildrenList"
    - path: "frontend/src/components/virtual-documents/AddChildDialog.tsx"
      provides: "Dialog to search and select a document to add as child"
      contains: "AddChildDialog"
    - path: "frontend/src/components/virtual-documents/VirtualDocumentDetailPanel.tsx"
      provides: "Detail panel showing virtual doc info, children, and merge button"
      contains: "VirtualDocumentDetailPanel"
  key_links:
    - from: "frontend/src/api/virtualDocuments.ts"
      to: "/api/v1/virtual-documents"
      via: "fetch calls"
      pattern: "virtual-documents"
    - from: "frontend/src/components/virtual-documents/VirtualDocumentDetailPanel.tsx"
      to: "frontend/src/api/virtualDocuments.ts"
      via: "useQuery and useMutation"
      pattern: "useQuery|useMutation"
    - from: "frontend/src/pages/DocumentsPage.tsx"
      to: "frontend/src/components/virtual-documents/CreateVirtualDocumentDialog.tsx"
      via: "button click opens dialog"
      pattern: "CreateVirtualDocumentDialog"
---

<objective>
Build the frontend UI for virtual documents: API client, children list with reorder/remove controls, add-child dialog, create dialog, detail panel with PDF merge download, and integration into the existing documents page.

Purpose: Users can interact with virtual documents through the web UI -- creating them, managing children, reordering, and generating merged PDFs.
Output: Complete frontend for all four VDOC requirements, integrated into the existing DocumentsPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-virtual-documents/21-RESEARCH.md
@.planning/phases/21-virtual-documents/21-01-SUMMARY.md

@frontend/src/api/documents.ts
@frontend/src/pages/DocumentsPage.tsx
@frontend/src/components/documents/DocumentDetailPanel.tsx
@frontend/src/components/documents/DocumentTable.tsx
@frontend/src/components/documents/DocumentActions.tsx
@frontend/src/App.tsx

<interfaces>
From Plan 01 API (src/app/routers/virtual_documents.py):
```
POST   /api/v1/virtual-documents/                          -> VirtualDocumentResponse (201)
GET    /api/v1/virtual-documents/?page=1&page_size=20      -> list[VirtualDocumentResponse] + PaginationMeta
GET    /api/v1/virtual-documents/{id}                      -> VirtualDocumentResponse (includes children)
POST   /api/v1/virtual-documents/{id}/children             -> VirtualDocumentChildResponse (201)
DELETE /api/v1/virtual-documents/{id}/children/{child_id}  -> success message
PUT    /api/v1/virtual-documents/{id}/children/reorder     -> list[VirtualDocumentChildResponse]
POST   /api/v1/virtual-documents/{id}/merge-pdf            -> PDF binary (application/pdf)
```

VirtualDocumentResponse shape:
```typescript
interface VirtualDocumentResponse {
  id: string;
  document_id: string;
  description: string | null;
  document_title: string | null;
  created_at: string;
  updated_at: string;
  children: VirtualDocumentChildResponse[];
}

interface VirtualDocumentChildResponse {
  id: string;
  virtual_document_id: string;
  child_document_id: string;
  order_index: number;
  child_title: string | null;
  child_filename: string | null;
  created_at: string;
}
```

From frontend/src/api/documents.ts:
```typescript
function authHeaders(): HeadersInit
async function apiFetch<T>(url: string): Promise<T>
async function apiMutate<T>(url, method, body?): Promise<T>
function buildUrl(base, params): string
export interface DocumentResponse { id, title, filename, ... }
export async function fetchDocuments(params): Promise<PaginatedDocumentsResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Virtual Document API Client and Create Dialog</name>
  <files>
    frontend/src/api/virtualDocuments.ts,
    frontend/src/components/virtual-documents/CreateVirtualDocumentDialog.tsx
  </files>
  <read_first>
    frontend/src/api/documents.ts,
    frontend/src/pages/DocumentsPage.tsx,
    frontend/src/components/documents/DocumentDetailPanel.tsx
  </read_first>
  <action>
    1. Create `frontend/src/api/virtualDocuments.ts` following the exact pattern from documents.ts (authHeaders, apiFetch, apiMutate, buildUrl -- copy the helper functions or import if exported):

    **Types:**
    ```typescript
    export interface VirtualDocumentChildResponse {
      id: string;
      virtual_document_id: string;
      child_document_id: string;
      order_index: number;
      child_title: string | null;
      child_filename: string | null;
      created_at: string;
    }

    export interface VirtualDocumentResponse {
      id: string;
      document_id: string;
      description: string | null;
      document_title: string | null;
      created_at: string;
      updated_at: string;
      children: VirtualDocumentChildResponse[];
    }

    export interface PaginatedVirtualDocumentsResponse {
      data: VirtualDocumentResponse[];
      meta: { page: number; page_size: number; total_count: number; total_pages: number };
    }
    ```

    **API functions:**
    - `createVirtualDocument(title: string, description?: string)` -- POST /api/v1/virtual-documents/
    - `fetchVirtualDocuments(params: {page?, page_size?})` -- GET /api/v1/virtual-documents/
    - `fetchVirtualDocument(id: string)` -- GET /api/v1/virtual-documents/{id}
    - `addChild(virtualDocId: string, childDocumentId: string)` -- POST .../children
    - `removeChild(virtualDocId: string, childId: string)` -- DELETE .../children/{childId}
    - `reorderChildren(virtualDocId: string, childIds: string[])` -- PUT .../children/reorder
    - `mergePdfUrl(virtualDocId: string): string` -- returns the URL for POST merge-pdf (for download link)
    - `downloadMergedPdf(virtualDocId: string)` -- POST .../merge-pdf, returns blob for download

    For `downloadMergedPdf`, use fetch with auth headers, get response as blob, create object URL and trigger download via temporary anchor element.

    2. Create `frontend/src/components/virtual-documents/CreateVirtualDocumentDialog.tsx`:
    - shadcn Dialog component with form fields: title (required input), description (optional textarea)
    - useMutation calling createVirtualDocument
    - On success: invalidate "virtual-documents" query, close dialog, show success toast (if sonner/toast is available, otherwise just close)
    - Use shadcn Button, Input, Label, Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger components
    - Export as named export
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -20 || echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - virtualDocuments.ts exports all 8 API functions with correct types
    - CreateVirtualDocumentDialog renders a form dialog with title and description fields
    - TypeScript compiles without errors in new files
  </acceptance_criteria>
  <done>API client and create dialog are complete. All virtual document endpoints have frontend client functions.</done>
</task>

<task type="auto">
  <name>Task 2: Virtual Document Detail Panel, Children List, Add Child Dialog, and Page Integration</name>
  <files>
    frontend/src/components/virtual-documents/VirtualDocumentChildrenList.tsx,
    frontend/src/components/virtual-documents/AddChildDialog.tsx,
    frontend/src/components/virtual-documents/VirtualDocumentDetailPanel.tsx,
    frontend/src/pages/DocumentsPage.tsx,
    frontend/src/components/documents/DocumentTable.tsx
  </files>
  <read_first>
    frontend/src/api/virtualDocuments.ts,
    frontend/src/api/documents.ts,
    frontend/src/pages/DocumentsPage.tsx,
    frontend/src/components/documents/DocumentDetailPanel.tsx,
    frontend/src/components/documents/DocumentTable.tsx,
    frontend/src/components/documents/DocumentActions.tsx,
    frontend/src/components/virtual-documents/CreateVirtualDocumentDialog.tsx
  </read_first>
  <action>
    1. Create `frontend/src/components/virtual-documents/VirtualDocumentChildrenList.tsx`:
    - Props: `children: VirtualDocumentChildResponse[]`, `virtualDocId: string`, `onReorder: (childIds: string[]) => void`, `onRemove: (childId: string) => void`
    - Renders an ordered list of children showing: order number, child_title, child_filename
    - Each row has: move-up button (ChevronUp icon, disabled if first), move-down button (ChevronDown icon, disabled if last), remove button (X/Trash icon)
    - Move up/down: rearranges the childIds array and calls onReorder with the new order
    - Remove: calls onRemove with the child id
    - Empty state: "No children added yet. Add documents to this virtual document."
    - Use shadcn Button (variant="ghost", size="icon") for action buttons
    - Use Tailwind for layout: each row is a flex row with items-center, gap-2, border-b

    2. Create `frontend/src/components/virtual-documents/AddChildDialog.tsx`:
    - Props: `virtualDocId: string`, `existingChildIds: string[]` (to filter out already-added docs), `onSuccess: () => void`
    - Dialog with a search input that filters documents by title
    - Uses useQuery to fetch documents via fetchDocuments with title filter
    - Displays results in a simple list with document title, filename
    - Each result has an "Add" button that calls addChild mutation
    - Already-added documents are shown grayed out with "Already added" label
    - On successful add: invalidate virtual-documents query, call onSuccess

    3. Create `frontend/src/components/virtual-documents/VirtualDocumentDetailPanel.tsx`:
    - Props: `virtualDocumentId: string`
    - Uses useQuery to fetch the virtual document via fetchVirtualDocument
    - Shows: document_title, description, created_at, child count
    - Renders VirtualDocumentChildrenList with reorder and remove handlers (using useMutation for reorderChildren and removeChild)
    - "Add Document" button opens AddChildDialog
    - "Generate Merged PDF" button at the bottom (disabled if no children):
      - On click, calls downloadMergedPdf(virtualDocId) -- shows loading state during download
      - Creates a blob URL and triggers browser download
    - Uses Card, CardHeader, CardContent from shadcn for layout

    4. Update `frontend/src/pages/DocumentsPage.tsx`:
    - Add a "Create Virtual Document" button next to the existing document upload area (or in a toolbar section)
    - Clicking it opens CreateVirtualDocumentDialog
    - Add a tab or toggle to switch between "Documents" and "Virtual Documents" views
    - Use a simple state variable `viewMode: "documents" | "virtual"` with two tab buttons
    - When viewMode is "virtual": show a list of virtual documents (using fetchVirtualDocuments + useQuery) in a simple table (title, children count, created_at)
    - When a virtual document is selected, show VirtualDocumentDetailPanel in the right panel instead of DocumentDetailPanel
    - Keep the existing document functionality unchanged when viewMode is "documents"

    5. Update `frontend/src/components/documents/DocumentTable.tsx` (if needed):
    - Add a visual indicator (badge or icon) if a document is also a virtual document (this may require an `is_virtual` flag in the DocumentResponse -- if not available from the backend, skip this change and rely on the separate virtual documents tab)
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -30 || echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - VirtualDocumentChildrenList renders children with move-up/down/remove buttons
    - AddChildDialog searches documents and adds them as children
    - VirtualDocumentDetailPanel shows full virtual doc with children management and merge PDF button
    - DocumentsPage has toggle between Documents and Virtual Documents views
    - All components use shadcn/ui and Tailwind following existing project patterns
    - Merged PDF download triggers browser file save dialog
  </acceptance_criteria>
  <done>Complete frontend for virtual documents integrated into the existing documents page. Users can create virtual docs, manage children, reorder them, and download merged PDFs.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit`
2. API client exports: `grep "export" frontend/src/api/virtualDocuments.ts`
3. Components exist: `ls frontend/src/components/virtual-documents/`
4. Page integration: `grep "virtual" frontend/src/pages/DocumentsPage.tsx`
5. All 4 new components created: VirtualDocumentChildrenList, AddChildDialog, CreateVirtualDocumentDialog, VirtualDocumentDetailPanel
</verification>

<success_criteria>
- User can create a virtual document from the documents page
- User can add, reorder (move up/down), and remove children
- User can download a merged PDF from a virtual document
- UI integrates naturally with existing documents page via tab toggle
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/21-virtual-documents/21-02-SUMMARY.md`
</output>
