---
phase: 36-identity-sso
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - frontend/src/pages/SSOSettingsPage.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/api/identity.ts
  - frontend/src/App.tsx
  - frontend/src/components/layout/SidebarNav.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05

must_haves:
  truths:
    - "Admin can configure LDAP/SAML/OIDC providers from an admin settings page"
    - "Login page shows SSO buttons for enabled providers alongside local login form"
    - "User clicking SSO button is redirected to IdP and lands back authenticated"
    - "Local login form continues to work unchanged"
  artifacts:
    - path: "frontend/src/pages/SSOSettingsPage.tsx"
      provides: "Admin page for managing identity providers"
      min_lines: 100
    - path: "frontend/src/api/identity.ts"
      provides: "API client for identity provider endpoints"
      exports: ["fetchProviders", "createProvider", "updateProvider", "deleteProvider", "testLdapConnection", "syncLdap"]
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Updated login with SSO buttons and sso_token handling"
  key_links:
    - from: "frontend/src/pages/SSOSettingsPage.tsx"
      to: "frontend/src/api/identity.ts"
      via: "CRUD operations on identity providers"
      pattern: "fetchProviders|createProvider"
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "/api/v1/identity/sso"
      via: "SSO login initiation redirect"
      pattern: "identity/sso"
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "frontend/src/stores/authStore.ts"
      via: "stores sso_token from URL params"
      pattern: "sso_token|setToken"
---

<objective>
Build the admin SSO settings page and update the login page with SSO provider buttons.

Purpose: Give admins a UI to configure identity providers and give users SSO login options.
Output: SSOSettingsPage for admin, updated LoginPage with SSO buttons, identity API client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-identity-sso/36-CONTEXT.md
@.planning/phases/36-identity-sso/36-02-SUMMARY.md

@frontend/src/pages/LoginPage.tsx
@frontend/src/stores/authStore.ts
@frontend/src/api/auth.ts
@frontend/src/App.tsx
@frontend/src/components/layout/SidebarNav.tsx

<interfaces>
<!-- Backend endpoints from Plan 02 -->

From src/app/routers/identity.py:
```
POST   /api/v1/identity/providers          — Create provider (admin)
GET    /api/v1/identity/providers          — List providers (admin)
GET    /api/v1/identity/providers/{id}     — Get provider (admin)
PUT    /api/v1/identity/providers/{id}     — Update provider (admin)
DELETE /api/v1/identity/providers/{id}     — Delete provider (admin)
POST   /api/v1/identity/providers/{id}/ldap/test  — Test LDAP (admin)
POST   /api/v1/identity/providers/{id}/ldap/sync  — Sync LDAP (admin)
GET    /api/v1/identity/sso/{id}/login     — Initiate SSO (public)
POST   /api/v1/identity/sso/saml/acs       — SAML callback (public)
GET    /api/v1/identity/sso/oidc/callback  — OIDC callback (public)
```

Response shapes:
```typescript
interface IdentityProvider {
  id: string;
  name: string;
  provider_type: "ldap" | "saml" | "oidc";
  config: Record<string, unknown>;  // sensitive fields masked
  is_enabled: boolean;
  created_at: string;
}

interface SSOLoginResponse {
  redirect_url: string;
  state?: string;
  code_verifier?: string;
}
```

From frontend/src/stores/authStore.ts:
```typescript
setToken: (token: string) => void;
loadProfile: () => Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identity API client + SSO Settings admin page</name>
  <read_first>
    - frontend/src/api/auth.ts (API client pattern)
    - frontend/src/pages/RetentionPage.tsx (admin CRUD page pattern)
    - frontend/src/pages/QueueAdminPage.tsx (alternative admin page pattern)
    - frontend/src/components/layout/SidebarNav.tsx (nav items)
    - frontend/src/App.tsx (routing)
  </read_first>
  <files>
    frontend/src/api/identity.ts
    frontend/src/pages/SSOSettingsPage.tsx
    frontend/src/App.tsx
    frontend/src/components/layout/SidebarNav.tsx
  </files>
  <action>
    1. Create frontend/src/api/identity.ts:
       - fetchProviders(token): GET /api/v1/identity/providers -> IdentityProvider[]
       - fetchProvider(token, id): GET /api/v1/identity/providers/{id}
       - createProvider(token, data): POST /api/v1/identity/providers
       - updateProvider(token, id, data): PUT /api/v1/identity/providers/{id}
       - deleteProvider(token, id): DELETE /api/v1/identity/providers/{id}
       - testLdapConnection(token, id): POST /api/v1/identity/providers/{id}/ldap/test
       - syncLdap(token, id): POST /api/v1/identity/providers/{id}/ldap/sync
       - fetchEnabledProviders(): GET /api/v1/identity/providers/public (no auth — for login page). NOTE: Backend needs a public endpoint that returns only enabled providers with just id, name, provider_type. If Plan 02 didn't add this, add it as a simple unauthenticated endpoint in the identity router that returns minimal provider info.
       - All functions follow existing pattern: use fetch with Authorization Bearer header, unwrap EnvelopeResponse

    2. Create frontend/src/pages/SSOSettingsPage.tsx:
       - Admin page following existing admin page patterns (RetentionPage, QueueAdminPage)
       - List all identity providers in a table: name, type (badge), enabled status (toggle), actions
       - "Add Provider" button opens a dialog/form with:
         * Name field
         * Type selector (LDAP / SAML / OIDC) — changing type shows different config fields
         * LDAP config fields: server_url, bind_dn, bind_password, base_dn, user_search_filter, group_search_filter, sync_interval_minutes, group_mapping (key-value pairs editor)
         * SAML config fields: idp_metadata_url, idp_entity_id, sp_entity_id, sp_acs_url, attribute_mapping
         * OIDC config fields: issuer_url, client_id, client_secret, redirect_uri, scopes (comma-separated), attribute_mapping
       - Edit button: opens same form pre-filled (password fields show placeholder, not actual value)
       - Delete button: confirmation dialog then soft-delete
       - For LDAP providers: "Test Connection" button (calls testLdapConnection, shows success/error toast) and "Sync Now" button (calls syncLdap, shows result toast)
       - Enable/disable toggle per provider
       - Use shadcn components: Card, Table, Dialog, Input, Select, Switch, Button, Badge
       - Use sonner toast for success/error feedback

    3. Add route to frontend/src/App.tsx:
       - Inside AdminRoute: <Route path="/admin/sso" element={<SSOSettingsPage />} />

    4. Add nav item to SidebarNav.tsx:
       - Under the admin section, add "SSO Settings" with a Shield icon (from lucide-react), path "/admin/sso"
       - Only visible to admin users (same pattern as other admin nav items)
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && npx --prefix frontend tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/identity.ts exports fetchProviders, createProvider, updateProvider, deleteProvider, testLdapConnection, syncLdap
    - SSOSettingsPage.tsx renders provider list with add/edit/delete functionality
    - Route /admin/sso exists in App.tsx under AdminRoute
    - "SSO Settings" nav item appears in SidebarNav.tsx for admins
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Admin SSO settings page with full provider CRUD, LDAP test/sync, and nav integration complete.</done>
</task>

<task type="auto">
  <name>Task 2: Update LoginPage with SSO buttons + sso_token handling</name>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (current login page)
    - frontend/src/stores/authStore.ts (token storage)
    - frontend/src/api/identity.ts (just created — fetchEnabledProviders)
  </read_first>
  <files>
    frontend/src/pages/LoginPage.tsx
  </files>
  <action>
    1. Update LoginPage.tsx to handle SSO token from URL:
       - On mount, check URL search params for "sso_token"
       - If sso_token present: call setToken(sso_token), loadProfile(), navigate to /inbox, clear URL params
       - This handles the redirect back from SSO provider

    2. Fetch enabled SSO providers:
       - On mount (if no sso_token), call fetchEnabledProviders() (unauthenticated endpoint)
       - Store in local state: providers list
       - If fetch fails or returns empty, show only local login (graceful degradation)

    3. Render SSO buttons below the local login form:
       - Separator: "Or sign in with" divider line (only shown if providers exist)
       - For each enabled provider, render a button:
         * SAML providers: button text "Sign in with {provider.name}", click redirects to GET /api/v1/identity/sso/{id}/login which returns redirect_url, then window.location.href = redirect_url
         * OIDC providers: same pattern — button text "Sign in with {provider.name}", click redirects to initiate endpoint, then window.location.href = redirect_url. Store code_verifier in sessionStorage for PKCE.
         * LDAP providers: Do NOT show a button (LDAP syncs users, they log in with local credentials after sync)
       - Button styling: outline variant, full width, with provider type icon (Shield for SAML, Key for OIDC from lucide-react)

    4. CRITICAL: The existing local login form (username + password + submit) must remain EXACTLY as-is. SSO buttons are ADDED below, not replacing anything. If no providers are enabled, the page looks identical to before.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && npx --prefix frontend tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - LoginPage.tsx handles sso_token URL parameter by storing token and redirecting
    - LoginPage.tsx fetches enabled providers on mount
    - SSO buttons rendered for SAML and OIDC providers (not LDAP)
    - Local login form (username/password/submit) unchanged
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Login page shows SSO buttons for enabled providers. SSO redirect flow completes by landing back on login page with sso_token that gets stored. Local login form preserved.</done>
</task>

</tasks>

<verification>
1. Admin navigates to /admin/sso and sees empty provider list
2. Admin clicks "Add Provider", selects SAML type, fills config, saves — provider appears in list
3. Admin can toggle provider enabled/disabled
4. Login page shows "Sign in with {name}" button for enabled SAML/OIDC providers
5. Login page still shows username/password form and local login works
6. After SSO redirect, login page picks up sso_token and authenticates
</verification>

<success_criteria>
- Admin SSO settings page functional with CRUD for all provider types
- LDAP providers show Test Connection and Sync Now buttons
- Login page displays SSO buttons for enabled SAML/OIDC providers
- Local login form completely preserved
- SSO token redirect flow handled on login page
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-identity-sso/36-03-SUMMARY.md`
</output>
