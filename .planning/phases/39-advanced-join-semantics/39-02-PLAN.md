---
phase: 39-advanced-join-semantics
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - frontend/src/components/designer/PropertiesPanel.tsx
  - frontend/src/types/workflow.ts
autonomous: false
requirements:
  - JOIN-01
  - JOIN-02
  - JOIN-03
must_haves:
  truths:
    - "Designer can select N-of-M join type and set threshold value"
    - "Designer can select cancelling join type"
    - "Designer can select timeout join type and set timeout duration"
    - "Threshold and timeout fields only appear when relevant join type is selected"
  artifacts:
    - path: "frontend/src/components/designer/PropertiesPanel.tsx"
      provides: "Join type dropdown with 5 options and conditional threshold/timeout inputs"
      contains: "n_of_m_join"
  key_links:
    - from: "frontend/src/components/designer/PropertiesPanel.tsx"
      to: "backend API"
      via: "triggerType, joinThreshold, joinTimeoutHours in node data saved to template"
      pattern: "joinThreshold|joinTimeoutHours"
---

<objective>
Add advanced join configuration UI to the workflow designer's properties panel.

Purpose: Template designers need to configure N-of-M, cancelling, and timeout joins visually in the drag-and-drop designer.
Output: Extended PropertiesPanel with join type dropdown (5 options) and conditional threshold/timeout input fields.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-advanced-join-semantics/39-CONTEXT.md
@.planning/phases/39-advanced-join-semantics/39-01-SUMMARY.md

<interfaces>
<!-- Current PropertiesPanel trigger type section (lines 235-254) -->
<!-- Shows dropdown with and_join/or_join when incomingEdgeCount >= 2 -->
<!-- Uses data.triggerType and updateNodeData(nodeId, { triggerType: value }) -->

<!-- Backend TriggerType values after Plan 01: -->
<!-- and_join, or_join, n_of_m_join, cancelling_join, timeout_join -->

<!-- ActivityTemplate new fields after Plan 01: -->
<!-- join_threshold: int | null (N in N-of-M) -->
<!-- join_timeout_hours: float | null (timeout duration) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PropertiesPanel with advanced join controls</name>
  <files>
    frontend/src/components/designer/PropertiesPanel.tsx,
    frontend/src/types/workflow.ts
  </files>
  <read_first>
    frontend/src/components/designer/PropertiesPanel.tsx,
    frontend/src/types/workflow.ts
  </read_first>
  <action>
1. In frontend/src/types/workflow.ts (or wherever the node data type is defined):
   - Ensure the activity node data type includes: triggerType, joinThreshold (number | null), joinTimeoutHours (number | null)
   - If there is a type for TriggerType, extend it with the 3 new values

2. In frontend/src/components/designer/PropertiesPanel.tsx, find the trigger type section (around line 235-254):
   - Replace the 2-option dropdown with a 5-option dropdown:
     <option value="and_join">AND-join (all branches)</option>
     <option value="or_join">OR-join (any branch)</option>
     <option value="n_of_m_join">N-of-M join (threshold)</option>
     <option value="cancelling_join">Cancelling join (threshold + cancel rest)</option>
     <option value="timeout_join">Timeout join (all or timeout)</option>

   - Update the onChange type assertion to include all 5 values

3. Add conditional fields below the trigger type dropdown:
   - When triggerType is "n_of_m_join" or "cancelling_join", show a "Join Threshold" number input:
     <label>Join Threshold (N of M)</label>
     <input type="number" min={1} max={incomingEdgeCount} value={data.joinThreshold ?? 1}
       onChange={e => updateNodeData(nodeId, { joinThreshold: parseInt(e.target.value) || 1 })} />
     <p className="text-xs text-muted-foreground mt-1">Fires when {N} of {incomingEdgeCount} branches complete</p>

   - When triggerType is "timeout_join", show a "Timeout Duration" number input:
     <label>Timeout (hours)</label>
     <input type="number" min={0.1} step={0.1} value={data.joinTimeoutHours ?? 1}
       onChange={e => updateNodeData(nodeId, { joinTimeoutHours: parseFloat(e.target.value) || 1 })} />
     <p className="text-xs text-muted-foreground mt-1">Join fires after this duration even if not all branches complete</p>

   - When triggerType is "cancelling_join", also show a note:
     <p className="text-xs text-amber-600 mt-1">Remaining incomplete branches will be cancelled when threshold is met</p>

4. Style all new inputs consistently with existing PropertiesPanel inputs (same className patterns: "mt-1 w-full rounded border px-3 py-2 text-sm").

5. When switching FROM n_of_m/cancelling/timeout TO and_join/or_join, clear the joinThreshold and joinTimeoutHours values by calling updateNodeData(nodeId, { joinThreshold: null, joinTimeoutHours: null }).
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PropertiesPanel.tsx contains "n_of_m_join" option value
    - PropertiesPanel.tsx contains "cancelling_join" option value
    - PropertiesPanel.tsx contains "timeout_join" option value
    - PropertiesPanel.tsx contains "joinThreshold" in an input onChange handler
    - PropertiesPanel.tsx contains "joinTimeoutHours" in an input onChange handler
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Designer shows 5 join types with conditional threshold/timeout inputs, TypeScript compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify advanced join UI in designer</name>
  <files>frontend/src/components/designer/PropertiesPanel.tsx</files>
  <action>Human verification of the advanced join UI. No code changes -- visual inspection only.</action>
  <what-built>Advanced join configuration in the workflow designer properties panel: 5 join types (AND, OR, N-of-M, Cancelling, Timeout) with conditional threshold and timeout inputs that appear based on selected join type.</what-built>
  <how-to-verify>
    1. Start the frontend dev server: cd frontend && npm run dev
    2. Open the workflow designer and create a template with 3+ parallel branches converging on one activity
    3. Select the convergence activity node
    4. Verify the Trigger Type dropdown shows 5 options
    5. Select "N-of-M join" -- verify threshold input appears with max = incoming edge count
    6. Select "Cancelling join" -- verify threshold input + amber warning text appears
    7. Select "Timeout join" -- verify timeout hours input appears
    8. Select "AND-join" -- verify threshold/timeout inputs disappear
  </how-to-verify>
  <verify>
    <automated>cd /d/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all 5 join types render correctly with conditional fields</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "n_of_m_join\|cancelling_join\|timeout_join" frontend/src/components/designer/PropertiesPanel.tsx` -- all 3 new types present
2. `grep -n "joinThreshold\|joinTimeoutHours" frontend/src/components/designer/PropertiesPanel.tsx` -- conditional inputs present
3. `cd frontend && npx tsc --noEmit` -- TypeScript compiles clean
</verification>

<success_criteria>
- All 5 join types selectable in designer dropdown
- Threshold input visible only for N-of-M and Cancelling joins
- Timeout input visible only for Timeout join
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/39-advanced-join-semantics/39-02-SUMMARY.md`
</output>
