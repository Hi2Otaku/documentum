---
phase: 10-delegation-work-queues-workflow-management
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - tests/test_delegation.py
  - tests/test_queues.py
  - tests/test_workflow_mgmt.py
  - tests/test_audit_query.py
autonomous: true
requirements:
  - USER-05
  - INBOX-08
  - QUEUE-01
  - QUEUE-02
  - QUEUE-03
  - QUEUE-04
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - AUDIT-05
must_haves:
  truths:
    - "All 12 Phase 10 requirements have at least one passing test"
    - "Delegation routing is tested end-to-end (unavailable user -> delegate gets work item)"
    - "Queue claim/release is tested with concurrent safety"
    - "Workflow halt/resume/abort/restart state transitions are tested"
    - "Audit query with filters returns correct results"
  artifacts:
    - path: "tests/test_delegation.py"
      provides: "Tests for USER-05 and INBOX-08"
      min_lines: 80
    - path: "tests/test_queues.py"
      provides: "Tests for QUEUE-01 through QUEUE-04"
      min_lines: 120
    - path: "tests/test_workflow_mgmt.py"
      provides: "Tests for MGMT-01 through MGMT-05"
      min_lines: 120
    - path: "tests/test_audit_query.py"
      provides: "Tests for AUDIT-05"
      min_lines: 60
  key_links:
    - from: "tests/test_delegation.py"
      to: "src/app/routers/users.py"
      via: "HTTP calls to /users/me/availability"
      pattern: "users/me/availability"
    - from: "tests/test_queues.py"
      to: "src/app/routers/queues.py"
      via: "HTTP calls to /queues/"
      pattern: "/queues/"
    - from: "tests/test_workflow_mgmt.py"
      to: "src/app/routers/workflows.py"
      via: "HTTP calls to /workflows/{id}/halt etc."
      pattern: "/halt|/resume|/abort|/restart"
    - from: "tests/test_audit_query.py"
      to: "src/app/routers/audit.py"
      via: "HTTP calls to /audit/"
      pattern: "/audit/"
---

<objective>
Create integration tests covering all 12 Phase 10 requirements: delegation (USER-05, INBOX-08), work queues (QUEUE-01 through QUEUE-04), workflow management (MGMT-01 through MGMT-05), and audit query (AUDIT-05).

Purpose: Verify that all Phase 10 features work correctly end-to-end through the API. These tests serve as the verification gate before phase completion.
Output: 4 test files with comprehensive coverage of all Phase 10 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-delegation-work-queues-workflow-management/10-CONTEXT.md
@.planning/phases/10-delegation-work-queues-workflow-management/10-RESEARCH.md
@.planning/phases/10-delegation-work-queues-workflow-management/10-01-SUMMARY.md
@.planning/phases/10-delegation-work-queues-workflow-management/10-02-SUMMARY.md
@.planning/phases/10-delegation-work-queues-workflow-management/10-03-SUMMARY.md

<interfaces>
<!-- Test infrastructure patterns from existing tests -->

From tests/conftest.py (session-scoped fixtures, SQLite in-memory):
```python
# Uses httpx AsyncClient with app fixture
# admin_token fixture provides JWT for superuser
# user_token fixture provides JWT for regular user
# db fixture provides AsyncSession
```

From tests/test_inbox.py (typical integration test pattern):
```python
import pytest
from httpx import AsyncClient

pytestmark = pytest.mark.asyncio

class TestInbox:
    async def test_list_inbox(self, client: AsyncClient, user_token: str):
        response = await client.get(
            "/api/v1/inbox",
            headers={"Authorization": f"Bearer {user_token}"},
        )
        assert response.status_code == 200
```

From tests/test_workflows.py (workflow setup helper pattern):
```python
# Creates template via API, installs it, starts workflow
# Then tests operations on the running workflow
```

API prefix: `/api/v1`
Auth header: `{"Authorization": f"Bearer {token}"}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegation and work queue tests</name>
  <files>tests/test_delegation.py, tests/test_queues.py</files>
  <read_first>tests/conftest.py, tests/test_inbox.py, tests/test_workflows.py, src/app/routers/users.py, src/app/routers/queues.py, src/app/services/engine_service.py, src/app/schemas/user.py, src/app/schemas/queue.py</read_first>
  <action>
1. Create `tests/test_delegation.py` covering USER-05 and INBOX-08:

   ```python
   """Integration tests for delegation (USER-05, INBOX-08)."""
   import pytest
   from httpx import AsyncClient

   pytestmark = pytest.mark.asyncio
   ```

   Test cases:
   a. `test_set_unavailable_with_delegate` (USER-05):
      - Create two users (user A and delegate B) via admin API
      - Get tokens for user A
      - PUT `/api/v1/users/me/availability` with `{"is_available": false, "delegate_id": B.id}`
      - Assert 200, response shows is_available=false, delegate_id=B.id

   b. `test_set_available_again` (USER-05):
      - After setting unavailable, PUT with `{"is_available": true}`
      - Assert is_available=true, delegate_id=null

   c. `test_cannot_delegate_to_self` (USER-05):
      - PUT with delegate_id = self
      - Assert 400

   d. `test_unavailable_requires_delegate` (USER-05):
      - PUT with `{"is_available": false}` (no delegate_id)
      - Assert 400

   e. `test_delegation_routing` (INBOX-08):
      - Create user A and delegate B
      - Set user A as unavailable with delegate B
      - Create a workflow template with a manual activity assigned to user A (performer_type=user, performer_id=A.id)
      - Install template, start workflow
      - Check user B's inbox: should have the work item (auto-routed)
      - Check user A's inbox: should NOT have the item
      - Verify audit trail contains "work_item_delegated" entry

   f. `test_delegation_does_not_affect_existing_items` (INBOX-08):
      - Start workflow with item assigned to user A (while A is available)
      - Then set A as unavailable
      - Verify A still has the existing item (not reassigned)

2. Create `tests/test_queues.py` covering QUEUE-01 through QUEUE-04:

   Test cases:
   a. `test_create_queue` (QUEUE-01):
      - POST `/api/v1/queues/` with `{"name": "Review Queue", "description": "..."}`
      - Assert 201, response has id, name, is_active=true

   b. `test_create_queue_duplicate_name` (QUEUE-01):
      - Create queue, then try creating with same name
      - Assert 400

   c. `test_list_queues` (QUEUE-01):
      - Create 2 queues, GET `/api/v1/queues/`
      - Assert 200, data has 2 items, pagination meta present

   d. `test_update_queue` (QUEUE-01):
      - Create queue, PUT with new name
      - Assert 200, name updated

   e. `test_delete_queue` (QUEUE-01):
      - Create queue, DELETE
      - Assert 204, GET returns 404 or empty

   f. `test_add_remove_member` (QUEUE-01):
      - Create queue, create user
      - POST `/api/v1/queues/{id}/members` with `{"user_id": user.id}`
      - Assert 201
      - GET queue detail: member appears
      - DELETE `/api/v1/queues/{id}/members/{user.id}`
      - Assert 204, member removed

   g. `test_queue_admin_only` (QUEUE-01):
      - Try queue CRUD with non-admin token
      - Assert 403

   h. `test_queue_performer_creates_shared_item` (QUEUE-02):
      - Create queue, add members A and B
      - Create template with manual activity: performer_type="queue", performer_id=queue.id
      - Install template, start workflow
      - Check: exactly ONE work item exists for the activity
      - That item has performer_id=None and queue_id=queue.id

   i. `test_queue_member_can_claim` (QUEUE-03):
      - After test_queue_performer_creates_shared_item setup
      - User A (queue member) calls POST `/api/v1/inbox/{item_id}/acquire`
      - Assert success, item now has performer_id=A.id, state=ACQUIRED

   j. `test_non_member_cannot_claim` (QUEUE-03):
      - Setup queue item, try claiming with user who is NOT a queue member
      - Assert 400 "Not authorized"

   k. `test_claimed_item_locked` (QUEUE-04):
      - User A claims the item
      - User B tries to claim same item
      - Assert 400 (already acquired)

   l. `test_queue_item_visible_in_inbox` (QUEUE-03):
      - Create queue with member A, start workflow with queue activity
      - GET `/api/v1/inbox` as user A
      - Assert queue item appears in inbox list

   m. `test_release_claimed_queue_item` (QUEUE-04):
      - Claim item, then release
      - Item returns to AVAILABLE, performer_id back to None
      - Other members can now claim it
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/src && python -m pytest tests/test_delegation.py tests/test_queues.py -x -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_delegation.py` exists with at least 5 test functions covering USER-05 and INBOX-08
    - `tests/test_queues.py` exists with at least 10 test functions covering QUEUE-01 through QUEUE-04
    - `tests/test_delegation.py` contains test for delegation routing (unavailable user -> delegate receives work item)
    - `tests/test_queues.py` contains test for queue item visibility in inbox
    - `tests/test_queues.py` contains test for claim locking (second claim fails)
    - All tests pass: `cd src && python -m pytest tests/test_delegation.py tests/test_queues.py -x` exits 0
  </acceptance_criteria>
  <done>Delegation tests verify availability toggle, delegation routing, and non-reassignment of existing items. Queue tests verify CRUD, member management, shared item creation, claim/release, lock enforcement, and inbox visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow management and audit query tests</name>
  <files>tests/test_workflow_mgmt.py, tests/test_audit_query.py</files>
  <read_first>tests/conftest.py, tests/test_workflows.py, src/app/routers/workflows.py, src/app/routers/audit.py, src/app/services/workflow_mgmt_service.py, src/app/schemas/workflow.py, src/app/schemas/audit.py</read_first>
  <action>
1. Create `tests/test_workflow_mgmt.py` covering MGMT-01 through MGMT-05:

   Helper: Create a reusable async helper that creates a template with start->manual->end, installs it, and starts a workflow. Return the workflow_id. Pattern from existing test_workflows.py.

   Test cases:
   a. `test_halt_running_workflow` (MGMT-01):
      - Start a workflow (state=RUNNING)
      - POST `/api/v1/workflows/{id}/halt` with admin token
      - Assert 200, state=HALTED
      - Verify work items changed to SUSPENDED state

   b. `test_halt_non_running_fails` (MGMT-01):
      - Start workflow, halt it, try halting again
      - Assert 400 "Invalid state transition"

   c. `test_resume_halted_workflow` (MGMT-02):
      - Start workflow, halt it
      - POST `/api/v1/workflows/{id}/resume` with admin token
      - Assert 200, state=RUNNING
      - Verify work items restored to AVAILABLE

   d. `test_resume_non_halted_fails` (MGMT-02):
      - Start workflow (RUNNING), try resuming
      - Assert 400

   e. `test_abort_running_workflow` (MGMT-03):
      - Start workflow
      - POST `/api/v1/workflows/{id}/abort` with admin token
      - Assert 200, state=FAILED

   f. `test_abort_halted_workflow` (MGMT-03):
      - Start workflow, halt, then abort
      - Assert 200, state=FAILED

   g. `test_restart_failed_workflow` (MGMT-05):
      - Start workflow, abort (FAILED)
      - POST `/api/v1/workflows/{id}/restart` with admin token
      - Assert 200, state=DORMANT
      - Verify work items deleted, activity instances reset to DORMANT
      - Verify process variables preserved

   h. `test_restart_non_failed_fails` (MGMT-05):
      - Start workflow (RUNNING), try restarting
      - Assert 400

   i. `test_list_workflows_filtered` (MGMT-04):
      - Start 3 workflows from different templates
      - Halt one, abort another
      - GET `/api/v1/workflows/admin/list?state=running` with admin token
      - Assert only running workflow returned
      - GET with template_id filter, assert correct filtering
      - GET with no filters, assert all returned with pagination

   j. `test_admin_only_enforcement` (MGMT-01-05):
      - Try halt/resume/abort/restart with non-admin token
      - Assert 403 for each

2. Create `tests/test_audit_query.py` covering AUDIT-05:

   Test cases:
   a. `test_query_audit_no_filters` (AUDIT-05):
      - Perform some auditable actions (create user, start workflow)
      - GET `/api/v1/audit/` with admin token
      - Assert 200, data is list of audit records, pagination meta present

   b. `test_query_audit_by_action_type` (AUDIT-05):
      - GET `/api/v1/audit/?action_type=user_created`
      - Assert only user_created records returned

   c. `test_query_audit_by_user_id` (AUDIT-05):
      - GET `/api/v1/audit/?user_id={admin_id}`
      - Assert all records have matching user_id

   d. `test_query_audit_by_date_range` (AUDIT-05):
      - GET with date_from and date_to
      - Assert records within range

   e. `test_query_audit_pagination` (AUDIT-05):
      - GET with skip=0&limit=2, then skip=2&limit=2
      - Assert pagination works, different records

   f. `test_audit_query_admin_only` (AUDIT-05):
      - Try with non-admin token
      - Assert 403
  </action>
  <verify>
    <automated>cd /d/Python/documentum_clone/src && python -m pytest tests/test_workflow_mgmt.py tests/test_audit_query.py -x -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_workflow_mgmt.py` exists with at least 9 test functions covering MGMT-01 through MGMT-05
    - `tests/test_audit_query.py` exists with at least 5 test functions covering AUDIT-05
    - `tests/test_workflow_mgmt.py` contains halt->resume->abort->restart lifecycle test
    - `tests/test_workflow_mgmt.py` contains filtered list test with state filter
    - `tests/test_audit_query.py` contains pagination test
    - All tests pass: `cd src && python -m pytest tests/test_workflow_mgmt.py tests/test_audit_query.py -x` exits 0
    - Full suite passes: `cd src && python -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Workflow management tests cover all state transitions (halt, resume, abort, restart), filtered listing, and admin-only enforcement. Audit query tests cover all filter params, pagination, and admin-only access. Full test suite green.</done>
</task>

</tasks>

<verification>
- `cd src && python -m pytest tests/test_delegation.py tests/test_queues.py tests/test_workflow_mgmt.py tests/test_audit_query.py -v --tb=short` all pass
- `cd src && python -m pytest tests/ -x -q` full suite passes
- Each requirement ID (USER-05, INBOX-08, QUEUE-01-04, MGMT-01-05, AUDIT-05) has at least one test
</verification>

<success_criteria>
- All 12 Phase 10 requirements have passing integration tests
- No regressions in existing test suite
- Tests cover both happy paths and error cases (wrong state, unauthorized, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/10-delegation-work-queues-workflow-management/10-04-SUMMARY.md`
</output>
