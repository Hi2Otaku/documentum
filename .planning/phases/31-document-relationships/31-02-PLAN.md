---
phase: 31-document-relationships
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - frontend/src/api/documentRelationships.ts
  - frontend/src/components/documents/DocumentRelationshipsPanel.tsx
  - frontend/src/components/documents/AddRelationshipDialog.tsx
  - frontend/src/components/documents/DocumentDetailPanel.tsx
autonomous: true
requirements:
  - REL-02
  - REL-03

must_haves:
  truths:
    - "User can see all relationships for a document grouped by direction (Outgoing / Incoming)"
    - "User can add a relationship via dialog with document search, type dropdown, and optional description"
    - "User can remove a relationship via remove button"
    - "User can click a related document title to navigate to that document's detail view"
    - "Empty state is shown when a document has no relationships"
  artifacts:
    - path: "frontend/src/api/documentRelationships.ts"
      provides: "API client with query key factory"
      exports: ["relationshipKeys", "fetchRelationships", "createRelationship", "deleteRelationship"]
    - path: "frontend/src/components/documents/DocumentRelationshipsPanel.tsx"
      provides: "Relationships panel component"
      contains: "DocumentRelationshipsPanel"
    - path: "frontend/src/components/documents/AddRelationshipDialog.tsx"
      provides: "Add relationship dialog"
      contains: "AddRelationshipDialog"
    - path: "frontend/src/components/documents/DocumentDetailPanel.tsx"
      provides: "Updated detail panel with relationships section"
      contains: "DocumentRelationshipsPanel"
  key_links:
    - from: "frontend/src/components/documents/DocumentRelationshipsPanel.tsx"
      to: "frontend/src/api/documentRelationships.ts"
      via: "useQuery + useMutation"
      pattern: "fetchRelationships|deleteRelationship"
    - from: "frontend/src/components/documents/AddRelationshipDialog.tsx"
      to: "frontend/src/api/documentRelationships.ts"
      via: "useMutation"
      pattern: "createRelationship"
    - from: "frontend/src/components/documents/DocumentDetailPanel.tsx"
      to: "frontend/src/components/documents/DocumentRelationshipsPanel.tsx"
      via: "component import"
      pattern: "DocumentRelationshipsPanel"
---

<objective>
Build the frontend for document relationships: API client, relationships panel, add dialog, and integration into the document detail view.

Purpose: Enables users to view, create, delete, and navigate document relationships directly from the document detail panel (per D-09 through D-12).

Output: DocumentRelationshipsPanel visible in DocumentDetailPanel showing grouped relationships with clickable navigation links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-document-relationships/31-CONTEXT.md
@.planning/phases/31-document-relationships/31-RESEARCH.md
@.planning/phases/31-document-relationships/31-01-SUMMARY.md

<interfaces>
<!-- API contract from Plan 01 backend -->

GET /api/v1/documents/{document_id}/relationships
Response: { data: RelationshipResponse[] }

POST /api/v1/documents/{document_id}/relationships
Body: { target_document_id: string, relationship_type: string, description?: string }
Response: { data: RelationshipResponse }

DELETE /api/v1/documents/{document_id}/relationships/{relationship_id}
Response: 204

RelationshipResponse:
```typescript
interface RelationshipResponse {
  id: string;
  source_document_id: string;
  target_document_id: string;
  relationship_type: string; // "supersedes" | "references" | "amends" | "attachment_of" | "related_to"
  description: string | null;
  direction: "outgoing" | "incoming";
  related_document_id: string;
  related_document_title: string;
  created_at: string;
  created_by: string | null;
}
```

From frontend/src/api/folders.ts (pattern to follow):
```typescript
export const folderKeys = {
  all: ["folders"] as const,
  tree: () => [...folderKeys.all, "tree"] as const,
};
async function apiFetch<T>(url: string): Promise<T> { ... }
async function apiMutate<T>(url, method, body?): Promise<T> { ... }
```

From frontend/src/components/documents/DocumentDetailPanel.tsx:
```typescript
interface DocumentDetailPanelProps {
  documentId: string | null;
  currentUserId: string;
}
// Integration point: add new section after Folders section (Section 7)
```

Available shadcn components: Badge, Button, Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, Select, SelectTrigger, SelectValue, SelectContent, SelectItem, Input, Label, Textarea, Separator, Card, CardHeader, CardTitle, CardContent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client and DocumentRelationshipsPanel component</name>
  <files>
    frontend/src/api/documentRelationships.ts,
    frontend/src/components/documents/DocumentRelationshipsPanel.tsx
  </files>
  <read_first>
    frontend/src/api/folders.ts,
    frontend/src/api/documents.ts,
    frontend/src/components/documents/DocumentDetailPanel.tsx,
    frontend/src/api/handle401.ts
  </read_first>
  <action>
1. Create `frontend/src/api/documentRelationships.ts` following the `folders.ts` pattern exactly:
   - Copy `authHeaders()`, `apiFetch<T>()`, `apiMutate<T>()` helpers (or import shared ones if they exist)
   - Add `apiDelete()` helper for DELETE requests (return void, check 401/ok)
   - Query key factory: `relationshipKeys = { all: ["document-relationships"], list: (docId) => [...all, docId] }`
   - `fetchRelationships(documentId: string): Promise<RelationshipResponse[]>` — GET, extract `.data`
   - `createRelationship(documentId, data: { target_document_id, relationship_type, description? }): Promise<RelationshipResponse>` — POST, extract `.data`
   - `deleteRelationship(documentId, relationshipId): Promise<void>` — DELETE
   - Export `RelationshipResponse` interface with all fields from the API contract

2. Create `frontend/src/components/documents/DocumentRelationshipsPanel.tsx` (per D-09, D-10, D-12):
   - Props: `{ documentId: string }`
   - Use `useQuery` with `relationshipKeys.list(documentId)` to fetch relationships
   - Group relationships by `direction` field: "outgoing" and "incoming" sections
   - Each section header: "Outgoing" / "Incoming" with count
   - Each relationship row shows:
     - `Badge` with `relationship_type` (formatted: underscore to space, capitalize — e.g., "attachment_of" -> "Attachment Of")
     - Clickable `related_document_title` as a link (use React Router `useNavigate` or an `onClick` prop)
     - Optional `description` in muted text if present
     - Remove button (`X` icon) that calls `deleteRelationship` via `useMutation`, then invalidates `relationshipKeys.list(documentId)`
   - Empty state (Claude's discretion): show "No relationships" text with a subtle icon when list is empty
   - "Add Relationship" button at top (per D-11) that sets state to open AddRelationshipDialog (dialog created in Task 2)
   - For navigation (per D-12): accept an `onNavigate?: (documentId: string) => void` prop. When a related document title is clicked, call `onNavigate(related_document_id)`. The parent (DocumentDetailPanel) will handle changing the selected document.
   - Loading state: use Skeleton components
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "relationshipKeys" frontend/src/api/documentRelationships.ts
    - grep "fetchRelationships" frontend/src/api/documentRelationships.ts
    - grep "createRelationship" frontend/src/api/documentRelationships.ts
    - grep "deleteRelationship" frontend/src/api/documentRelationships.ts
    - grep "DocumentRelationshipsPanel" frontend/src/components/documents/DocumentRelationshipsPanel.tsx
    - grep "direction" frontend/src/components/documents/DocumentRelationshipsPanel.tsx
    - grep "onNavigate" frontend/src/components/documents/DocumentRelationshipsPanel.tsx
  </acceptance_criteria>
  <done>API client exports fetch/create/delete functions with query keys; DocumentRelationshipsPanel renders grouped relationships with remove buttons, navigation links, and empty state; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: AddRelationshipDialog and DocumentDetailPanel integration</name>
  <files>
    frontend/src/components/documents/AddRelationshipDialog.tsx,
    frontend/src/components/documents/DocumentDetailPanel.tsx
  </files>
  <read_first>
    frontend/src/components/documents/DocumentRelationshipsPanel.tsx,
    frontend/src/components/documents/DocumentDetailPanel.tsx,
    frontend/src/api/documentRelationships.ts,
    frontend/src/api/documents.ts
  </read_first>
  <action>
1. Create `frontend/src/components/documents/AddRelationshipDialog.tsx` (per D-11):
   - Props: `{ open: boolean, onOpenChange: (open: boolean) => void, documentId: string }`
   - Dialog with shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter`
   - Form fields:
     - **Document search**: `Input` field for searching documents by title. Use existing `/api/v1/documents/?title=...` endpoint (from documents.ts or inline fetch). Show results in a simple list/dropdown below the input. User clicks to select. Show selected document title.
     - **Relationship type**: `Select` dropdown with the 5 types: Supersedes, References, Amends, Attachment Of, Related To (values: supersedes, references, amends, attachment_of, related_to)
     - **Description**: optional `Textarea`
   - Submit button calls `createRelationship` via `useMutation`
   - On success: invalidate `relationshipKeys.list(documentId)`, close dialog, reset form
   - On error: show error message (from API response)
   - Validation: disable submit until document selected and type chosen
   - Filter out the current document from search results (prevent self-relationship at UI level)

2. Update `frontend/src/components/documents/DocumentDetailPanel.tsx`:
   - Import `DocumentRelationshipsPanel` from `./DocumentRelationshipsPanel`
   - Add a new section after the Folders section (Section 7), following the same pattern:
     - `<Separator />`
     - Section with `<DocumentRelationshipsPanel documentId={documentId} onNavigate={...} />`
   - For `onNavigate`: This component receives `documentId` as a prop. The simplest approach is to use React Router's `useNavigate` to navigate to `/documents?selected={related_document_id}`, or if the parent manages selection state, pass a callback. Check how the documents page handles document selection and wire accordingly. If there is a `setSelectedDocumentId` or similar in a parent, use that. Otherwise navigate via URL.
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "AddRelationshipDialog" frontend/src/components/documents/AddRelationshipDialog.tsx
    - grep "relationship_type" frontend/src/components/documents/AddRelationshipDialog.tsx
    - grep "createRelationship" frontend/src/components/documents/AddRelationshipDialog.tsx
    - grep "DocumentRelationshipsPanel" frontend/src/components/documents/DocumentDetailPanel.tsx
    - grep "Relationships" frontend/src/components/documents/DocumentDetailPanel.tsx
  </acceptance_criteria>
  <done>AddRelationshipDialog provides document search, type selection, and description input; DocumentDetailPanel shows relationships section with full CRUD; TypeScript compiles clean; clicking a related document navigates to it</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles without errors
cd D:/Python/documentum_clone/frontend && npx tsc --noEmit

# All new files exist
ls frontend/src/api/documentRelationships.ts
ls frontend/src/components/documents/DocumentRelationshipsPanel.tsx
ls frontend/src/components/documents/AddRelationshipDialog.tsx

# Build succeeds
cd D:/Python/documentum_clone/frontend && npm run build
```
</verification>

<success_criteria>
- API client with fetch/create/delete and query key factory exists
- DocumentRelationshipsPanel shows relationships grouped by direction with navigation links and remove buttons
- AddRelationshipDialog provides document search, type dropdown, and optional description
- DocumentDetailPanel includes the relationships section after folders
- Clicking a related document navigates to its detail view (per D-12)
- Frontend builds and TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/31-document-relationships/31-02-SUMMARY.md`
</output>
