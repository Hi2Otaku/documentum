---
phase: 37-workflow-error-handling-compensation
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - frontend/src/components/designer/PropertiesPanel.tsx
  - frontend/src/hooks/useSaveTemplate.ts
  - frontend/src/pages/DesignerPage.tsx
autonomous: true
requirements:
  - WFERR-01
  - WFERR-02

must_haves:
  truths:
    - "Template designer can select an error handler activity for any activity in the properties panel"
    - "Template designer can select a compensation activity for any activity in the properties panel"
    - "Selected error handler and compensation activity IDs are saved to the backend when template is saved"
  artifacts:
    - path: "frontend/src/components/designer/PropertiesPanel.tsx"
      provides: "Error handler and compensation dropdowns in properties panel"
      contains: "errorHandlerActivityId"
    - path: "frontend/src/hooks/useSaveTemplate.ts"
      provides: "Saves error_handler_activity_id and compensation_activity_id to backend"
      contains: "error_handler_activity_id"
  key_links:
    - from: "frontend/src/components/designer/PropertiesPanel.tsx"
      to: "frontend/src/hooks/useSaveTemplate.ts"
      via: "designerStore node data -> save payload"
      pattern: "errorHandlerActivityId"
    - from: "frontend/src/hooks/useSaveTemplate.ts"
      to: "/api/v1/templates/{id}/activities"
      via: "PUT/POST with error_handler_activity_id"
      pattern: "error_handler_activity_id"
---

<objective>
Add error handler and compensation activity selection to the visual workflow designer.

Purpose: Template designers need to attach error handlers and compensation activities to workflow activities through the drag-and-drop designer UI, completing the design-time configuration for WFERR-01 and WFERR-02.
Output: Updated PropertiesPanel with dropdowns, updated save hook to persist selections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-workflow-error-handling-compensation/37-01-SUMMARY.md
@frontend/src/components/designer/PropertiesPanel.tsx
@frontend/src/hooks/useSaveTemplate.ts
@frontend/src/pages/DesignerPage.tsx
@frontend/src/types/designer.ts

<interfaces>
From frontend/src/types/designer.ts (after Plan 01):
```typescript
export interface ActivityNodeData extends Record<string, unknown> {
  name: string;
  activityType: 'start' | 'end' | 'manual' | 'auto' | 'sub_workflow' | 'event';
  // ... existing fields ...
  errorHandlerActivityId?: string | null;
  compensationActivityId?: string | null;
}
```

From frontend/src/hooks/useSaveTemplate.ts:
```typescript
// Creates/updates activities with payload including all ActivityNodeData fields
// Maps camelCase node data to snake_case API fields
// e.g., escalationAction -> escalation_action
```

From frontend/src/pages/DesignerPage.tsx:
```typescript
// activitiesToNodes converts backend response to React Flow nodes
// Reads activity fields and maps to ActivityNodeData
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Designer properties panel dropdowns + save hook + node conversion</name>
  <read_first>
    - frontend/src/components/designer/PropertiesPanel.tsx (full file)
    - frontend/src/hooks/useSaveTemplate.ts (full file)
    - frontend/src/pages/DesignerPage.tsx (activitiesToNodes function, lines 22-54)
    - frontend/src/types/designer.ts
  </read_first>
  <files>
    frontend/src/components/designer/PropertiesPanel.tsx,
    frontend/src/hooks/useSaveTemplate.ts,
    frontend/src/pages/DesignerPage.tsx
  </files>
  <action>
    1. In `frontend/src/pages/DesignerPage.tsx`, update `activitiesToNodes` to include:
       ```
       errorHandlerActivityId: a.error_handler_activity_id ?? null,
       compensationActivityId: a.compensation_activity_id ?? null,
       ```

    2. In `frontend/src/components/designer/PropertiesPanel.tsx`, add an "Error Handling" section in the activity properties area (after the Timer/Escalation section, before the end of the form):
       - Only show for manual and auto activity types (not start/end)
       - Add a heading: "Error Handling" with a ShieldAlert icon
       - Add a dropdown "Error Handler Activity":
         - Options: "None" + all other activities in the current template (from designerStore nodes, excluding the current node and excluding start/end nodes)
         - Value: `data.errorHandlerActivityId ?? ''`
         - onChange: `updateNodeData(nodeId, { errorHandlerActivityId: e.target.value || null })`
         - Helper text: "Activity to execute if this activity fails"
       - Add a dropdown "Compensation Activity":
         - Same options list as error handler
         - Value: `data.compensationActivityId ?? ''`
         - onChange: `updateNodeData(nodeId, { compensationActivityId: e.target.value || null })`
         - Helper text: "Activity to undo this activity's work during compensation"
       - To get the list of other activities, use `useDesignerStore((s) => s.nodes)` and filter to get node id + name pairs, excluding current node and start/end types

    3. In `frontend/src/hooks/useSaveTemplate.ts`, add to both the create and update activity payloads:
       ```
       error_handler_activity_id: data.errorHandlerActivityId ?? null,
       compensation_activity_id: data.compensationActivityId ?? null,
       ```
       Place after the existing `warning_threshold_hours` field mapping.

       IMPORTANT: The error_handler_activity_id and compensation_activity_id reference other activity IDs. When creating new activities, these IDs may not exist yet (they reference the frontend node IDs). The save hook must handle this:
       - On first save pass, save all activities WITHOUT error_handler/compensation IDs
       - After all activities have backend IDs (from newIdMap), do a SECOND pass to update activities that have error_handler_activity_id or compensation_activity_id, mapping frontend node IDs to backend IDs via newIdMap
       - This is similar to how flows reference activity IDs that must exist first

       Actually, check the existing save logic: if the save hook already creates activities first then flows (which reference activity IDs), the same pattern applies. The error_handler_activity_id and compensation_activity_id are self-referential FKs within the same template. The simplest approach:
       - In the initial create/update calls, send `error_handler_activity_id: null` and `compensation_activity_id: null`
       - After all activities are created/updated and newIdMap is populated, iterate nodes that have errorHandlerActivityId or compensationActivityId set, resolve the IDs through newIdMap, and make PATCH/PUT calls to update those specific activities with the resolved IDs
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20 || echo "typecheck done"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "errorHandlerActivityId" frontend/src/components/designer/PropertiesPanel.tsx
    - grep -q "compensationActivityId" frontend/src/components/designer/PropertiesPanel.tsx
    - grep -q "Error Handler" frontend/src/components/designer/PropertiesPanel.tsx
    - grep -q "Compensation" frontend/src/components/designer/PropertiesPanel.tsx
    - grep -q "error_handler_activity_id" frontend/src/hooks/useSaveTemplate.ts
    - grep -q "compensation_activity_id" frontend/src/hooks/useSaveTemplate.ts
    - grep -q "errorHandlerActivityId" frontend/src/pages/DesignerPage.tsx
    - grep -q "compensationActivityId" frontend/src/pages/DesignerPage.tsx
  </acceptance_criteria>
  <done>Template designer can select error handler and compensation activities from dropdowns in the properties panel. Selections persist through template save with proper ID resolution for self-referential FKs.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes (or only pre-existing errors)
- PropertiesPanel shows Error Handling section for manual/auto activities
- Dropdowns list other activities in the template (excluding current, start, end)
- Save hook sends error_handler_activity_id and compensation_activity_id to backend
- DesignerPage loads existing error handler/compensation settings from backend
</verification>

<success_criteria>
Template designers can attach error handlers and compensation activities to any manual or auto activity in the visual workflow designer. Settings are loaded from backend and saved correctly with self-referential FK resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/37-workflow-error-handling-compensation/37-03-SUMMARY.md`
</output>
