---
phase: 37-workflow-error-handling-compensation
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/app/routers/workflows.py
  - frontend/src/components/workflows/WorkflowDetailPanel.tsx
  - frontend/src/components/workflows/AdminActionBar.tsx
  - frontend/src/components/workflows/ActivityErrorPanel.tsx
  - frontend/src/api/workflows.ts
autonomous: true
requirements:
  - WFERR-04

must_haves:
  truths:
    - "Failed activities display error message and error details in the workflow operations UI"
    - "Operator can click Retry on a failed activity to reset it to ACTIVE state"
    - "Operator can click Skip on a failed activity to mark it COMPLETE and advance the workflow"
    - "Operator can trigger compensation from the workflow detail panel"
  artifacts:
    - path: "frontend/src/components/workflows/ActivityErrorPanel.tsx"
      provides: "Error details display with retry/skip/compensate buttons"
      contains: "ActivityErrorPanel"
    - path: "src/app/routers/workflows.py"
      provides: "Compensation trigger endpoint"
      contains: "trigger_compensation"
  key_links:
    - from: "frontend/src/components/workflows/ActivityErrorPanel.tsx"
      to: "/api/v1/workflows/{id}/activities/{id}/retry"
      via: "fetch POST"
      pattern: "retry"
    - from: "frontend/src/components/workflows/ActivityErrorPanel.tsx"
      to: "/api/v1/workflows/{id}/compensate"
      via: "fetch POST"
      pattern: "compensate"
---

<objective>
Add operator-facing UI for viewing error details on failed activities, retrying/skipping them, and triggering workflow compensation.

Purpose: Operators need to see why activities failed and take corrective action (retry, skip, or trigger compensation rollback) from the workflow operations page.
Output: ActivityErrorPanel component, updated WorkflowDetailPanel with error info, compensation trigger endpoint and button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-workflow-error-handling-compensation/37-01-SUMMARY.md
@src/app/routers/workflows.py
@frontend/src/components/workflows/WorkflowDetailPanel.tsx
@frontend/src/components/workflows/AdminActionBar.tsx
@frontend/src/api/workflows.ts

<interfaces>
From src/app/schemas/workflow.py (after Plan 01):
```python
class ActivityInstanceResponse(BaseModel):
    id: uuid.UUID
    workflow_instance_id: uuid.UUID
    activity_template_id: uuid.UUID
    state: ActivityState
    started_at: datetime | None = None
    completed_at: datetime | None = None
    error_message: str | None = None
    error_details: dict | None = None
    created_at: datetime

class ActivityRetryResponse(BaseModel):
    activity_instance_id: uuid.UUID
    status: str  # "requeued" or "skipped"
    message: str
```

From src/app/services/engine_service.py (after Plan 01):
```python
async def trigger_compensation(db, workflow_id, user_id) -> WorkflowInstance
```

Existing retry endpoint: POST /{workflow_id}/activities/{activity_id}/retry
Existing skip endpoint: POST /{workflow_id}/activities/{activity_id}/skip
Both already work for auto activities in ERROR state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compensation endpoint + extend retry/skip to manual activities</name>
  <read_first>
    - src/app/routers/workflows.py
    - src/app/services/engine_service.py (grep for handle_activity_error, trigger_compensation)
    - src/app/schemas/workflow.py
    - frontend/src/api/workflows.ts
  </read_first>
  <files>
    src/app/routers/workflows.py,
    frontend/src/api/workflows.ts
  </files>
  <action>
    1. In `src/app/routers/workflows.py`, add a POST endpoint `/{workflow_id}/compensate`:
       - Requires current_user (admin or supervisor)
       - Calls `engine_service.trigger_compensation(db, workflow_id, str(current_user.id))`
       - Returns `EnvelopeResponse[WorkflowActionResponse]` with message "Compensation initiated"
       - Catches ValueError and returns 400

    2. The existing retry_auto_activity and skip_auto_activity endpoints already handle ERROR state activities.
       Verify they work for both auto AND manual activities (they should since they only check ActivityState.ERROR).
       If the skip endpoint only handles auto activities (check if it looks up AutoActivityLog specifically), make it also work for manual activities by making the AutoActivityLog lookup optional.

    3. In `frontend/src/api/workflows.ts`, add:
       - `export async function retryActivity(workflowId: string, activityId: string)` -- POST to `/api/v1/workflows/{workflowId}/activities/{activityId}/retry`
       - `export async function skipActivity(workflowId: string, activityId: string)` -- POST to `/api/v1/workflows/{workflowId}/activities/{activityId}/skip`
       - `export async function triggerCompensation(workflowId: string)` -- POST to `/api/v1/workflows/{workflowId}/compensate`
       - Follow existing patterns (authHeaders, envelope response)
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && python -c "from app.routers.workflows import router; routes = [r.path for r in router.routes]; assert '/{workflow_id}/compensate' in routes, f'Missing compensate route in {routes}'; print('route ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "compensate" src/app/routers/workflows.py
    - grep -q "trigger_compensation" src/app/routers/workflows.py
    - grep -q "triggerCompensation" frontend/src/api/workflows.ts
    - grep -q "retryActivity" frontend/src/api/workflows.ts
    - grep -q "skipActivity" frontend/src/api/workflows.ts
  </acceptance_criteria>
  <done>Compensation trigger endpoint exists. Frontend API functions for retry, skip, and compensate are available.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityErrorPanel component + integrate into WorkflowDetailPanel</name>
  <read_first>
    - frontend/src/components/workflows/WorkflowDetailPanel.tsx
    - frontend/src/components/workflows/AdminActionBar.tsx
    - frontend/src/api/workflows.ts (after Task 1 changes)
  </read_first>
  <files>
    frontend/src/components/workflows/ActivityErrorPanel.tsx,
    frontend/src/components/workflows/WorkflowDetailPanel.tsx,
    frontend/src/components/workflows/AdminActionBar.tsx
  </files>
  <action>
    1. Create `frontend/src/components/workflows/ActivityErrorPanel.tsx`:
       - Props: `{ workflowId: string; activities: ActivityInstance[] }` where ActivityInstance has id, state, error_message, error_details, activity_template_id
       - Filter activities to those in "error" state
       - If none in error, return null (don't render)
       - For each errored activity, render a Card with:
         - Activity name (from activity_template_id lookup or just show ID with label)
         - Red AlertTriangle icon + "Error" badge
         - error_message in a text block
         - Collapsible error_details section (JSON formatted in a <pre> block if present)
         - Two buttons: "Retry" (RefreshCw icon) and "Skip" (SkipForward icon)
         - Retry calls retryActivity(workflowId, activityId), shows toast, invalidates queries
         - Skip calls skipActivity(workflowId, activityId), shows toast, invalidates queries
       - Use useMutation from @tanstack/react-query for both actions
       - Import shadcn Button, Card, CardContent, Collapsible components
       - Use lucide-react icons: AlertTriangle, RefreshCw, SkipForward

    2. Update `frontend/src/components/workflows/WorkflowDetailPanel.tsx`:
       - Import ActivityErrorPanel
       - In the detail view, after the activity instances list/tab, add `<ActivityErrorPanel>` passing workflowId and the activity_instances array from the workflow detail response
       - This shows error details inline in the workflow detail panel

    3. Update `frontend/src/components/workflows/AdminActionBar.tsx`:
       - Add a "Compensate" button (Undo icon from lucide-react) visible when workflow state is "running" or "failed"
       - Button calls triggerCompensation(workflowId) via useMutation
       - On success: invalidate queries, toast "Compensation initiated"
       - On error: toast error message
  </action>
  <verify>
    <automated>cd D:/Python/documentum_clone && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20 || echo "typecheck done"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ActivityErrorPanel" frontend/src/components/workflows/ActivityErrorPanel.tsx
    - grep -q "retryActivity" frontend/src/components/workflows/ActivityErrorPanel.tsx
    - grep -q "skipActivity" frontend/src/components/workflows/ActivityErrorPanel.tsx
    - grep -q "ActivityErrorPanel" frontend/src/components/workflows/WorkflowDetailPanel.tsx
    - grep -q "Compensate" frontend/src/components/workflows/AdminActionBar.tsx
    - grep -q "triggerCompensation" frontend/src/components/workflows/AdminActionBar.tsx
  </acceptance_criteria>
  <done>Failed activities show error message/details in workflow detail panel. Retry and Skip buttons work. Compensate button available for admins on running/failed workflows.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes (or only pre-existing errors)
- ActivityErrorPanel renders for errored activities with retry/skip buttons
- Compensate button appears in AdminActionBar for running/failed workflows
- API functions exist for retry, skip, and compensate
- Backend compensate endpoint calls trigger_compensation
</verification>

<success_criteria>
Operators can see error details on failed activities in the workflow operations UI, retry or skip them, and trigger compensation rollback. All UI integrated into existing WorkflowDetailPanel.
</success_criteria>

<output>
After completion, create `.planning/phases/37-workflow-error-handling-compensation/37-02-SUMMARY.md`
</output>
